# Cambridge Starters Practice - 環境變數範例
#
# 1. 複製此檔為 `.env.local`（已被 .gitignore 排除）
# 2. 填入實際的 secret value
# 3. **絕對不要把 .env / .env.local commit 進 git**
#
# OpenAI TTS 試產用（屬 P3-9-C / P2-4C-2B-2 範圍）：
#   - 用於 `scripts/generate_openai_tts_sample.mjs`
#   - 只用於把自製文字（transcript / ttsScript）轉成自製音檔
#   - 嚴禁用於下載官方音檔 / 上傳官方題目原文
#   - 詳見 docs/TTS_AUDIO_WORKFLOW.md「第二階段：OpenAI TTS examiner voice 試產流程」
OPENAI_API_KEY=

# Brave Search API（discovery crawler 真實 search provider 第一版；屬 P3-10-D-2B 範圍）：
#   - 用於 `scripts/discover_resources.mjs --provider brave-search`
#   - 只用於對 query 做 web search、取候選 URL，不抓網頁內容、不下載 asset
#   - 申請：請參考 Brave Search API 官方頁面（免費 / 付費 quota 由使用者自行確認）
#   - 注意：本腳本不會自動讀 .env.local；請以 shell `export` 或 inline 注入：
#       BRAVE_SEARCH_API_KEY=... node scripts/discover_resources.mjs --provider brave-search ...
#     或使用 Node 20+ 的 `--env-file=.env.local` 參數。
BRAVE_SEARCH_API_KEY=
