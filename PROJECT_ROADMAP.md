# Project Roadmap

階段以 P 為單位，由淺入深。✅ 表示已完成、🟡 進行中、⬜ 未開始。

---

## P1 基礎架構（✅ 已完成）

- ✅ Next.js + TypeScript + Tailwind 初始化
- ✅ 資料夾結構：`app/`、`components/`、`data/`、`lib/`、`public/{images,audio}`、`docs/`
- ✅ 首頁 + 兩個入口（複習區 / 測驗區）
- ✅ 範例 `vocabulary.json` 與 `quizzes.json`
- ✅ 已建立 `lib/data.ts` 基礎資料載入 helper（type-safe 匯出 `vocabulary` / `quizzes`，供 P2、P3 串接使用）
- ✅ 文件骨架（README、AI_DEV_WORKFLOW、ROADMAP、PRODUCT_SPEC、DATA_SCHEMA、TASK_ROUTER）
- ✅ `npm run lint` / `typecheck` / `build` 全綠
- ✅ AI workflow 文件規範補齊（語言規則、Codex 驗收前置閱讀）
- ✅ Codex 驗收手冊建立（`docs/CODEX_VALIDATION_RUNBOOK.md`）
- ✅ AGENTS.md / CLAUDE.md 規則指向整理

### 可選 housekeeping（不阻擋 P2）

- ⬜ 加入 `.editorconfig`
- ⬜ 加入簡單的 GitHub repo / 遠端

## P2 單字複習（🟡 進行中）

> 註：`lib/data.ts` 的型別安全載入 helper 已於 P1 完成。P2 重點是**頁面串接與互動 UI**，不再重做 helper 本身。
> UI / UX 一律遵守 `docs/PRODUCT_SPEC.md` 的「國小低年級使用者設計原則」與「單字複習主流程」。
> 主流程於本輪起改為 **A~Z 字母入口 → 字母總覽 → 單字詳情**；原 category tab 模式保留為未來補充模式。

### P2-1 基礎資料串接與單字卡能力（已完成）

- ✅ `/review` 串接 `lib/data.ts` 的 `vocabulary`
- ✅ 單字卡 UI：圖片區 + 英文 + 中文 + 例句 + 發音按鈕
- ✅ 圖片 fallback：缺圖時改為首字母色塊（client 預載 + 初始 placeholder）
- ✅ 發音播放 fallback：缺音檔或播放失敗時顯示「音檔準備中」提示

### P2-2 A~Z 字母入口與字母總覽

- ✅ `/review` 主入口改為 A~Z 字母網格（後續已於 P2-4B 將 A~Z 字母入口搬到 `/review/words`，`/review` 改為複習中心首頁）
- ✅ 字母按鈕顯示該字母目前單字數；無單字字母淡化 / 不可點
- ✅ 新增 `/review/letter/[letter]` 字母總覽頁，列出該字母所有單字
- ✅ 補入 `lib/vocabularyNavigation.ts` helper（letter status、word list、word navigation）

### P2-3 單字詳情頁與跨字母順序導覽

- ✅ 新增 `/review/word/[id]` 單字詳情頁（重用 `VocabularyCard` 與 fallback 邏輯）
- ✅ 詳情頁「上一個 / 下一個」依整體 A~Z 順序前進，自動跨字母
- ✅ 第一個單字 disabled 上一個、最後一個單字 disabled 下一個
- ✅ 詳情頁提供「回 X 總覽」返回連結

### P2-4A 單字補強 + 詳情頁翻牌互動

- ✅ 補強 `data/vocabulary.json` 至 54 筆，覆蓋 17 個字母與全部 11 個分類（每筆沿用既有 schema、placeholder image / audio path）
- ✅ `VocabularyCard` 新增 `revealMode` prop，初始隱藏中文翻譯與例句
- ✅ `/review/word/[id]` 啟用翻牌：「🔍 看答案」/「🙈 再想一次」雙態切換；切換上一個 / 下一個自動回到收起狀態（靠父層 `key={current.id}` remount）

### P2-4B 複習中心首頁與複習類型入口

> `/review` 與 `/quiz` 分工：`/review` 複習中心**只負責分項能力練習**（5 張主入口卡）；考卷、歷屆 / sample 題、AI 仿真題、交卷評分等屬 `/quiz` 測驗區。複習中心可在頁面底部放一個低視覺層級的輔助提示連到 `/quiz`，但不作為主入口卡。

- ✅ `/review` 改為複習中心首頁（**5 張主入口卡**，不再是 A~Z 字母網格）
- ✅ A~Z 單字複習移到 `/review/words`，原 `/review/letter/[letter]` 與 `/review/word/[id]` 動線完整保留
- ✅ 看圖練習入口（卡片顯示「準備中」，未來連到 `/review/picture` 或類似路徑）
- ✅ 聽力練習入口（卡片顯示「準備中」）
- ✅ 句型練習入口（卡片顯示「準備中」）
- ✅ 位置 / 顏色 / 數量入口（卡片顯示「準備中」）
- ✅ 底部輔助提示「想做完整考卷？請到測驗區。」連到 `/quiz`（**視覺層級低於主入口卡**，避免讓人誤把考卷功能當成複習中心主功能；`/quiz` 仍維持骨架，本輪未動）
- ✅ 平板同 Wi-Fi 使用策略補入 `README.md` 與 `docs/PRODUCT_SPEC.md`

### P2-4C 真實素材與複習類型實作（🟡 進行中）

> 註：本子階段把舊版 P2-4B 的「真實素材」收尾任務，與 P2-4B 新增的「準備中」入口未來實作合併在同一階段。
> 各個複習類型（看圖、聽力、句型、位置 / 顏色 / 數量）的具體實作細節，待 ChatGPT 收斂為獨立任務單後再交給 Claude Code 動工。

#### P2-4C-1 看圖練習第一版（已完成）

- ✅ 新增 `/review/picture` 看圖練習頁（server page + `<PicturePractice>` client 元件）
- ✅ 題型「看圖選英文」4 選 1
- ✅ Deterministic 選項生成（避免 hydration mismatch）：正確答案 + 後 3 個 vocabulary 循環取，正確答案位置 = `index % 4`
- ✅ 點選後即時答對 / 答錯回饋，鼓勵語氣，答錯顯示正確答案
- ✅ 「下一題」按鈕切換，全部題庫循環，無 crash
- ✅ 圖片缺檔 fallback（與 `VocabularyCard` 同風格：首字母 + 「圖片準備中」、client 預載成功才切真圖）
- ✅ 進度指示「第 X 題 / 共 N 題」
- ✅ `/review` 看圖練習卡片從 coming-soon 改為 ready，連 `/review/picture`
- ✅ 不做分數保存、不做交卷、不做 localStorage（屬 `/quiz` 測驗區範圍）

#### P2-4C-2A 看英文選圖第二題型（已完成）

- ✅ `/review/picture` 新增題型切換 tab（看圖選字 / 看字選圖），預設「看圖選字」
- ✅ 切換題型時自動 reset 到第 1 題，狀態完全重置
- ✅ 新增 `WordToImageQuestion`：題目顯示英文單字（sky-50 區塊區別於圖片題目區）+ 4 個圖片選項（grid-cols-2，每個用 `<PracticeImage>`，缺圖時 fallback 顯示首字母）
- ✅ 即時答對 / 答錯回饋：答對顯示「🎉 答對了！」，答錯顯示「差一點點！正確答案是 {word} 那張圖」+ 鼓勵語
- ✅ 答對 / 答錯視覺與看圖選字一致（emerald / rose / 淡化）；答錯時錯選項 rose、正確圖片仍 emerald 高亮
- ✅ 「下一題」按鈕循環，全題庫 54 題無 crash
- ✅ 抽出 file-private `PracticeImage` 元件，看圖選字大圖與看字選圖選項小圖共用，size 由 prop 控制（lg / sm）
- ✅ 抽出 file-private `NextButton` 元件，兩題型共用
- ✅ Deterministic 選項生成沿用 P2-4C-1 同一個 `buildOptions` helper

#### P2-4C-2B-1 小批圖片素材接入與 fallback 回歸（已完成 + 持續補件中）

- ✅ 新增 10 個自製 SVG 圖像素材到 `public/images/`（apple / cat / dog / book / red / blue / one / two / mother / father）
- ✅ **2026-05-10 補 banana.svg**（P2-4C-2B-1 / P3-9-C 第三刀後續）：黃色香蕉主體 + 棕色蒂頭 + 棕色尾端 + 淡黃 highlight；同 viewBox `0 0 200 200` + amber-50 背景 + Tailwind 色 + `aria-hidden="true"` 慣例；不含英文單字；不使用官方素材 / 外部素材 / 下載圖片。讓 L3 `q-lc-001` 4 張圖卡（apple / banana / cat / dog）全部顯示真實圖片不再走 fallback。
- ✅ 風格統一：`viewBox="0 0 200 200"` + 淡黃 `#fef3c7` 背景，純圖形（color 用色塊不放單字文字、避免洩漏答案）
- ✅ 更新 `data/vocabulary.json` 中對應 10 筆的 `image` 從 `.png` 改為 `.svg`，其餘 44 筆 placeholder path 維持，讓 fallback 仍可測
- ✅ `<PracticeImage>` / `<VocabularyCard>` 圖片預載與 fallback 邏輯**未動**——`<img>` + `new window.Image()` 對 SVG 原生支援，與 PNG 行為一致
- ✅ 不下載 Cambridge 官方圖片、不下載歷屆考題圖片、不使用網路抓圖、不使用版權外部素材

#### P2-4C-2B-2 後續複習類型與素材（🟡 部分進行中）

- ⬜ 補更多自製 SVG / PNG 圖片素材（往剩下 44 個未補圖單字推進）
- 🟡 真實音檔（建議用 TTS 自製，避免官方版權）——**P3-9-C 第一刀已落地第一個 L3 listening 自製 TTS 音檔**（`public/audio/starters/l3/q-lc-001.m4a`，macOS `say` + `afconvert` 流程）；完整流程與硬邊界文件 `docs/TTS_AUDIO_WORKFLOW.md` 已就位；**P2 vocabulary 音檔仍 ⬜**——需逐筆補 54 字單字音檔
- ⬜ 補齊更多單字（往未覆蓋字母 I / K / L / N / Q / U / V / X / Z 推進）
- ⬜ 聽力練習實作（聽單字 / 聽句子、選圖片）
- ⬜ 句型練習實作（`This is...` / `I can see...` / `There is...`）
- ⬜ 位置 / 顏色 / 數量練習實作（介系詞、顏色 + 名詞、數字 + 名詞）
- ⬜ 視需要再啟用 category 補充模式入口（既有元件已保留）
- ⬜ **單字閱讀練習模式**：圖片 + 英文 + 可顯示中文 + 「下一個」；屬 review 區（不是正式考卷）；視需要可作為 `/review/words` 詳情頁的補充模式
- ⬜ **單字拼字測驗模式**：只顯示圖片、不顯示英文答案、孩子自己拼字輸入、可按「看答案」、「下一題」；屬 review 區獨立練習模式（**對應正式 Cambridge Starters Reading & Writing Part 3 結構**，但不是 `/quiz` 考卷流程）

## P3 考前練習與題庫（🟡 進行中）

> P3 是「測驗區」的完整路線，從題目資料、素材策略、各題型一直到模擬考與錯題複習。
> P3-1 已完成（schema / 型別 / 範例資料 / 文件）；P3-2 ~ P3-6 皆 ⬜，請暫不要動。
> 詳細產品方向見 `docs/PRODUCT_SPEC.md`「測驗與考前練習方向」。
> P3 涵蓋了原 P4「題型擴充」與原 P5「模擬考」的內容；舊章節保留作為歷史紀錄（見下文）。

### P3-1 考題資料 schema 擴充（已完成）

> 範圍：本子階段只做**資料 schema / 型別 / 範例資料 / 文件**，**不做** UI / localStorage 實際讀寫 / AI 出題工具 / 匯入工具。

- ✅ `QuestionSource` 字串字面量（`official_sample` / `past_paper` / `ai_generated` / `custom`）寫入 `lib/types.ts`
- ✅ `QuestionType` discriminator（`multiple-choice` / `picture-choice` / `word-choice` / `listening-choice` / `fill-blank` / `matching`）寫入 `lib/types.ts`
- ✅ `BaseQuestion` 共用欄位：`id` / `type` / `source` / `prompt` / `explanation` / `image` / `audio` / `difficulty` / `topic` / `promptVersion`
- ✅ 6 個題型專屬型別：`ExamMultipleChoiceQuestion` / `PictureChoiceQuestion` / `WordChoiceQuestion` / `ListeningChoiceQuestion` / `FillBlankQuestion` / `MatchingQuestion`，皆 extend `BaseQuestion`；`ExamQuestion` discriminated union 統合
- ✅ Listening 題目特化欄位：`audio` 必填、可選 `transcript`、`ttsScript`、`optionType`
- ✅ `ExamPaper` 完整考卷型別：`examPaperId` / `title` / `description` / `sections` / `sourceMix` / `createdAt` / `updatedAt`
- ✅ `ExamSection` / `SourceMix` 配套型別
- ✅ `ExamSessionState` localStorage 狀態型別：含 `examSessionId` / `examPaperId` / `questionOrder` / `answers` / `currentIndex` / `submitted` / `score` / `wrongQuestionIds` / `createdAt` / `updatedAt` / `schemaVersion`，附 `ExamAnswerMap` 配套
- ✅ schema 完整寫入 `docs/DATA_SCHEMA.md`「P3 考前練習：題庫 / 考卷 / Session schema」段，含 6 題型 + ExamPaper + Session + localStorage key 命名提案 + 「不在 P3-1 範圍」清單
- ✅ 範例資料：新增 `data/p3-example-questions.json`（7 題涵蓋 6 題型）與 `data/exam-papers.example.json`（1 份範例考卷），不被 `lib/data.ts` import，純 schema 演示
- ✅ 既有 `MultipleChoiceQuestion` / `QuizQuestion` / `Quiz` / `data/quizzes.json` 完全不動，舊 schema 與新 schema 並存

### P3-2 本機資料匯入流程（🟡 進行中）

> 範圍拆兩個子階段：P3-2-A 規劃 / 文件（純人工流程，本輪完成）；P3-2-B 自動化工具（未來，本輪不做）。

#### P3-2-A 資料夾規劃與 SOP 文件（已完成）

- ✅ 建立 `source_materials/` 資料夾結構：`samples/` / `past_papers/` / `ai_generated/` / `custom/` 四個子目錄各對應一個 `QuestionSource`，皆有 `.gitkeep` 保留
- ✅ 撰寫 `source_materials/README.md` 匯入 SOP：用途定位、資料夾與 `source` 對應、可放 / 禁止做、git 政策、建議流程、命名規範、與 P3-1 schema 關係、不在 P3-2-A 範圍清單
- ✅ `source_materials/.gitignore` 排除原始二進位（PDF / 圖片 / 音檔 / 影片 / 壓縮檔），保留純文字（`.md` / `.txt` / `.csv` / `.json` / `.gitkeep`）
- ✅ 範例草稿檔 `source_materials/custom/example-question-draft.md`：欄位對照表 + 7 個範例（覆蓋 6 題型，自製內容、不抄真題）+ 命名規範 + 提醒
- ✅ `docs/DATA_SCHEMA.md` 補「本機素材匯入流程（P3-2-A）」一節：三層資料分工、`source` ↔ 整理區子目錄對應、工作流程圖、P3-2-A vs P3-2-B 範圍切分、嚴禁清單
- ✅ 第一版**不做**自動網路爬蟲（已寫入 `source_materials/README.md` 與 DATA_SCHEMA 兩處）

#### P3-2-B 自動化工具（尚未開始）

- ⬜ 整理後文字 / CSV → `data/*.json` 的轉換工具（CLI 或 script）
- ⬜ 圖片素材命名規範 helper / 批次重命名工具
- ⬜ 音檔素材命名規範 helper（含 TTS 自動生成 wrapper，例如 macOS `say -o` 包裝）
- ⬜ JSON schema 驗證 helper（對齊 `lib/types.ts` 的 `ExamQuestion` 確保整理出的題目欄位完整）

### P3-3 AI 仿真題生成規劃（🟡 進行中）

> 範圍拆兩個子階段：P3-3-A 文件 / prompt 範本（本輪完成）；P3-3-B 實際 AI 工具串接（未來，本輪不做）。

#### P3-3-A AI 仿真題 Prompt 標準格式（已完成）

- ✅ 撰寫 `docs/AI_QUESTION_GENERATION.md` 規格文件（10 個 section：定位 / 難度原則 / 來源規則硬邊界 / 題型範圍 / 草稿輸出格式 / 轉換流程 / 品質檢查 6 項 / 與 P3-1 + P3-2-A 對齊 / 不在 P3-3 範圍 / 版本化）
- ✅ 撰寫 `source_materials/ai_generated/prompt-template.md` 可直接複製給 AI 使用的 prompt 範本（v1）：角色 / 年齡 / 6 題型 / 可調參數 / 類 YAML 草稿格式 / 硬邊界 / 8 項自我檢查
- ✅ 撰寫 `source_materials/ai_generated/example-ai-questions.md` AI 草稿範例：6 題覆蓋 6 種題型（`q-ai-mc-001` / `q-ai-pc-001` / `q-ai-wc-001` / `q-ai-lc-001` / `q-ai-fb-001` / `q-ai-mt-001`），id 與既有 P3-1 範例不撞名；附自我檢查紀錄與整理者提醒
- ✅ 草稿欄位定義：`imagePrompt`（描述自製插畫需求）/ `ttsScript`（描述自製音檔腳本）/ `promptVersion`（出題 prompt 版本，便於回溯品質）
- ✅ 強制 `source: "ai_generated"`、嚴禁假裝為 `official_sample` / `past_paper` / `custom`
- ✅ `docs/DATA_SCHEMA.md` 補「AI 仿真題草稿與正式題庫的關係（P3-3）」一節：草稿 → 審核 → 正式題庫流程圖、草稿欄位 vs 正式 schema 對應表、P3-3-A 範圍切分
- ✅ 已新增 prompt v1 第一批人工試跑草稿 `source_materials/ai_generated/2026-05-08-starters-v1-batch01.md`：8 題覆蓋 6 題型（mc × 2 / pc × 1 / wc × 1 / lc × 2 / fb × 1 / mt × 1），id 用 `q-ai-v1-*-001/002` 與既有不撞名，附完整人工品質檢查紀錄表（10 項全 ✓）+ 「需要日後調整」分析（含對 P2-4C-2B-2 圖片 / 音檔素材的依賴）

#### P3-3-B 實際 AI 工具串接（尚未開始）

- ⬜ AI API 客戶端串接（OpenAI / Anthropic / Claude SDK 等）
- ⬜ 自動把 AI 草稿轉成正式 `data/*.json`（屬 P3-2-B 範圍，未來合併）
- ⬜ 自動圖像生成 wrapper（如有需要，先評估自製素材策略）
- ⬜ 自動 TTS 生成 wrapper（macOS `say -o` 或雲端 TTS）
- ⬜ AI 評分功能：**永久不做**（見「目前明確不做」清單）

### P3-4 Listening 題型第一版

- ⬜ 聽音選圖（播音檔 → 三 / 四選一圖）
- ⬜ 聽句子選答案
- ⬜ 支援 audio path 與 TTS path 兩種來源
- ⬜ 缺音檔時 fallback：「音檔準備中」（沿用 P2 機制）

### P3-5 Reading & Writing 題型第一版

- ⬜ 看圖選字
- ⬜ 選字填空（給選項版的 fill blank）
- ⬜ Matching / 連連看
- ⬜ Fill in the blanks（自由填空，比對忽略大小寫與前後空白）
- ⬜ 簡單拼字（看圖拼字）

### P3-6 完整考卷 Session、交卷與錯題複習（🟡 進行中）

> 階段拆兩個子階段：P3-6-A 最小可玩第一版（本輪完成）；P3-6-B 持久化 / 操作 / 結果頁完整版（未來）。
> 既有「完整考卷生成 / 作答進度保存 / 考卷操作 / 交卷與結果頁 / 計時相關」5 個分區為 P3-6-B 完整目標；本輪只做 P3-6-A 最小可跑版。

#### P3-6-A `/quiz` 最小可玩流程第一版（已完成）

- ✅ `/quiz` 從骨架升級為**可實際操作**的測驗頁（server page 載入 P3-1 範例資料 + `<QuizPlay>` client 元件管狀態）
- ✅ 讀取 `data/exam-papers.example.json` 第一份考卷 + `data/p3-example-questions.json` 7 題；依 sections 順序平鋪 questionOrder
- ✅ 進度顯示「第 X 題 / 共 N 題」+ 6 題型最小渲染（multiple-choice / picture-choice / word-choice / listening-choice / fill-blank 選項版 + 自由填空版 / matching 閱讀型）
- ✅ 一題一頁流程：未答題「下一題」disabled、答完可前進、最後一題顯示「看結果」
- ✅ 完成畫面：「答對 N / M 題」+ 依答對率四級鼓勵文案 + 「🔁 重新開始」按鈕 + 回首頁 link
- ✅ matching 題用閱讀型（顯示 pairs + 「我看完了」）作答；計分時視為「答完即正確」
- ✅ fill-blank 自由填空（無 options）用 text input，比對忽略大小寫與前後空白（對齊 schema 規則）
- ✅ listening 題顯示 transcript / ttsScript 文字（最小可玩，**不播放音檔**——音檔自製屬 P2-4C-2B-2 範圍）
- ✅ 圖片缺檔 fallback：file-private `<QuizImage>` 元件（與 `<PracticeImage>` 同模式但獨立，避免動 P2-4C 既有元件）；`new window.Image()` 預載成功才切真圖
- ✅ `lib/data.ts` 最小擴充：新增 `p3ExampleQuestions` / `p3ExamplePapers` export，舊 `vocabulary` / `quizzes` 不動
- ✅ 第一版**不計時**（對齊 PRODUCT_SPEC「測驗與考前練習方向 → 完整考卷 Session → 計時相關」）
- ✅ 純 React local state，**無 localStorage / 後端 / DB**
- ✅ 題目排序貼近正式 Cambridge Starters：Section 1 Listening（`listening-choice`）→ Section 2 Reading & Writing（`picture-choice` → `word-choice` → `multiple-choice` → `fill-blank` → `matching`）；題目卡頂端顯示對應段落徽章（sky 系 / amber 系），不修改 JSON 來源
- ✅ 題目卡加 Part 標示對齊正式 Cambridge Pre A1 Starters parts 架構：`getStarterPartInfo()` helper 把 6 題型映射到對應 Part（lc → Part 3 聽音選圖；pc → Part 1 / Part 2 preview 看圖判斷 / 看圖選答案；wc → Part 3 看圖認字 / 拼字練習；mc → Part 4 preview 短句選字；fb → Part 4 短文 / 句子填空；mt → Part 5 preview 圖文配對 / 故事理解預備）；徽章合併段落英文｜中文「Listening｜聽力練習」/「Reading & Writing｜閱讀與書寫練習」，徽章下方獨立行顯示「Part X：題型中文」；頁首補小字「目前為練習版，題型逐步對齊正式 Cambridge Starters」避免使用者誤解為完整正式考卷

#### P3-6-B 持久化 / 操作 / 結果頁完整版（🟡 進行中）

> 詳細產品方向見 `docs/PRODUCT_SPEC.md` 的「測驗與考前練習方向 → 完整考卷 Session」。
>
> 子分區：P3-6-B-1（localStorage 持久化，已完成）+ P3-6-B-2（繼續作答 / 重新測驗 / 直接交卷，已完成）+ P3-6-B-3（完整結果頁與每題詳解）+ P3-6-B-4（錯題詳解與錯題複習）+ P3-6-B-5（計時器）。

##### P3-6-B-1 localStorage Session 持久化（已完成）

- ✅ 新增 `lib/examSessionStorage.ts` helper：集中處理 localStorage key (`cambridge-starters-practice:quiz-session:v1`)、`loadSession` / `saveSession` / `clearSession` / `createEmptySession` / `isCompatibleSession`、`schemaVersion = 1` 檢查、try/catch 防壞資料 / SSR 環境（`typeof window === "undefined"`）造成頁面崩潰
- ✅ Session 欄位精簡版：`schemaVersion` / `paperId` / `questionOrder` / `currentIndex` / `answers`（key = 題目 id、value = 字串）/ `submitted` / `startedAt` / `updatedAt` / `submittedAt`（可選）
- ✅ 與 `lib/types.ts` 的 `ExamSessionState` 概念對齊但**精簡**：第一刀不存 `examSessionId` / `score` / `wrongQuestionIds` 等欄位（屬 P3-6-B-3+ 範圍）
- ✅ `components/QuizPlay.tsx` 三個獨立 state（currentIndex / answers / submitted）改為單一 `session` state；hydration 後嘗試從 localStorage 恢復；session 變更後自動寫回 localStorage
- ✅ 不相容情境（schemaVersion / paperId / questionOrder 任一不符 / JSON parse 失敗 / SecurityError 等）一律丟棄舊 session 並建立新 session，**頁面不 crash**
- ✅ 嚴守邊界：只存使用者作答狀態；不存官方資料、不存圖片本體、不存音檔本體

##### P3-6-B-2 繼續作答 / 重新測驗 / 直接交卷（已完成）

- ✅ **繼續作答**：重新整理 / 重開分頁時，若 localStorage 中有同 paperId + questionOrder 的 session 則自動恢復 currentIndex / answers / submitted；畫面頂端顯示 emerald 系小提示「🔁 已恢復上次作答進度」（按下方「重新測驗」可清除重來）；首次點選任何答案後提示自動消失
- ✅ **重新測驗**：題目卡下方加灰底 chip 按鈕「🔁 重新測驗」；按下後 `clearSession()` + 建立新 session（currentIndex = 0、answers = {}、submitted = false）；無 confirm dialog（依任務單）
- ✅ **直接交卷**：題目卡下方加 amber 系 chip 按鈕「📝 直接交卷」；按下後 submitted = true + submittedAt 寫入；立即進結果頁
- ✅ **結果頁**：顯示「答對 N / 共 M 題」+「已作答 X / 共 M」+「未作答 M-X 題」雙欄統計（emerald / rose 配色）+ 鼓勵文案（依答對率四級）+「重新測驗」按鈕（清 session、回第一題）；未作答題目算錯，UI 明示「未作答的題目算錯」鼓勵語
- ✅ 完成畫面的「重新測驗」與題目卡下方「重新測驗」共用同一個 `handleRestart`（清 localStorage + 建立新 session + 隱藏 restored 提示）
- ✅ 對應 `app/quiz/page.tsx` 補 `paperId` prop 傳給 `<QuizPlay>`（從 `paper.examPaperId` 讀，目前是 `starters-mock-001`）
- ✅ React 19 `react-hooks/set-state-in-effect` 規則：localStorage 讀取的 setState 透過 `queueMicrotask` 包成非同步 callback，與既有 `QuizImage` 的 `probe.onload` 模式一致

##### P3-6-B-3 完整結果頁與每題詳解（已完成）

> 第一刀：在現有結果頁加「每題詳解列表」（每題卡片：第幾題 / Section / Part / 題目 / 狀態 / 你的答案 / 正確答案 / 說明）。**不**新增獨立錯題頁、**不**新增 `/quiz/wrong` 路由、**不**升 schemaVersion、**不**存 score / wrongQuestionIds 到 localStorage（屬 P3-6-B-4 範圍）。

- ✅ 結果頁顯示每題作答 vs 正確答案——`QuestionDetailCard` 元件（每題一張卡片，emerald 答對 / rose 答錯 / amber 未作答三色配色）
- ✅ 顯示簡單講解（小一友善語氣，鼓勵 > 懲罰）——優先顯示 `BaseQuestion.explanation`；無 explanation 時依狀態給鼓勵性 fallback（「答得很好！」/「再想一下，下次一定可以的～」/「下次可以再試一次～」）
- ✅ 顯示正確答案——`formatCorrectAnswer()` helper：option-based 與 fill-blank 直接顯示 `question.answer`；matching 顯示「本題目前為閱讀型練習，完成即算正確」
- ✅ 顯示使用者答案——`formatUserAnswer()` helper：option-based 與 fill-blank 直接顯示 `answer` 字串；matching `_done` token 顯示「已完成閱讀配對練習」；未作答顯示「尚未作答」
- ✅ 未作答題目清楚標示——amber 系視覺 + 「未作答」chip + 「尚未作答」用字，列入未作答計數，**不算對**
- ✅ 從 `session.answers` 即時計算每題對錯——**不**升 schemaVersion，**不**動 localStorage schema
- ✅ 重新整理結果頁仍保持 submitted 狀態（既有 P3-6-B-1 hydration 機制即可——`submitted: true` + `submittedAt` 由 localStorage 自動恢復）
- ✅ 重複利用既有 `getSectionTag()` + `getStarterPartInfo()` + `isCorrect()` + `isAnswered()` + `MATCHING_DONE_TOKEN` helpers，沒新增 `lib/types.ts` 欄位

##### P3-6-B-4 錯題詳解與錯題複習（🟡 進行中）

> 第一刀（filter）：在 P3-6-B-3 結果頁詳解列表上加 inline filter（全部 / 只看錯題 / 只看未作答 / 需要再練習），純 React local state、不存 localStorage。
> 第二刀（retry mode）：結果頁加「再練習這些題目」按鈕進入 inline retry mode；retry quiz / retry result 全部用 React in-memory state，**不**新增 `/quiz/wrong` 路由 / **不**升 schemaVersion / **不**新增 `wrongQuestionIds` localStorage 欄位 / **不**覆蓋原始測驗分數（屬後續刀數）。

- ✅ 結果頁支援全部 / 錯題 / 未作答 / 需要再練習篩選——`DetailFilter` type 4 個值（all / incorrect / unanswered / review）+ ResultView 內 React local state；review = incorrect ∪ unanswered（matching 完成後算 correct，不出現在 review）
- ✅ 篩選按鈕顯示數量——`FILTER_LABELS` + `FILTER_ORDER` + 4 個 chip buttons（aria-pressed 切換 amber / white 配色），每個 chip 右側 inline 數字 badge 顯示對應 count（all = total / incorrect / unanswered / review = incorrect + unanswered）
- ✅ 空狀態提示——`EMPTY_STATE_MESSAGES` 三段友善文字（incorrect → 「太棒了，目前沒有答錯的題目！」/ unanswered → 「很好，這次每一題都有作答！」/ review → 「全部都很棒，這次沒有需要再練習的題目！」）；emerald 系空狀態卡片
- ✅ 結果頁新增「再練習這些題目」入口——rose 系按鈕，僅 reviewCount > 0 時顯示，按鈕文案含 reviewCount 數字（例如「🔁 再練習這些題目（5）」），位於詳解列表與重新測驗之間；按下傳 incorrect + unanswered 的題目 id 給 `handleStartRetry`
- ✅ 支援 inline retry mode——`QuizPlay` 加 4 個 React state（`retryQuestionIds: string[] | null` / `retryAnswers` / `retryIndex` / `retrySubmitted`）+ 5 個 handler（startRetry / exitRetry / selectRetryAnswer / retryNext / retrySubmitNow），retry quiz 內聯 JSX（amber 系 banner「🔁 再練習模式」+ 進度「再練習 第 X / Y 題」+ 直接交卷 / 返回完整結果按鈕）；retry mode 完成後顯示 `RetryResultView`（amber 系 banner 含 `<strong>不會覆蓋</strong>` 強調 + 統計 + 鼓勵語 + retry 每題詳解 + 回到完整測驗結果按鈕 + 重新測驗按鈕 + 回首頁）；retry result detail 卡片用 **原始試卷 index**（讓家長辨識「第 N 題」對齊整份試卷編號），retry quiz progress 用 **retry-relative index**（「再練習 第 X / Y 題」）
- ✅ retry mode 不新增路由、不升 schemaVersion——純 in-memory state、不存 localStorage、不污染既有 session schema；reload 後因 localStorage 仍是 submitted=true 的原始 session，使用者會回到完整結果頁（retry state 自然丟失）；handleRestart 同步重置 retry state（透過 `resetRetryState` helper）
- ⬜ 錯題複習頁可獨立進入（不透過完整考卷；屬下一刀，需新路由 `/quiz/wrong` 或類似）
- ⬜ 錯題狀態於 localStorage 保存（`wrongQuestionIds` 對齊 `ExamSessionState`，需升 `QUIZ_SESSION_SCHEMA_VERSION` 到 v2 + 寫對應 migration；屬下一刀）
- ⬜ 錯題歷史紀錄（屬下一刀，跨 session 累積錯題）

##### P3-6-B-5 計時器（⬜ 未開始）

- ✅ **第一版不計時**（已於 P3-6-A 落實——`/quiz` UI 無計時器）
- ⬜ 未來若加入模擬考計時器：設為可選功能、預設關閉

### P3-7 官方資源索引與人工整理流程（🟡 進行中）

> 建立 Cambridge Pre A1 Starters **官方公開資源的連結索引與人工筆記**，作為自製題的「題型結構參考」。本階段純文件，**不下載任何官方檔案、不爬蟲、不把官方 sample 題目轉入正式題庫**。
>
> 子分區：P3-7-A（索引文件第一版，已完成）+ P3-7-B（format 對 P3-9 模板逐項校正，🟡 第一輪完成）+ P3-7-C（wordlist 對自家 vocabulary 分類校正）+ P3-7-D（sample / mock toolkit 題型觀察筆記）。

#### P3-7-A 官方資源索引文件第一版（已完成）

- ✅ 新增 `docs/OFFICIAL_RESOURCES.md`（v1，2026-05-08），用繁體中文撰寫；明示「只保存連結與人工筆記、不下載官方 PDF / 圖片 / 音檔、不複製官方題目 / 歷屆題、不做自動爬蟲、不把官方 sample 題目轉入正式題庫、不使用網路圖片當正式素材」。
- ✅ **用途與硬邊界**：對齊 PRODUCT_SPEC「目前明確不做」、source_materials/README「官方資源與歷史題整理原則」、AI_QUESTION_GENERATION「來源規則硬邊界」三處硬邊界，列出 7 條 ✅ 可以做（保存連結 / 人工筆記 / wordlist 整理方向 / sample paper 觀察筆記 / mock test toolkit 流程觀察 / 官方題型結構轉自製題規則 / 自製圖片與音檔）+ 9 條 ❌ 不可做（爬蟲 / 自動下載 PDF / 圖片 / 音檔 / commit 官方檔案 / 複製 sample 題目 / 複製歷屆題 / 網路圖片當正式素材 / data JSON 放官方 URL / 聲稱 AI 題是官方題）。
- ✅ **索引格式**：每筆條目 8 欄位（資源名稱 / 官方用途 / 本專案用途 / 可參考內容 / 不可直接使用內容 / 對應 Roadmap / 對應文件 / 連結）；連結欄位由人工從主入口導航後驗證填入，**v1 不預先填入 sub-page URL** 避免 AI 生成過時資訊。
- ✅ **A 段 官方考試格式與 parts**：4 個條目（A-1 Cambridge English 主站 / A-2 Pre A1 Starters 考試資訊頁 / A-3 Test format 頁 / A-4 Handbook for teachers）。
- ✅ **B 段 官方 preparation / sample resources**：4 個條目（B-1 Preparation 頁 / B-2 Sample papers 入口 / B-3 Wordlist 入口 → 連到 C 段 / B-4 Mock test toolkit 入口 → 連到 D 段）。
- ✅ **C 段 Wordlist / vocabulary 方向**：C-1 wordlist 條目 + C-2 5 條使用原則（可作 vocabulary 範圍參考 / 不複製整份 PDF / 自家 `data/vocabulary.json` 為主 / 分類概念對齊既有 11 個 VocabularyCategory / 未來人工整理需確認來源）+ C-3 與 P3-7-C 對接。
- ✅ **D 段 Sample papers / mock test toolkit 方向**：D-1 sample papers 5+5 對照表（可參考題型順序 / 互動方式 / 題目數量 / 音檔節奏 / 場景設計；不複製題目 / 圖片 / 音檔 / 答題單 / 場景圖原檔）+ D-2 mock test toolkit 4+4 對照表 + D-3 與 P3-7-D 對接。
- ✅ **用官方資源校正 P3-9 模板**：11 條檢查清單（L1 互動描述 / L2 answer type / L3 聽音選圖 / L4 顏色塗色指令 / RW1 圖句判斷 / RW2 大圖 yes/no / RW3 看圖拼字 / RW4 短文填空 / RW5 故事圖 one-word / `getStarterPartInfo()` preview 對應調整 / `STARTERS_PART_TEMPLATES.md` 是否升 v2）+ 校正動作的非目的（不抄官方原文 / 不要求 100% 還原 / 不轉 sample 為 data JSON）。
- ✅ **AI 仿真題素材來源策略**：6 條 ✅ 應使用（官方題型結構 / 本專案 vocabulary / 自製 imagePrompt / 自製 ttsScript / 自製 explanation / 人工審核）+ 6 條 ❌ 不應使用（官方題目全文 / 官方圖片 / 官方音檔 / 歷屆題原文 / 網路圖片 / 外部 URL）+ 違反時的處理 SOP。
- ✅ **與其他文件的關係**：對齊 PRODUCT_SPEC / source_materials/README / STARTERS_PART_TEMPLATES / AI_QUESTION_GENERATION / DATA_SCHEMA / PROJECT_ROADMAP 六處關係表。
- ✅ **版本標記** v1 + v2 後續規劃（等 P3-7-B / P3-7-C / P3-7-D 動工後回填 sub-page URL 與人工筆記檔指向）。

#### P3-7-B 官方 format 對 P3-9 模板逐項校正（🟡 進行中）

> 依 `docs/OFFICIAL_RESOURCES.md` 的「用官方資源校正 P3-9 模板」11 條清單，人工瀏覽官方 format / Handbook / sample 後逐項校正 `docs/STARTERS_PART_TEMPLATES.md`。
>
> 第一輪（已完成）：依官方公開 format 公開說明（Listening 4 parts / 20 Q / 20 min / heard twice，R&W 5 parts / 25 Q / 20 min / spelling must be correct，Speaking 4 parts / 3-5 min）校正 9 個 Part 模板描述；`docs/STARTERS_PART_TEMPLATES.md` 升 v2；補三層分級對應表 + 8 項 v2 後續實作優先順序建議。
> 第二 / 三輪（未開始）：handbook / sample papers / mock test toolkit 人工逐頁觀察筆記後可能升 v3。

- ✅ 已依官方 format 公開說明校正 P3-9 模板 v2（`docs/STARTERS_PART_TEMPLATES.md` 升 v2）
- ✅ 已補 Listening / Reading & Writing format 摘要（4 parts / 20 Q / 20 min vs 5 parts / 25 Q / 20 min）+ Speaking 預留說明
- ✅ 已補 spelling must be correct / each recording heard twice / 1~2 examples per part 三條官方規則
- ✅ 已校正 L1~L4 / RW1~RW5 模板描述（補官方規則 + 練習版邊界 + schema 差距）
- ✅ 已補目前 P3 schema 對應表分級（三層：已較接近 / preview / 尚未支援）
- ✅ 已補 v2 後續實作優先順序建議（8 項排序，L3 + TTS 音檔最先 / L1 hotspot 最後）
- ⬜ 後續 handbook / sample paper 人工逐頁觀察筆記（屬 P3-7-D 範圍延伸）
- ⬜ 後續 P3-9 v3 校正（依人工筆記結果）
- ⬜ 後續 wordlist / vocabulary coverage 校正（屬 P3-7-C 範圍）

#### P3-7-C 官方 wordlist 對自家 vocabulary 分類校正（⬜ 未開始）

> 依 `docs/OFFICIAL_RESOURCES.md` C 段，人工瀏覽官方 wordlist 後輸出**覆蓋差距分析**（不抄整份字表）。

- ⬜ 撰寫 `source_materials/notes/wordlist-coverage.md`（規劃中）：自家 vocabulary 對齊官方 wordlist 的主題覆蓋度與字數差距分析
- ⬜ 確認 `lib/types.ts` 的 `VocabularyCategory` 11 個分類是否需要新增 / 改名
- ⬜ 依差距分析給 P2-4C-2B-2 補字優先序建議
- ⬜ 不整份複製官方表格

#### P3-7-D Sample / mock test toolkit 題型觀察筆記（⬜ 未開始）

> 依 `docs/OFFICIAL_RESOURCES.md` D 段，人工瀏覽官方 sample papers + mock test toolkit 後輸出**結構觀察筆記**（不複製題目 / 圖片 / 音檔）。

- ⬜ 撰寫 `source_materials/notes/sample-paper-observations.md`（規劃中）：每 Part 一段題型互動 / 題數 / 音檔節奏觀察筆記
- ⬜ 撰寫 `source_materials/notes/mock-test-flow.md`（規劃中）：模擬考流程節奏的人工筆記，給 P5 完整仿真考試體驗 / P4 Speaking Agent 考官台詞模板參考
- ⬜ 兩份筆記檔皆只放人工觀察、不放官方檔案；連結指向官方公開頁面而非下載連結
- ⬜ 觀察結果回饋 P3-9-A 模板（若有形式偏差）

### P3-8 AI 仿真題生成流程（⬜ 未開始）

> 把 P3-3 的 prompt 範本與 P3-7 整理出的題型結構對齊，建立**完整的人工 + AI 仿真題生成工作流**。本子階段強調流程設計與文件，**不做** API 串接（API 串接屬 P3-3-B）。

- ⬜ 流程文件：根據 P3-7 整理的官方題型格式 + 本專案 `data/vocabulary.json` 自家單字，生成自製練習題
- ⬜ 強制標 `source: "ai_generated"`；**絕不聲稱是官方題**
- ⬜ **絕不複製 / 改寫 / 變形歷屆題或官方 sample 原文**
- ⬜ AI Editor 角色：把 AI 草稿對齊既有 prompt-template、檢查 6 題型欄位完整、補 `imagePrompt` / `ttsScript` / `promptVersion`
- ⬜ 人工審核：Cambridge Starters 風格 / 小一友善語氣 / 答案正確性 / 圖片與音檔可實作性
- ⬜ 流程：**先進 `source_materials/ai_generated/`，經審核後**轉正式 `data/*.json`
- ⬜ 與 P3-2-B（自動轉換工具）配合：審核後的草稿可由轉換工具批次轉 JSON

### P3-9 正式題型模板化（🟡 進行中）

> 把目前 generic 6 題型對齊到 Cambridge Pre A1 Starters **正式 Parts 結構**，建立可重用的「題型模板」，作為 P3-8 AI 出題流程的**輸入參考**。**Speaking 先不在 P3 實作**，留給 P4 Speaking Examiner Agent 統一處理。
>
> 子分區：P3-9-A（文件第一版，已完成）+ P3-9-B（schema / metadata 實作，🟡 第一刀完成）+ P3-9-C（part-specific quiz UI 實作，未開始）。

#### P3-9-A 正式題型模板文件第一版（已完成）

- ✅ 新增 `docs/STARTERS_PART_TEMPLATES.md` 第一版（v1，2026-05-08），用繁體中文撰寫；明示是「自製練習模板」，不是官方題庫、不複製官方題目；硬邊界與 PRODUCT_SPEC「目前明確不做」對齊。
- ✅ 大架構：明示三段（Listening Part 1~4 / Reading & Writing Part 1~5 / **Speaking 不在 P3，留 P4**）。
- ✅ 整理 **Listening Part 1~4** 模板，每個 Part 含 12 欄位：
  - 官方方向簡述 / 本專案練習版目標 / 題目互動方式 / 需要的資料欄位 / `imagePrompt` 建議 / `ttsScript` 建議 / `answer` 型態 / 目前 P3 schema 是否已支援 / 未來需要補哪些功能 / 是否需要圖片 / 是否需要音檔 / 是否適合 AI 仿真題生成
  - L1：大圖 + 人物 / 物件位置連線；本專案第一版簡化為「看圖 + 聽句子 + 選人物 / 選位置」
  - L2：聽對話寫 name / number；本專案第一版做「聽句子 + 文字輸入」
  - L3：聽音選 A/B/C 圖；目前 `listening-choice` 最接近，schema 已支援
  - L4：聽指令塗顏色；本專案第一版做「聽句子 + 選顏色 / 選物件」，真正塗色 UI 屬 P3-9-C
- ✅ 整理 **Reading & Writing Part 1~5** 模板，每個 Part 含 10 欄位（無 `ttsScript` / 無音檔欄位）：
  - RW1：看圖 + 句子 yes/no；對應 `true-false` / `picture-choice` 未來版本
  - RW2：看大圖回答 yes/no；需要 scene image + 多題共用結構（schema 待補）
  - RW3：看圖拼字；對應「只顯示圖片不顯示英文，孩子自己拼字」未來單字拼字測驗模式（P2-4C-2B-2 / P3-9-C）
  - RW4：短文 / 句子填空；目前 `fill-blank` 單空格已支援，多空格短文待補
  - RW5：看故事圖一字答案；需要 3 張圖 / scene image + one-word answer（schema 待補）
- ✅ **目前 P3 schema 對應表**：6 題型 → 正式 part 對應狀態，明示「練習版近似對應、不是完整正式題型、未來逐步新增更精準 part-specific types 或 metadata」。對齊 `components/QuizPlay.tsx` 的 `getStarterPartInfo()`。
- ✅ **未來題型資料欄位建議**：列 `starterSection` / `starterPart` / `skillFocus` / `ttsScript` / `imagePrompt` / `expectedAnswerType` / `difficulty` 升級 + 其他建議欄位（imageHotspots / sharedSceneImage / images[] / colorPalette / instructions[] / wordBank / multiBlankAnswers）；明示**本輪只寫建議，不修改 `lib/types.ts` / `data/*.json` / 任何 components**。
- ✅ **AI 仿真題 prompt 如何使用模板**：寫出 8 步流程（選 section + part → 套用 part template → 限制 vocabulary → 生成 imagePrompt / ttsScript → 生成 answer / explanation → 人工審核 → 進 source_materials → 轉正式 data JSON），對齊 P3-3 / P3-8 既有規劃；建議未來 prompt-template v2 加 `starterSection` / `starterPart` 必填參數。
- ✅ **與 P4 Speaking Examiner Agent 的關係**：明示 P3 不實作 Speaking、SP1~SP4 模板未來歸屬 `docs/SPEAKING_EXAMINER_AGENT_DESIGN.md`（規劃中），AI 回饋不是 Cambridge 官方成績、處處標示「AI 練習回饋，非官方考試分數」。
- ✅ 與其他文件關係表：列出與 `docs/DATA_SCHEMA.md` / `docs/AI_QUESTION_GENERATION.md` / `source_materials/README.md` / `PROJECT_ROADMAP.md` / `docs/PRODUCT_SPEC.md` 的分工。
- ✅ 文件版本標記：v1（2026-05-08）。

#### P3-9-B part-specific schema / metadata 實作（🟡 進行中）

> 把 P3-9-A 模板中的「未來題型資料欄位建議」實際寫進 `lib/types.ts` 與 `data/*.json`。需配合 DATA_SCHEMA 同步更新。
>
> 第一刀（已完成）：4 個 metadata 字面量類型 + BaseQuestion 4 個 optional 欄位 + 7 題範例補 metadata + Quiz Part 顯示優先讀 metadata + DATA_SCHEMA 補新一節。
> 後續刀數（未開始）：ttsScript / imagePrompt 升正式 schema 評估 + difficulty 字面量升級 + part-specific question types + metadata validator + 多題共用 scene image + 多空格 fill-blank + 多圖序列等。

- ✅ 新增 `StarterSection` / `StarterPart` / `SkillFocus` / `ExpectedAnswerType` 字串字面量類型（`lib/types.ts`）
- ✅ `BaseQuestion` 補 4 個 optional metadata 欄位（`starterSection` / `starterPart` / `skillFocus` / `expectedAnswerType`）；既有資料相容（缺值時 UI fallback）
- ✅ 7 題範例（`data/p3-example-questions.json`）補 starter metadata；不改 prompt / answer / options / explanation
- ✅ `getStarterPartInfo()` / `getSectionTag()` 優先讀 metadata，缺值才 fallback 到依 `question.type` 推導；新增 `STARTER_PART_DISPLAY` map（13 個 part 含 SP1~SP4 預留）
- ✅ `getStarterPartInfo()` 升級為支援 `(starterPart, question.type)` 細分文案（P3-9-C 小修）：RW4 + multiple-choice 顯示「Part 4 preview：短句選字 / 詞彙選擇」（解決 P3-9-B 第一刀的 mc → RW4「短文 / 句子填空」文案落差）；其他 (part, type) 組合維持 default map 文案；fallback 路徑保留
- ✅ `docs/DATA_SCHEMA.md` 補「Starters part metadata（P3-9-B）」一節（用途 / 邊界 / 欄位說明 / 範例 jsonc / 7 題對應表 / 不在 P3-9-B 第一刀範圍）+「UI 文案細分（依 `question.type` 微調）」對照表
- ⬜ `ttsScript` / `imagePrompt` 是否升為正式 schema 欄位（vs 只在草稿層）的決策
- ⬜ `difficulty` 升級為 `"starter-easy"` / `"starter-medium"` 字面量評估
- ⬜ part-specific question types（例如 `true-false` / `spelling-input`；對應 RW1 / RW3）
- ⬜ metadata validator helper（runtime 檢查 metadata 與 type 是否一致；例如 listening-choice 應對應 starterSection: "listening"）
- ⬜ 多題共用 scene image 結構（`sceneGroup` / `sharedSceneImage`，給 RW2 用）
- ⬜ 多空格 fill-blank 結構（`multiBlankAnswers: string[]`，給 RW4 多空格用）
- ⬜ `images: string[]` 多圖序列（給 RW5 用）
- ⬜ 拼字輸入結構（`spelling-input` 子題型 / `inputMode` metadata，給 RW3 用）
- ⬜ yes-no 答題型態（`true-false` 子題型 / `answerStyle: "yes-no"` metadata，給 RW1 / RW2 用）
- ⬜ Listening hotspot / colorPalette / instructions（給 L1 / L4 進階版用）

#### P3-9-C part-specific quiz UI 實作（🟡 進行中）

> 把 P3-9-A 模板中各 Part 的 UI 細節寫進 `components/QuizPlay.tsx` 或拆分新 components；依 P3-9-B 的 schema 渲染。
>
> 第一刀 L3 audio 準備版（已完成）：listening-choice 補 `audioSrc?` optional 欄位 + `<audio controls>` UI + 載入失敗 fallback 文字 + 聽兩次小提示。

- ✅ L3 listening-choice 支援 optional `audioSrc`（`lib/types.ts` ListeningChoiceQuestion 補 optional 欄位 + 音檔三欄位關係註解）
- ✅ L3 UI 支援 `<audio controls src={audioSrc}>` audio player（`components/QuizPlay.tsx` ListeningChoiceView 重構）
- ✅ 無音檔 / 載入失敗時 fallback 顯示 transcript / ttsScript 文字練習（audioStatus state + onCanPlay / onError 切換 + 友善「音檔準備中」訊息）
- ✅ 「聽兩次」UI 提示（💡 正式考試中錄音會播放兩次；本練習版可自行重播音檔練習）
- ✅ `data/p3-example-questions.json` `q-lc-001` 補 `audioSrc: /audio/starters/l3/q-lc-001.mp3`（實體 mp3 未產生，UI 自動降級為文字練習）
- ✅ `docs/DATA_SCHEMA.md` listening-choice 補 audioSrc 欄位說明 + transcript / ttsScript / audioSrc 三者關係表 + 硬邊界
- ✅ `docs/STARTERS_PART_TEMPLATES.md` L3 模板補 audioSrc 支援說明（升 v2.1）
- ✅ 建立自製 TTS 音檔流程文件 `docs/TTS_AUDIO_WORKFLOW.md`（macOS `say` + `afconvert` 完整步驟；硬邊界：不串雲端 API / 不下載官方音檔 / 不用網路 mp3；命名規則 / 路徑 / 人工檢查 6 項 / git 政策 / 雲端 TTS 評估規劃）
- ✅ 建立 `public/audio/starters/l3/` 路徑
- ✅ q-lc-001 第一個自製音檔產生：`public/audio/starters/l3/q-lc-001.m4a`（macOS `say` 產生 AIFF + `afconvert -f m4af -d aac` 轉 m4a；AAC 格式約 12 KB / 1.86 秒；瀏覽器原生支援）；`data/p3-example-questions.json` audioSrc 從 `.mp3` 改為 `.m4a` 對齊實體檔案
- ⬜ 多題 L3 音檔（目前只有 1 題範例）
- ⬜ 音檔品質檢查流程（自動化，目前用 `docs/TTS_AUDIO_WORKFLOW.md`「人工檢查」6 項手動確認）
- ⬜ 未來雲端 TTS 評估（OpenAI / Google / Azure，目前不串 API）
- ⬜ 音檔快取 / 管理策略（避免每次 reload 重新下載）
- ✅ Listening 題考試中隱藏 transcript / ttsScript（P3-9-C 第二刀，2026-05-10）：audioSrc 可播時 UI 不顯示文字稿、改顯示 🎧「請先聽音檔，再選答案」+「文字稿會在交卷後訂正時顯示」雙段提示
- ✅ 交卷後結果頁 / 每題詳解 / retry result detail 仍顯示 transcript（透過既有 `getQuestionPromptDisplay()` helper，listening 走「優先 transcript → 退 ttsScript → 退備援文字」），不影響訂正
- ✅ 音檔缺失（audioSrc 缺值 / onError 觸發）時 fallback 顯示「音檔準備中，先用文字練習」+ transcript / ttsScript 文字（避免完全無法練）
- ⬜ 練習模式顯示文字稿開關（未來功能：家長可選擇「練習時也露文字稿」例如初學者複習用）
- ✅ OpenAI TTS examiner voice 試產流程文件 / 腳本（2026-05-10）：`docs/PRODUCT_SPEC.md`「目前明確不做 → AI / 自動化」段補「OpenAI TTS examiner voice 試產（一題版例外）」開放條款 + 6 條邊界；`docs/TTS_AUDIO_WORKFLOW.md` 升 v2 新增「第二階段」段（4 步流程 + API key 規範 + examiner-style instructions + 人工實聽 5 項 + 失敗處理表 + 切換 audioSrc 時機）；`scripts/generate_openai_tts_sample.mjs` 腳本（讀 OPENAI_API_KEY 環境變數；無 key 安全退出 exit 0；只產一題 `q-lc-001-openai.mp3`；`gpt-4o-mini-tts` model + `alloy` voice + Cambridge-style examiner instructions；拒絕覆蓋既有 `q-lc-001.m4a` 與既存 OpenAI 版本；用 Node 內建 fetch 不裝套件）；`.env.example`（`OPENAI_API_KEY=` 空值範本，可 commit）+ `.gitignore` 加 `!.env.example` 例外。**本輪未實際呼叫 API**——Claude 環境無 OPENAI_API_KEY，腳本「無 key 安全退出」路徑已自測通過 (exit 0)。
- ✅ 使用者實聽比較 macOS `say` vs OpenAI TTS（2026-05-10）——**v1 實聽**：「比 macOS say 正式但語速太慢、有點刻意」→ instructions 升 v2；**v2 實聽**：「v2 版自然很多，先採用」→ 正式切換為 q-lc-001 的 examiner voice 音檔
- ✅ OpenAI TTS instructions v2 調整 + 腳本支援版本化輸出（2026-05-10）：`scripts/generate_openai_tts_sample.mjs` instructions 從 v1 「Speak slowly」系列改為 v2「natural exam pace, not overly slow」+「Do not over-emphasize each word」+「standard English」（移除 British 綁死）；輸出檔名加 `-v2` suffix（預設）/ 可透過 `OPENAI_TTS_OUTPUT_SUFFIX=v3` 等環境變數產生更多版本；不覆蓋既有 v1 `q-lc-001-openai.mp3` 與既存 v2 + macOS say `q-lc-001.m4a`；`docs/TTS_AUDIO_WORKFLOW.md` 補「OpenAI examiner voice 實聽調整紀錄」子段含 v1 / v2 對照表 + v3+ 觸發條件
- ✅ q-lc-001 audioSrc 切換到 OpenAI TTS v2（2026-05-10）：`data/p3-example-questions.json` `q-lc-001.audioSrc` 從 `/audio/starters/l3/q-lc-001.m4a` 改為 `/audio/starters/l3/q-lc-001-openai-v2.mp3`；保留三版檔案並存（v2 = 正式 / v1 = 比較紀錄 / macOS say = fallback / 歷史比較）；不刪除舊音檔；不改 transcript / ttsScript / answer / options / metadata；`docs/TTS_AUDIO_WORKFLOW.md` 升 v2.2 補「目前 q-lc-001 採用版本」對照表 + 三版並存策略說明
- ⬜ 批次產生多題 L3 音檔（OpenAI 版本通過後，可考慮把 examiner voice 流程套用到後續多題；目前主線仍是 macOS `say` 批次）
- ⬜ 未來 P4 Speaking Examiner Agent 共用 examiner voice 策略（與 Listening 試產的 examiner voice 文化對齊；屬 P4 動工前置討論）
- ⬜ L1 場景圖 + 4 選項（含可選 hotspot 互動）
- ⬜ L2 文字輸入 name / number（鍵盤切換）
- ✅ L3 A / B / C / D 圖選項視覺第一版（P3-9-C 第三刀後續，2026-05-10）：`q-lc-001` 切到 `optionType: "image"` + 4 張既有 SVG ImageOption（apple/banana/cat/dog）；`components/QuizPlay.tsx` 新增 `ListeningImageOptionButton`（左上角 A/B/C/D 圓形標籤、selected 時 amber-500、unselected 時 sky-500；不顯示 `option.value` 英文單字避免聽力答案外洩；`fallbackInitial` 改用標籤字母而非 value 首字母，缺檔時也不會露出答案首字）；`ListeningChoiceView` image 分支改用 `(opt, idx)` 配 `String.fromCharCode(65 + idx)` 派發 A/B/C/D；`docs/DATA_SCHEMA.md` listening-choice 段補 P3-9-C 第三刀 A/B/C/D 視覺 + fallback 改用標籤字母說明、jsonc 範例升級為 image 版；`docs/STARTERS_PART_TEMPLATES.md` L3 模板段標題加「+ 第三刀 A/B/C/D 圖選項視覺」+ 內容更新對應狀態 + schema 對應表第一層 listening-choice 條目同步、版本段加 v2.3；`README.md` `/quiz` 條目補 L3 圖卡升級說明
- ✅ L3 q-lc-001 四張圖卡完整顯示（P2-4C-2B-1 / P3-9-C 第三刀後續，2026-05-10）：補齊 `public/images/banana.svg` 自製素材（黃色香蕉主體 + 棕色蒂頭 / 棕色尾端 + 淡黃 highlight；同既有 SVG 風格慣例：`viewBox="0 0 200 200"` + amber-50 背景 + Tailwind 色 + `aria-hidden="true"`，無英文文字、無外部素材、無官方圖片），讓 L3 4 張 ImageOption（apple/banana/cat/dog）全部顯示真實圖片；不再有「B 圖片準備中」fallback；`README.md` 圖片素材段從 10 個 SVG 升 11 個 SVG + 補件描述、`/quiz` L3 圖卡段移除「banana 仍缺檔走 fallback」改為「4 張全部顯示真實圖片」
- ✅ L3 q-lc-001 已調整為 3 選項 A/B/C（P3-9-C 第三刀後續，2026-05-10）：`data/p3-example-questions.json` `q-lc-001.options` 從 4 張 ImageOption（apple/banana/cat/dog）調整為 **3 張**（apple/banana/cat，移除 dog），對齊正式 Cambridge Starters L3 的 3 張選項版面；`components/QuizPlay.tsx` `ListeningChoiceView` image 分支 grid class 從 `grid-cols-2 gap-3 sm:gap-4` 升為 `grid-cols-2 gap-3 sm:grid-cols-3 sm:gap-4`——手機 2 欄（第 3 張自然換行至第二列獨佔一格）/ 桌機 3 欄（A B C 同一列）；`answer` / `audioSrc` / `transcript` / `ttsScript` / `explanation` / metadata 全部不動；OpenAI v2 音檔保留；`ListeningImageOptionButton` 元件本身不變（`String.fromCharCode(65 + idx)` 自動只派發 A/B/C 不再有 D）；`docs/DATA_SCHEMA.md` jsonc 範例 + `optionType` 說明同步、`docs/STARTERS_PART_TEMPLATES.md` L3 模板段升 v2.4 + 互動方式 / 未來功能清單同步、`README.md` `/quiz` L3 段同步「3 張自製 SVG」+「移除 dog 選項」+「sm:grid-cols-3」描述
- ⬜ 未來多題 L3 圖片題庫（目前只有 q-lc-001 一題；未來補 where is...? / how many...? / which one...? 等多元主題）
- ⬜ 未來更完整 L3 Part 3 題型模板（含 example handling / heard-twice UI 對齊官方規則 / 音檔快取管理 / part-level 題目分組顯示）
- ⬜ L4 簡化版選顏色 / 選物件 UI；真正塗色互動屬後續延伸
- ✅ RW1 yes / no 判斷題 UI 第一版（P3-9-C 第三刀，2026-05-10）：`lib/types.ts` 新增 `TrueFalseQuestion` discriminated union 成員（image / prompt / answer "yes" | "no" 必填）；`components/QuizPlay.tsx` 新增 `<TrueFalseView>` + `<YesNoButton>`（Yes emerald + ✓ / No rose + ✗，min-h-24/28 大按鈕，role="radio" + aria-checked / aria-label 完整）；`getStarterPartInfo` 加 (RW1, true-false) 細分覆寫顯示「Part 1：看圖判斷 yes / no」；`formatUserAnswer` / `formatCorrectAnswer` 對 true-false 顯示「Yes ✓」/「No ✗」；`app/quiz/page.tsx` `RW_TYPE_ORDER` 加 `"true-false": 2`；新增 `q-tf-001` 範例題（`/images/cat.svg` + `It is a cat.` + answer `yes` + RW1 metadata 完整）；`data/exam-papers.example.json` reading-writing section.questionIds 加 q-tf-001 + sourceMix ai_generated 從 3 升 4；`docs/DATA_SCHEMA.md` schema 對應表加 true-false 一列 + 詳細 schema 說明（與 picture-choice 並存差異）；`docs/STARTERS_PART_TEMPLATES.md` RW1 模板升 v2.2「已實作 true-false」+ schema 對應表第一層加 true-false → RW1 🟢 一筆
- ✅ RW1 yes/no 題已補 No answer 樣本（P3-9-C 第三刀後續，2026-05-10）：新增 `q-tf-002`（cat.svg + "It is a dog." + answer "no" + RW1 metadata）讓孩子不只練 Yes；目前 RW1 yes/no 分布為 1 yes (q-tf-001) + 1 no (q-tf-002) 平衡 50 / 50；`data/exam-papers.example.json` questionIds 加 q-tf-002（緊接 q-tf-001 之後）+ `sourceMix.ai_generated` 從 4 升 5；題目重用既有 cat.svg、未新增圖片
- ⬜ 未來補更多 RW1 題目與更精準官方格式（多題 RW1 yes/no 多元主題 + RW2 共用 scene image 多題判斷 + 真正 ✓ / ✗ 手寫互動）
- ⬜ 未來更精準對齊官方 part format（真正 ✓ / ✗ 手寫互動而非按鈕；屬未來進階）
- ⬜ RW2 場景圖固定 + 下方多題滾動
- ✅ RW3 看圖拼字輸入第一版（P3-9-C 第三刀後續，2026-05-10）：`lib/types.ts` `QuestionType` union 加 `"spelling"` + 新增 `SpellingQuestion` discriminated union 成員（image / prompt / answer 必填，含完整 docstring 說明與 word-choice / fill-blank 差異）；`components/QuizPlay.tsx` 新增 `<SpellingView>` 元件（圖大圖 `aspect-square` + `<PromptText>` 提示語 + 大型 `<input type="text">` text-3xl 文字、停 autoCapitalize / autoCorrect / autoComplete / spellCheck 避免行動裝置干擾）+ `isCorrect` 加 spelling case（normalize trim + toLowerCase 比對，**不做 fuzzy matching** 對齊「spelling must be correct」精神：`apple` / `Apple` / `APPLE` / `"  apple  "` 答對；`aple` 算錯）+ `getStarterPartInfo` 加 (RW3, spelling) 細分覆寫顯示「Part 3：看圖拼字」+ fallback switch 加 case "spelling" + (RW3, word-choice) 細分顯示「Part 3：看圖認字（看字選圖 preview）」；`QuestionView` switch 加 case "spelling" 派發 `<SpellingView>`；`formatUserAnswer` / `formatCorrectAnswer` / `getQuestionPromptDisplay` 對 spelling 走 default 路徑（直接顯示 raw 字串）；`app/quiz/page.tsx` `RW_TYPE_ORDER` 加 `spelling: 4`（接在 word-choice = 3 之後，從認字到拼字）+ multiple-choice / fill-blank / matching 整體 +1（5 / 6 / 7）；新增 `q-sp-001` 範例題（source ai_generated / image apple.svg 重用 / prompt "Look at the picture. Write the word." / answer "apple" / explanation 「圖片是蘋果，所以正確單字是 apple。」 / RW3 metadata 完整 / promptVersion "starters-rw3-spelling-v1"）；`data/exam-papers.example.json` reading-writing section.questionIds 加 q-sp-001（緊接 q-wc-001 之後）+ sourceMix.ai_generated 從 5 升 6 + description 從「7 題」改「10 題」+「看圖、選字、填空、連連看、yes / no 判斷、看圖拼字」；`docs/DATA_SCHEMA.md` 6 題型表加 spelling 一列 + 詳細 schema 說明段（範例 jsonc + 6 條要點 + 與 word-choice / fill-blank 並存差異說明）+ 7 題範例 metadata 表升級為 10 題（補 q-tf-001 / q-tf-002 / q-sp-001）；`docs/STARTERS_PART_TEMPLATES.md` 升 v2.5：RW3 模板段升級為「P3-9-C 第三刀後續已實作 spelling 題型」狀態 + schema 對應表第一層加 spelling → RW3 🟢 一筆 + 第三層 RW3 從 ⬜ 改 ✅ 第一版；`README.md` `/quiz` 條目順序加 `true-false` / `spelling` + q-sp-001 描述 + 從「6 題型」升「8 題型」
- ✅ RW3 多題拼字題庫第一版（P3-9-C 第三刀後續，2026-05-10）：在 q-sp-001 基礎上新增 3 題（q-sp-002 cat / q-sp-003 dog / q-sp-004 book），讓 RW3 不再只有 apple 一題；目前 4 題覆蓋 food（apple）/ animals（cat / dog）/ school（book）三大主題；皆重用既有 SVG（apple.svg / cat.svg / dog.svg / book.svg）+ 同 prompt「Look at the picture. Write the word.」+ 同 RW3 metadata + 同 promptVersion `starters-rw3-spelling-v1`；`data/p3-example-questions.json` 加 3 題（同 q-sp-001 結構，topic 各別為 animals / animals / school 對齊 vocabulary category）；`data/exam-papers.example.json` reading-writing section.questionIds 加 q-sp-002 / q-sp-003 / q-sp-004（緊接 q-sp-001 之後維持 RW3 連續分區）+ sourceMix.ai_generated 從 6 升 9 + paper-level description 從「10 題」改「13 題混合題型，含聽力、看字選圖、yes/no、看圖拼字（4 題 RW3 多元主題）」；`docs/DATA_SCHEMA.md` 範例 metadata 表升級為 13 題；`docs/STARTERS_PART_TEMPLATES.md` schema 對應表 spelling 條目同步「已支援 4 題多元主題」+ RW3 模板未來功能清單把「多題 RW3 spelling 題庫」標 ✅ + 版本段加 v2.6；`README.md` `/quiz` 條目加 q-sp-002 / q-sp-003 / q-sp-004 描述
- ✅ RW3 缺字提示版第一版（P3-9-C 第三刀後續，2026-05-12）：`lib/types.ts` `SpellingQuestion` 加 optional `spellingHint?: string` 欄位（**只用於 UI 顯示提示、絕不參與 `answer` 比對**——`isCorrect` 不讀此欄位、`normalize` 完全不變、不做 fuzzy matching 邊界維持）+ 完整 docstring 標用途 / 格式建議（`_` 表缺字、字母間空格分隔提升小一可讀性）；`components/QuizPlay.tsx` `<SpellingView>` 在圖 + 提示語之後 / 輸入框之前條件渲染淡藍提示區塊（`bg-sky-50` 與圖片區 `bg-amber-50` 區別、標籤「缺字提示」+ `font-mono text-3xl/4xl font-black tracking-[0.4em]` 大字間距、僅 question.spellingHint 存在時顯示）；`isCorrect` / `normalize` / `formatUserAnswer` / `formatCorrectAnswer` / `getStarterPartInfo` / `RW_TYPE_ORDER` / 結果頁 / Listening / RW1 yes/no UI 全部不動；`data/p3-example-questions.json` 4 題 q-sp-001~004 皆補 `spellingHint`（apple → `"a _ _ l e"` / cat → `"c _ t"` / dog → `"d _ g"` / book → `"b _ _ k"`），其他欄位 image / prompt / answer / explanation / metadata 全保留；未新增題目 / 圖片 / 音檔；`docs/DATA_SCHEMA.md` spelling 段 jsonc 範例補 `spellingHint` + 要點補完整說明（optional / UI only / 不參與 isCorrect）；`docs/STARTERS_PART_TEMPLATES.md` 升 v2.7：RW3 模板未來功能清單把「拼字提示渲染」標 ✅ + schema 對應表 spelling 條目同步「缺字提示版已第一版落地」+ 加 1 條未來 ⬜「字母重組版」；`README.md` `/quiz` q-sp 段補「淡藍缺字提示區塊 + 4 題 hint 字串」描述
- ✅ RW3 字母重組版第一版（P3-9-C 第三刀後續，2026-05-13）：`lib/types.ts` `SpellingQuestion` 加 optional `letterScramble?: string` 欄位（**只用於 UI 顯示提示、絕不參與 `answer` 比對**——`isCorrect` 不讀此欄位、`normalize` 完全不變、不做 fuzzy matching 邊界維持、第一版不做拖曳 / 點選組字互動）+ 完整 docstring 標用途 / 格式建議（空格分隔每個字母提升小一可讀性）/ 可與 `spellingHint` 並存於同一題；`components/QuizPlay.tsx` `<SpellingView>` 在 spellingHint 區塊之後 / 輸入框之前條件渲染淡紫提示區塊（`bg-violet-50` 與 spellingHint 的 `bg-sky-50` 視覺區別、標籤「字母重組」+ `font-mono text-3xl/4xl font-black tracking-[0.4em]` 大字間距、僅 question.letterScramble 存在時顯示）；既有 `spellingHint` 區塊保留並先於 letterScramble 顯示——一題可同時顯示「缺字提示」+「字母重組」兩種視覺輔助；`isCorrect` / `normalize` / `formatUserAnswer` / `formatCorrectAnswer` / `getStarterPartInfo` / `RW_TYPE_ORDER` / 結果頁 / Listening / RW1 yes/no UI 全部不動；`data/p3-example-questions.json` 4 題 q-sp-001~004 皆補 `letterScramble`（apple → `"p p a l e"` / cat → `"t a c"` / dog → `"g d o"` / book → `"o b k o"`），其他欄位（含既有 spellingHint）全保留；未新增題目 / 圖片 / 音檔；`docs/DATA_SCHEMA.md` spelling 段 jsonc 範例補 `letterScramble` + 要點補完整說明 + UI 渲染段補淡紫區塊描述；`docs/STARTERS_PART_TEMPLATES.md` 升 v2.8：RW3 模板未來功能清單把「字母重組版」標 ✅ + schema 對應表 spelling 條目同步「字母重組版已第一版落地」；`README.md` `/quiz` q-sp 段補「淡紫字母重組區塊 + 4 題 scramble 字串」描述
- ⬜ 未來 RW3 字母重組互動進階（拖曳字母塊 / 點選字母組字 / 觸控友善）
- ⬜ 未來 RW3 review 區獨立練習模式（不交卷的拼字練習；屬 P2-4C-2B-2 範圍）
- ⬜ 未來 RW3 看答案 / 再試一次按鈕（與 retry mode 整合）
- ⬜ 未來更精準對齊正式 Starters RW3 格式（嚴格 spelling must be correct 開關 / 寬鬆模式切換）
- ⬜ P3-9-C / P5 前：小朋友實機使用觀察與提示策略調整（依 [`docs/USER_TEST_NOTES.md`](./docs/USER_TEST_NOTES.md) v1 整理 2026-05-13 收斂之觀察清單與後續決策方向，待小朋友實際試玩後收集回饋；可能影響 RW3 提示策略 / 結果頁 No ✗ 視覺 / 互動進階 / review 區獨立練習 / 多題庫擴張等後續刀數的優先順序）
- ⬜ RW4 多空格短文 + 字詞 bank 拖拉
- ⬜ RW5 多圖序列 + one-word 輸入
- ⬜ `getStarterPartInfo()` 升級為從 `question.starterPart` 讀（取代依 `question.type` 推導）

## P3-10 正式練習資料補齊與 web resource collector（🟡 部分進行中）

> 目標：把 `/quiz` 從「題型功能驗證版」逐步補成「真正可給小朋友完整練習的資料包」。
> 三層架構（resource index / imported source dataset / formal practice data）+ 7 種 `sourceType` + 5 種 `reviewStatus`。
> 完整規劃見 [`docs/PRACTICE_DATA_PLAN.md`](./docs/PRACTICE_DATA_PLAN.md)（umbrella）+ 三份子計畫文件。
> **硬邊界**：crawler / collector 不爬蟲式批次抓、不繞 robots.txt、不下載官方音檔 / 圖片到 `public/`；任何匯入題目必須走 `reviewStatus: approved_for_practice` 才能進 `/quiz`。

### 子分區與本輪完成

- ✅ **P3-10-A**：正式資料匯入流程與來源欄位規劃（2026-05-13）—— 新增 [`docs/PRACTICE_DATA_IMPORT_PLAN.md`](./docs/PRACTICE_DATA_IMPORT_PLAN.md)（7 種 `sourceType` 字面量：official / third_party / user_provided / user_verified / ai_generated / custom / handmade；三層架構 7 步流程；正式資料 16 欄位要求；最小可玩資料包目標；後續擴充 9 項）+ [`docs/PRACTICE_DATA_PLAN.md`](./docs/PRACTICE_DATA_PLAN.md) umbrella 引用三份子計畫；example JSON 已 commit `data/imported/resource-index.example.json` / `source-document.example.json` / `normalized-questions.example.json`（共 3 + 2 + 3 筆範例）；`.gitignore` 加 `*.generated.json` / `research_cache/` / `tmp_crawl/` / `.local_research/` 排除 generated artifacts；README 文件索引補 4 條 + 加「目前 /quiz 仍是題型功能驗證版」說明指向 P3-10 規劃。
- ✅ **P3-10-B**：Web resource collector 規劃與最小 CLI 原型（2026-05-13）—— 新增 [`docs/WEB_RESOURCE_COLLECTOR_PLAN.md`](./docs/WEB_RESOURCE_COLLECTOR_PLAN.md)（三種模式：index-only / full-text / asset-aware；硬邊界 6 條；CLI flag 7 個；candidate 偵測 7 種 candidateType）+ `scripts/web_resource_collect.mjs` v0.1 最小 CLI 原型（**無新依賴**，使用 Node 18+ 內建 fetch + regex；支援 index-only + full-text；asset-aware 屬未來範圍）；輸出 `data/imported/resource-index.generated.json` / `source-document.generated.json`（已 gitignore）。
- ✅ **P3-10-C**：Question import normalization 規劃（2026-05-13）—— 新增 [`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md`](./docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md)（對齊 8 種題型 / reviewStatus 5 狀態機：imported_raw → ai_normalized → human_review_required → approved_for_practice / rejected / id 規則 / 必填 vs 選填 / 與既有文件分工）；normalizer 實作屬 P3-10-E、本輪未實作。

### 後續待辦

- 🟡 **P3-10-D**：collector 實測與第一批來源匯入——**部分完成**（2026-05-13）：collector 對使用者提供的 2 個真實 URL（https://www.yle.tw/download.asp / https://www.certificate.tw/category/learning）完成 index-only + full-text 測試，皆 HTTP 200 / 寫出符合 schema 的 generated JSON；額外驗證 404 路徑（warnings 含 non_2xx_status）+ 非 HTML PNG（warnings 含 non_html_content_type + 解析欄位全空）；`scripts/web_resource_collect.mjs` 補 `warnings: []` + `mode` + `retrievedAt` 三欄位（向後相容、example JSON 不動）；non-HTML content-type 時跳過 HTML regex 解析（避免對 binary 跑 regex 出垃圾字串）；`docs/WEB_RESOURCE_COLLECTOR_PLAN.md` 升 v1.1 + 新增 G（non-2xx / non-HTML / fetch failure / timeout 處理策略表）/ H（duplicate URL / 重抓策略、normalizedUrl 去重 key、append history 候選）/ I（output JSON 三新欄位）三段；`docs/PRACTICE_DATA_PLAN.md` P3-10-D 條目同步部分完成狀態。**未做**：多 URL 批次模式 / asset-aware 下載 / PDF parser / 第一批 source-document 正式 import 進題庫（待 P3-10-D 後續刀數 / E / F / K）
- 🟡 **P3-10-D-2**：Discovery crawler 自動找資料來源——**部分完成**（2026-05-13）：把 collector 從「使用者提供 URL → collector 抓」升級為「discovery → collector queue」雛形。新增 [`docs/DISCOVERY_CRAWLER_PLAN.md`](./docs/DISCOVERY_CRAWLER_PLAN.md)（A 目標 / B 資料流程 10 步 / C 4 類 query 共 17 條 / D search provider 5 種，先支援 `manual-json` + `mock`、其他 future-* 暫拒 / E URL 分類 4 軸：4 種 sourceType / 12 種 resourceType / 4 種 level / 9 種 detectedExamParts / F 排除降權規則 8 條 / G 與 collector 銜接表（每 resourceType 對應 `collectorMode`） / H scoring 規則加分 15 條扣分 5 條 / I CLI flag 5 個 + exit code / J 19 欄位 output schema / K 與既有文件關係 / L 後續擴充 8 條） + `scripts/discover_resources.mjs` v0.1 最小 CLI 原型（**無新依賴**，純 Node 內建 + rule-based 分類；支援 manual-json + mock；其他 provider 直接 exit 2；不發 HTTP 請求到候選 URL、不下載 asset、不呼叫 OpenAI、不寫正式題庫） + 3 份 example JSON（`data/imported/discovery-queries.example.json` 17 筆 / `search-results.example.json` 10 queries / 13 results / `discovered-resources.example.json` 6 筆 cover official / third_party / shopping 降權 / Movers 降權 / 中文來源） + `.gitignore` 加 `data/imported/discovered-resources.generated.json` 排除；CLI 端到端執行對 13 search results 產出 13 筆 discovered entries（11 筆 `shouldCollect=true`、2 筆封鎖：1 筆 shopping_or_product_page + 1 筆 duplicate_url）。**未做**：外部 search API（Bing / Google CSE / SerpAPI）/ 自動 pipe 給 collector（待 P3-10-D-3）/ AI 分類 / discovery dashboard / 歷史紀錄
- 🟡 **P3-10-D-2B**：Discovery crawler 接真實 Search Provider 第一版——**部分完成（happy path verified）**（2026-05-13）：CLI 升 v0.2、provider adapter 化（拆出 `runManualJsonProvider` / `runMockProvider` / `runBraveSearchProvider` / `runSearchProvider` dispatcher / `classifyDiscoveredResources` 統一下游 pipeline）；新增 **Brave Search API** 真實 provider（headers `X-Subscription-Token`、`safesearch=moderate`、AbortController timeout 15s、回 JSON 後對應 `web.results[]` → `{ title, url, snippet=description, sourceProvider, rank }`）；CLI 新增 4 個 flag：`--query-file`（與 `--queries` 同義） / `--limit-per-query`（預設 5、Brave 上限 20） / `--query-limit`（預設 20、防呆） / `--search-out`（真實 provider 寫 `search-results.generated.json` 路徑、不指定則不寫）；rate-limit / safety：每 query 之間 `sleep(500)`、單一 query fetch / parse 失敗 → 記錄 error 但不中斷整批；真實 provider 缺 `BRAVE_SEARCH_API_KEY` 時 exit 2 + 印 3 步驟申請流程（cp .env.example .env.local / 填 key / 重跑）；`.env.example` 加 `BRAVE_SEARCH_API_KEY=` placeholder；`.gitignore` 加 `data/imported/search-results.generated.json`（與既有 `discovered-resources.generated.json` 一起，example JSON 仍 commit）；`docs/DISCOVERY_CRAWLER_PLAN.md` 升 v2、補 D-1 五個 provider 比較表（Brave / Tavily / Bing / Google CSE / SerpAPI 各列「適合本專案 / API key 需求 / Node fetch 容易度 / result 對應 schema 容易度 / 免費額度（標註使用者自行確認） / 優點 / 缺點 / 建議優先度」）+ D-2 硬邊界更新（不硬編 key / 不偽裝 UA / 不爬搜尋結果頁 / 不 follow URL / 不自動 pipe）+ I 段 CLI flag 更新 + L 後續擴充 8 條 + M v2 升級紀錄；CLI 端到端跑 4 種 regression：(1) `--help` exit 0 / (2) manual-json 13 entries / 11 shouldCollect=true（與 v0.1 一致，無 regression） / (3) mock 與 manual-json 相同結果 / (4) future-bing-api exit 2 / (5) brave-search 無 API key exit 2 + 印申請步驟（不 crash stack trace、不硬編假資料）。
  - **Brave Search happy path 已由使用者本機小量實測成功**（2026-05-13）：使用者執行 `node --env-file=.env.local scripts/discover_resources.mjs --provider brave-search --query-file data/imported/discovery-queries.example.json --limit-per-query 1 --query-limit 1 --search-out data/imported/search-results.generated.json --out data/imported/discovered-resources.generated.json`；CLI 端到端 fetch Brave Search → 回傳 1 result（title=`Pre A1 Starters` / url=`https://www.lebusanglais.com/wp-content/uploads/2024/12/Pre-A1-Starters-Sample-Paper.pdf` / rank=1）→ 成功寫出 `search-results.generated.json` + `discovered-resources.generated.json`（1 entry / `shouldCollect=true` / sourceType=`third_party` / resourceType=`pdf` / level=`Pre A1 Starters` / score=8 / reasons=[`keyword_starters`, `keyword_pre_a1`, `keyword_sample_paper`] / collectorMode=`index-only` / reviewStatus=`discovered_candidate` / provenance.discoveryVersion=`discover_resources.mjs@v0.2` / provenance.searchProvider=`brave-search`）；對應實測紀錄寫入 `docs/DISCOVERY_CRAWLER_PLAN.md` N 段 + M v2.1。**happy path 驗證仍不代表正式題庫匯入完成**——後續仍需 P3-10-D-3（pipe collector）/ E（AI normalize）/ F（人工審核）/ K（寫入正式題庫）才會進 `/quiz`。**生成檔案絕不 commit**（已 gitignore）；**`.env.local` 絕不 commit**；**真實 API key 絕不出現在文件 / 回報 / log**。
- **未做**（P3-10-D-2B 整體仍 🟡）：Tavily / Bing / Google CSE / SerpAPI 落地；discovery → collector pipe（屬 P3-10-D-3）；AI scoring；`.env.local` 自動載入；對 17 條 query 完整批次實測（**屬 user-side quota 決策、不在本輪範圍**）
- 🟡 **P3-10-D-3**：Discovery → Collector 自動 pipe——**部分完成**（2026-05-13）：新增 `scripts/collect_discovered_resources.mjs` v0.1 pipe CLI（讀 `data/imported/discovered-resources.generated.json` 內 `shouldCollect=true` 條目、依 `collectorMode` 分流至 collector helper、產出 batch 結構 `data/imported/source-documents.batch.generated.json`）；支援 6 個 flag：`--input` / `--out` / `--limit`（預設 5、防呆避免一次抓太多） / `--only-should-collect`（預設 yes） / `--dry-run`（預設 no） / `--help`；對 HTML 重用 collector 既有 `buildSourceDocumentEntry`（full-text）/ `buildResourceIndexEntry`（index-only）；對 pdf / image / audio / video **不下載 body**、只用 HEAD 抓 metadata（contentType / contentLength / httpStatus）+ 加 `asset_collection_not_implemented`（pdf 多一筆 `pdf_parser_not_implemented`）warnings；單筆 fetch / parse 失敗時記 error 並繼續下一筆、整批不中斷；每 fetch 之間 500ms delay；HTTP timeout 沿用 collector 預設 15s；3 種 skip 路徑：`skipped_not_should_collect`（shouldCollect=false 且 only-should-collect=yes）/ `skipped_missing_url`（entry 缺 url）/ `skip_due_to_limit`（超過 --limit 上限的 eligible 條目）；`scripts/web_resource_collect.mjs` **微調**——把 `main()` 包進 `import.meta.url === pathToFileURL(process.argv[1]).href` 判斷 + 新增 `export { COLLECTOR_VERSION, COLLECTOR_USER_AGENT, DEFAULT_TIMEOUT_MS, fetchUrl, buildResourceIndexEntry, buildSourceDocumentEntry, buildWarnings }`（**CLI 行為完全不變**：直接 `node scripts/web_resource_collect.mjs --mode ... --url ...` 仍照舊跑 main；只有透過 `import` 時不會觸發 CLI flow）；`.gitignore` 加 `data/imported/source-documents.batch.generated.json` 排除；`docs/DISCOVERY_CRAWLER_PLAN.md` G 段補 pipe 與 collector 分工說明 + M v3 升級紀錄；`docs/WEB_RESOURCE_COLLECTOR_PLAN.md` F 段尾段補 D-3 pipe 落地說明 + v1.2 微調紀錄。CLI 端到端跑 3 種測試全綠：(1) `--help` exit 0 / (2) **dry-run**（讀使用者本機 happy path 1 筆 PDF → totalInput=1 / eligible=1 / dryRun=1，輸出 batch 結構正確、status=dry_run、warnings 含 dry_run code、未實際 fetch） / (3) **real 小量 `--limit 1`**（PDF asset HEAD only → HTTP 200 / contentType `application/pdf` / contentLength 6914257 bytes 6.9MB、status=collected、document.kind=`asset_metadata`、warnings 含 asset_collection_not_implemented + pdf_parser_not_implemented、**body 未下載 / 未解析**） + 額外 fixture 驗證：(a) HTML full-text 分支（yle.tw fixture → status=collected / document.kind=source_document / headings=4 / links=50 / assets=26 / cleanedTextLength=269 / 重用 collector 規則無 regression） + (b) 3 種 skip 路徑（shouldCollect=false / missing url / limit 截斷皆正確分類）。**仍不代表正式題庫匯入完成**——後續仍需 P3-10-E（AI normalize；需 OpenAI API） / F（人工審核 + reviewStatus 升 `approved_for_practice`） / K（寫入 `data/p3-example-questions.json` 並上線 `/quiz`）；本輪硬邊界全守：未動 `data/p3-example-questions.json` / `data/exam-papers.example.json` / `public/images/` / `public/audio/`；未呼叫 OpenAI；未下載 PDF / image / audio binary；未解析 PDF；不接後端 / DB / 登入。**未做**：asset 下載到 `tmp_crawl/`（屬未來 asset-aware 模式）/ PDF parser（屬 P3-10 後續刀數，需評估 pdfjs-dist / pdf-parse 依賴）/ multi-batch history / discovery 重跑時 batch 增量更新 / AI normalizer
- 🟡 **P3-10-E**：AI normalizer 原型（rule-based / mock-ai 第一版）——**部分完成**（2026-05-13）：新增 `scripts/normalize_collected_sources.mjs` v0.1（~430 行；6 段：常數 / CLI parsing / helpers / 共用 output item builder / rule-based draft + 5 種 skip 分類 / main）；支援 6 個 flag：`--input`（預設 `data/imported/source-documents.batch.generated.json`） / `--out`（預設 `data/imported/normalized-questions.generated.json`） / `--mode rule-based|mock-ai`（預設 rule-based；openai mode 已預留字面量但 exit 2） / `--limit`（預設 5、防呆） / `--dry-run yes|no`（預設 no） / `--help`。**只處理** `item.status="collected"` + `document.kind="source_document"` 條目；5 種 skipped 分類：`skipped_not_collected` / `skipped_asset_metadata` / `skipped_resource_index` / `skipped_not_source_document` / `skipped_no_cleaned_text`、加 `skip_due_to_limit` 共 6 種；無 extractedCandidates 時走 **observation** 路徑（**不硬造題**）保留 headings 前 3 個摘要供人工檢視；有 extractedCandidates 時每個 candidate 獨立產一筆 draft，draft 結構 `{ questionType, starterPart, prompt, answer=null, options=[], confidence, normalizationNotes[] }`——**保守邊界 rule-based 不猜 answer / options**，留給 reviewer 手補；所有 output items **一律** `reviewStatus="needs_human_review"` + `sourceStatus="draft_from_collected_source"` + `isReadyForPractice=false`，等 P3-10-F 人工審核升 `approved_for_practice` 才能進正式題庫（P3-10-K）；mode=mock-ai 同 rule-based 邏輯但每筆 draft 加 `mock_ai_response` warning + normalizationNotes 前綴改 `mode:mock-ai`，作為「AI 跑過但走 rule-based fallback」的模擬；`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` E 段完全重寫（v2，含 E-1 input / E-2 mode 對應 / E-3 6 種 skipped 分類 / E-4 observation 路徑 / E-5 draft 路徑 / E-6 一律標 reviewStatus / E-7 batch output schema / E-8 v0.1 不做清單）；`docs/PRACTICE_DATA_PLAN.md` P3-10-E 條目從 ⬜ 改 🟡；CLI 6 種測試全綠：(1) `--help` exit 0 / (2) **rule-based 跑使用者本機 PDF batch**（totalInput=1 / eligible=0 / drafts=0 / observations=0 / skipped=1 with `skipped_asset_metadata`，**證明 third-party PDF 不會被轉題**） / (3) **rule-based 跑 fixture**（5 種輸入分支：source_document + 2 candidates → drafts=2 / source_document + 0 candidates → observations=1 / source_document + 空 cleanedText → skipped_no_cleaned_text / resource_index → skipped_resource_index / status=failed → skipped_not_collected；summary 計算正確） / (4) **dry-run yes** 把所有 draft 標 dry_run + 加 dry_run warning / (5) **mode=mock-ai** 加 mock_ai_response warning 與 normalizationNotes 前綴 / (6) `--mode openai` 與 unsupported mode 皆 exit 2 + 印申請 / 未支援訊息。**本輪硬邊界全守**：未呼叫 OpenAI / 未要求 OPENAI_API_KEY（rule-based / mock-ai 不需任何 env） / 未下載 PDF / image / audio / 未解析 PDF / 未把 third-party source 轉正式題庫 / 未修改 `data/p3-example-questions.json` / `data/exam-papers.example.json` / 未新增正式題目 / 未產 TTS / 未改 UI / quiz / review / schema / 未接後端 / DB / 登入。**未做**：openai mode 真實實作（需獨立刀數含 OPENAI_API_KEY 讀取 / prompt 設計 / token 與成本管控） / 自動 approve / answer / options 推斷 / 寫入正式題庫（屬 P3-10-F / K）
- ⬜ **P3-10-E 後續**：openai mode 落地（屬獨立刀數；需 OPENAI_API_KEY、prompt 規格對齊 `docs/AI_QUESTION_GENERATION.md`、token / 成本管控）
- 🟡 **P3-10-F 後續**：reviewed output 覆寫保護 / merge-with——**部分完成**（2026-05-13）：`scripts/review_normalized_questions.mjs` v0.1 → v0.2；補上 Codex 在 P3-10-F 驗收時指出的 UX 風險（reviewed-questions.generated.json 覆寫式、reviewer 編輯可能遺失）。新增 2 個 CLI flag：`--overwrite yes|no`（預設 no）與 `--merge-with <existing-reviewed-json>`；`--out` 已存在但兩者皆未指定時 **exit 2**（不寫任何檔，印錯誤訊息 + 3 條對策：a 加 `--overwrite yes` / b 加 `--merge-with` / c 改 out 路徑）；`--overwrite yes` 允許覆寫並把 `summary.overwritten=true` + batchWarnings 加 `overwrite_enabled`；`--merge-with` 讀 existing reviewed batch、依 mergeKey 命中 → 保留 reviewerFields + reviewStatus（reviewer 編輯不遺失） + source provenance 以最新 input 為準（理由：reviewer 驗的是題目本身、上游 metadata 可能因 discovery 重跑而更新），warning 加 `merged_from_existing_review`；existing 有但新 input 無對應的條目 → 整筆保留 status=`orphaned_existing_review` + warning `orphaned_existing_review`，reviewerFields 完整保留不遺失。**mergeKey 規則**：優先 `sourceItemId + 從 originalDraft.normalizationNotes 取出的 candidate_index:<n>`；fallback `sourceItemId + originalDraft.prompt + originalDraft.questionType + originalDraft.starterPart`；同時支援 normalizer 條目（draft 在 `item.draft`）與 reviewed 條目（draft 在 `item.originalDraft`）。summary 加 3 個新欄位 `merged` / `orphaned` / `overwritten`；payload 加 top-level `batchWarnings[]` 區塊（與 items[] 並列），收錄 4 個新 warning code（`output_exists_requires_overwrite_or_merge` / `overwrite_enabled` / `merged_from_existing_review` / `orphaned_existing_review`）；`mergeWith` 路徑也記入 payload。HELP_TEXT 重寫含 v0.2 升級紀錄 + mergeKey 規則 + 三種寫檔模式 + batchWarnings 4 code；`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` 升 v3.1（F-pre-7 5 個子段含三種寫檔模式表 + mergeKey 規則 + merge 行為 + summary 新欄位 + batchWarnings code 表）；`docs/PRACTICE_DATA_IMPORT_PLAN.md` C-5 步補覆寫保護說明；`docs/PRACTICE_DATA_PLAN.md` P3-10-F 條目下新增子彈點。CLI 5 種 regression 全綠：(1) `--help` v0.2 標題 + 新 flag 都有列 / (2) **out 不存在** → 正常建立 / overwritten=false / (3) **out 已存在 + 無 flag** → exit 2 + 印 3 條對策 / (4) **`--overwrite yes`** → 覆寫成功 / batchWarnings=[overwrite_enabled] / summary.overwritten=true / (5) **`--merge-with` fixture**（normalized 3 筆 input：fx-001 / fx-002 / fx-003-new + existing reviewed 3 筆：fx-001 approved 已填 / fx-002 WIP / fx-old orphaned）→ summary `merged=2 orphaned=1 queued=3`、fx-001 `reviewerFields.approved=true / answer="apple"` 完整保留、fx-002 WIP notes 保留、fx-old 整筆保留為 orphaned_existing_review + reviewerFields 仍含 `q-fb-imp-old` / `answer="have"`、fx-003-new 為 fresh template、batchWarnings=[merged_from_existing_review, orphaned_existing_review] / (6) **validate-reviewed regression**（跑 merge 結果）→ totalInput=4 / approvedClaimed=1 / passedValidation=1（fx-001）/ skippedNotApproved=3（fx-002 / fx-003-new / fx-old）；對既有 `data/imported/reviewed-questions.generated.json`（PDF skipped）跑 validate-reviewed 也通過 totalInput=1 / approvedClaimed=0 / skippedNotApproved=1。**本輪硬邊界全守**：未呼叫 OpenAI / 未需 OPENAI_API_KEY / 未下載 PDF / image / audio / 未解析 PDF / 未修改 `data/p3-example-questions.json` / `data/exam-papers.example.json` / 未讓 `/quiz` 使用 imported 題庫 / 未產 TTS / 未改 UI / quiz / review / schema / 未接後端 / DB / 登入 / 未紀錄真實 API key。**未做**：批次操作 helper（bulk-approve 等 reviewer ergonomics）/ JSON Schema 檔案（IDE auto-validation）/ Review UI dashboard / openai mode / approved → 正式題庫的轉換 CLI（屬 P3-10-K）
- 🟡 **P3-10-F**：匯入題目人工審核流程（CLI + JSON workflow 第一版）——**部分完成**（2026-05-13）：新增 `scripts/review_normalized_questions.mjs` v0.1（~440 行；6 段：常數 / CLI parsing / JSON helpers / prepare-review 邏輯 + 5 種 skip 分類 / validate-reviewed 驗證邏輯 + 題型 specific 規則 / main）；6 個 flag：`--input`（必填，兩 mode 對應不同上游） / `--out`（選填，prepare-review 預設 `data/imported/reviewed-questions.generated.json` / validate-reviewed 預設 `data/imported/review-validation.generated.json`） / `--mode prepare-review|validate-reviewed`（必填） / `--limit`（prepare-review 預設 10、防呆） / `--dry-run yes|no`（預設 no） / `--help`；**prepare-review** filter：只 queue `status=draft + reviewStatus=needs_human_review + isReadyForPractice=false + draft!=null`，其餘 5 種 skip reason（`skipped_status_not_draft` / `skipped_review_status_not_needs_review` / `skipped_already_ready` / `skipped_draft_null` / `skip_due_to_limit`）；reviewerFields template 預填 `approved=false` + `approvedForPractice=false` + `finalQuestion.{type, starterPart, prompt}`（從 draft 抓），**answer / options 一律留空**——不亂猜（保守邊界與 P3-10-E 一致）；**validate-reviewed**：只驗 `reviewerFields.approvedForPractice === true` 的條目，其餘 `validationStatus=skipped` + reason；驗證項目：approved=true 一致性 / reviewStatus="approved_for_practice" 一致性 / finalQuestion 必填欄位（id / type / starterPart / prompt / answer 各自非空）/ type 與 starterPart 字面量對齊 ALLOWED_QUESTION_TYPES 9 種（含 listening-image-choice） + ALLOWED_STARTER_PARTS L1-L4 / RW1-RW5；題型 specific：spelling answer 非空字串、true-false answer 必 yes/no（忽略大小寫）、CHOICE_TYPES（multiple-choice / word-choice / listening-image-choice / listening-choice / picture-choice）options>=2 且 answer 對應 options（純字串 or `{id, value}` 物件）；output 寫 `data/imported/review-validation.generated.json` + 印 console summary（含 totalInput / approvedClaimed / passedValidation / failedValidation / skippedNotApproved），**不修改 reviewed file、不寫正式題庫**；`.gitignore` 加 2 行（`reviewed-questions.generated.json` + `review-validation.generated.json`）；`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` 升 v3（新增 F-pre 段 7 個子段：兩 mode / prepare-review filter / reviewerFields template / reviewer 操作流程 4 步 / validate-reviewed 驗證規則表 / output schema / v0.1 不做清單）；`docs/PRACTICE_DATA_IMPORT_PLAN.md` C 段流程 4/5/6 步重寫對齊 P3-10-E + F + K 範圍；`docs/PRACTICE_DATA_PLAN.md` P3-10-F 條目從 ⬜ 改 🟡。CLI 端到端跑 4 種測試全綠：(1) `--help` exit 0 / (2) **prepare-review 跑使用者本機 normalized output**（totalInput=1 / eligible=0 / queued=0 / skipped=1，**PDF item 正確 skip 因 status=skipped 不是 draft**） / (3) **prepare-review fixture**（5 種 normalizer output 條件：3 種 draft type → queued=3 / observation → skipped / failed/skipped → skipped；reviewerFields template 全預填且 `approved=false`） / (4) **dry-run yes**（drafts→ dry_run + warning，reviewerFields 仍存） / (5) **validate-reviewed fixture**（6 種 reviewer 編輯情境：valid spelling → passed / true-false answer "maybe" → failed `true_false_answer_invalid` / multiple-choice answer not in options → failed `answer_not_in_options` / missing required fields → failed 3 errors / `approvedForPractice=false` → skipped / 不一致 `approvedForPractice=true` 但 `approved=false` 且 reviewStatus 未升 → failed 2 errors；summary passedValidation=1 / failedValidation=4 / skippedNotApproved=1 全部正確）+ (6) 邊界 exit 2（缺 --input / --mode 缺值 / unsupported mode / input 不存在）。**本輪硬邊界全守**：未呼叫 OpenAI / 未需要 OPENAI_API_KEY / 未下載 PDF / image / audio / 未解析 PDF / 未修改 `data/p3-example-questions.json` / 未修改 `data/exam-papers.example.json` / 未讓 `/quiz` 使用 imported 題庫 / 未產 TTS / 未改 UI / quiz / review / schema / 未接後端 / DB / 登入 / 未紀錄真實 API key。**未做**：approved → 正式題庫的轉換 CLI（屬 P3-10-K，含 `lib/data.ts` 加 `approved_for_practice` 過濾邏輯與正式 ExamQuestion schema 對齊） / Review UI dashboard / 多 reviewer 簽核 / 與 `docs/AI_QUESTION_GENERATION.md` 6 項品質檢查的自動化整合
- ⬜ **P3-10-G**：Vocabulary 圖片 / SVG 補齊第一批（往 20+ 張推進；對齊「最小可玩資料包」vocabulary review 目標）
- ⬜ **P3-10-H**：RW3 spelling **source-first** 題庫擴充到最小可玩數量（至少 8 題；目前 4 題） — **方向修正（P3-10-L，2026-05-14）**：不再用 AI 補題；必須透過 source registry approved sources 產生（[`docs/SOURCE_REGISTRY_PLAN.md`](./docs/SOURCE_REGISTRY_PLAN.md)）。既有 4 題 ai_generated / custom 視為 dev seed、不計入正式匯入版完成度
- ⬜ **P3-10-I**：RW1 yes/no **source-first** 題庫擴充（至少 4 題；目前 2 題） — **方向修正（P3-10-L，2026-05-14）**：不再用 AI 補題；必須透過 source registry approved sources 產生
- ⬜ **P3-10-J**：L3 listening **source-first** 多題補齊（至少 3 題配新 OpenAI v2 音檔；目前 1 題） — **方向修正（P3-10-L，2026-05-14）**：題目文字來源走 source-first；TTS 音檔自製仍延續既有流程（[`docs/TTS_AUDIO_WORKFLOW.md`](./docs/TTS_AUDIO_WORKFLOW.md)）
- ✅ **P3-10-L**：正式來源優先匯入規則 + Source Registry（2026-05-14）—— 新增 [`docs/SOURCE_REGISTRY_PLAN.md`](./docs/SOURCE_REGISTRY_PLAN.md)（A 段背景 / B 段 source-first 原則 7 種 sourceKind + 6 條總結 / C 段 source registry 18 欄位 + 5 種 publisherType / 5 種 collectionStatus / 4 種 reviewStatus / D 段匯入規則 D-1 ~ D-6 硬邊界 / E 段正式流程 9 步 / F 段與既有文件關係 / G 段本輪落地範圍 / H 段版本）；新增 `data/imported/source-registry.example.json` 5 筆示範資料（official_learning_material / official_sample needs_manual_check / past_paper needs_manual_check / third_party_practice / unknown）— 範例條目刻意**不**標 approved_for_import，避免聲稱已完成授權確認；新增 `scripts/validate_source_registry.mjs` v0.1 靜態 schema validator（無新依賴；`--input` / `--help` flags；驗證必填欄位 / enum 值 / sourceUrl 格式 / official_sample 與 past_paper 必填強化 / approved_for_import 一致性檢查；不抓網路 / 不呼叫 API / 不寫輸出檔，純 stdout summary；exit code 0=全通過 / 1=有失敗 / 2=CLI 錯）；同步更新 `docs/PRACTICE_DATA_PLAN.md`（升 v1.1）/ `docs/PRACTICE_DATA_IMPORT_PLAN.md`（升 v1.1，三層架構補 Layer 0 + 7 步流程補第 0 步 source registry gate）/ `docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md`（升 v4.3）/ `docs/DISCOVERY_CRAWLER_PLAN.md`（升 v3.1，B 段補步驟 7.5 source registry gate）/ `docs/WEB_RESOURCE_COLLECTOR_PLAN.md`（升 v1.3，G 段尾段補 source-first 邊界）/ `README.md`（文件索引補 SOURCE_REGISTRY_PLAN 一條）；P3-10-H / I / J 改為 source-first 題庫擴充（**不再** 是 AI 補題）。**本輪硬邊界全守**：不新增 AI / custom 題湊題數 / 不改 `data/p3-example-questions.json` / 不改 `data/exam-papers.example.json` / 不讓 `/quiz` 切到 imported 題庫 / 不改 UI / 不呼叫 OpenAI / Brave Search API / 不下載 PDF / image / audio / 不解析 PDF / 不爬網站 / 不自動產題 / 不新增 npm 依賴 / 不擴張 `lib/types.ts` `QuestionSource` union / 不 commit `.env.local` / 不 commit `*.generated.json`。lint / typecheck / build 全綠；validator CLI `--help` / 跑 example registry 全通過（5 entries / passed=5 / failed=0 / exit=0）+ 破題 fixture 驗證錯誤訊息正確（12 errors / exit=1）。本輪是**方向修正、不是補題**——正式匯入版以官方 / 歷屆 / 可追溯來源為主，不再用 AI 隨便補題。
  - 🟡 **P3-10-L 修補**（2026-05-15）：Codex 有條件通過後最小修補——`scripts/validate_source_registry.mjs` v0.1 → **v0.1.1**：補上 batch-level **duplicate sourceId** 檢查（`findDuplicateSourceIds` / `applyDuplicateSourceIdErrors` helpers + main flow 串接；同批內 sourceId 重複時，所有相同 id 的 entry 都標 failed + push `duplicate_sourceId:<id>` + `duplicate_sourceId_at_entries:<indices>` 兩條錯誤；不 crash、其他 per-entry errors 仍列出；summary 補 `duplicateSourceIds=N`；exit code 仍是 1 當有失敗）；HELP_TEXT 升 v0.1.1 + 補 batch-level 第 9 條規則說明。`README.md` 修正舊 AI 仿真題敘述：長期方向把單一「AI 仿真題 + 人工審核（先進 source_materials/ai_generated/、經審核後轉正式 JSON）」bullet 拆成兩條（正式題庫主線 source-first + AI 延伸題降級為「正式題庫完成後的延伸模式」，**不**用來補正式題庫數量 / 不偽裝主線標記 / UI 報告與正式 / 歷史題庫明確區分）；長段「AI 仿真題會依官方題型結構...」改寫加入 P3-10-L source-first 引用與「不再直接視為正式 JSON 候選」；「下一步」段題目來源標記說明補 P3-10-L 後新規則。`.gitignore` 加 `data/imported/source-registry.generated.json` 避免未來 generated registry 誤 commit（`source-registry.example.json` 仍 trackable，git check-ignore 驗證通過）。**6 種 CLI regression 全綠**：(1) `--help` v0.1.1 標題 + 第 9 條 batch-level 規則 + summary 補 duplicateSourceIds 都印 / (2) example registry 仍 PASS（5 entries / passed=5 / failed=0 / duplicateSourceIds=0 / exit=0） / (3) 純 duplicate fixture（2 筆同 sourceId=src-dup → 兩筆都 FAIL with `duplicate_sourceId:src-dup` + `duplicate_sourceId_at_entries:0,1`、summary duplicateSourceIds=1 / exit=1） / (4) duplicate + 其他 per-entry errors fixture（第 2 筆 official_sample 但 sourceUrl="not-a-url" / publisher 空 / publisherType=third_party / provenanceNotes 空 → per-entry 6 條 errors **與** duplicate 2 條 errors 同時列出，不 crash） / (5) lint / typecheck / build 全綠 / (6) 正式題庫（`data/p3-example-questions.json` / `data/exam-papers.example.json`） / lib/* / components/* 整輪未動。**本輪硬邊界全守**：不新增 AI / custom 題 / 不改正式題庫 / 不改 schema / 不改 UI / 不呼叫 OpenAI / Brave / 不下載 PDF / image / audio / 不爬網站 / 不新增 npm 依賴 / 不 commit `.env.local` / 不 commit `*.generated.json` / 不紀錄真實 API key。屬 P3-10-L 最小修補；不擴大 scope。修補後待 Codex 重新驗收把原本「有條件通過」收斂為通過；P3-10 / P3 整體仍 🟡 不標完成、P4 / P5 仍 ⬜ 不啟動。
- 🟡 **P3-10-M**：Source registry generated workflow（部分完成，2026-05-15）—— 新增 `scripts/build_source_registry.mjs` v0.1：對 `data/imported/discovered-resources.{example,generated}.json` 保守推論並寫出 `data/imported/source-registry.generated.json`。CLI flags：`--input` / `--out` / `--help` / `--mode build`（只支援 build，其他 mode exit 2） / `--limit 20`（正整數、預設 20）。**核心保守策略**：(a) **deterministic sourceId**（`src-gen-001` / `src-gen-002` ... 由 input 順序產出；重跑相同 input 產出 byte-identical output）；(b) **dedup by `normalizedUrl ?? url`**（同 URL 多次出現只保留第一筆，後續標 `skipped_duplicate_url`；無效 URL → `skipped_missing_or_invalid_url`；超過 `--limit` → `skipped_due_to_limit`）；(c) **publisher / publisherType 嚴格 allowlist**——`OFFICIAL_HOSTNAMES`（`cambridgeenglish.org` / `cambridge.org`）才能標 `publisherType="official"`；`LOCAL_AGENCY_HOSTNAMES`（`yle.tw` / `certificate.tw`）標 `publisherType="school"`；discovery 若把 `sourceType="official"` 標到非 allowlist hostname，**自動降為 `publisherType="third_party"` 與 `sourceKind="third_party_practice"`**（驗收：fake-cambridge.example fixture 已驗）；(d) **sourceKind / publisherType 一致性自動降級**——validator 規則 #6 要求 `official_sample` / `past_paper` 必須 `publisherType ∈ {official, school}`，若推論不一致則降級為 `third_party_practice` 或 `unknown`，避免 validator fail；(e) **partsCovered 從 disc.detectedExamParts 過濾 ALLOWED_PARTS**（L1-L4 / RW1-RW5 / SP1-SP4 / unknown），空則設 `["unknown"]`，**不亂猜** L1 / RW3；(f) **fileType 推論**（resourceType `pdf` → `pdf`；`page` / `worksheet` / `vocabulary_list` / `listening_practice` / `reading_writing_practice` / `sample_paper` → `html`；fallback 依 URL extension）；(g) **language 自動偵測**（CJK 字符 → `zh-Hant`，否則 `en`）；(h) **exam 由 level 推導**（含「Pre A1」/「Starters」→ `Cambridge Starters`；含「Movers」→ `Cambridge Movers`；含「Flyers」→ `Cambridge Flyers`；其他 → `unknown`）；(i) **collectionStatus 一律 `discovered`**（discovery 並未實際 collect 任何 metadata / body / asset）；(j) **accessType 一律 `unknown`**（除非 reviewer 確認，否則保守不聲稱 public）；(k) **collectedAt / lastCheckedAt 一律 `null`**。**reviewStatus 規則**：預設 `pending_review`；下列任一觸發 → 升 `needs_manual_check`：`fileType === "pdf"` / `sourceKind ∈ {official_sample, past_paper, official_learning_material}` / `sourceKind === "unknown"` / `publisherType === "unknown"` / discovery `score < 5` / `shouldCollect === false`。**絕不**輸出 `approved_for_import`——硬邊界。**provenanceNotes / rightsNotes 用固定保守模板**：`Generated from discovery output (disc id: ..., source query: ...); reviewer must verify source kind, publisher, parts covered, and rights before import.` / `Not reviewed. Do not import until reviewer confirms usage boundary, license, and copyright. Auto-generated entry — never approve without manual verification.`，**絕不**聲稱已授權 / 已審核 / 可直接匯入。**端到端 6 種測試全綠**：(1) `--help` exit 0 + 完整 usage（含 What it does / Defaults / What it never does / Output / Exit code 段）/ (2) example input 6 筆 → written=6 / skipped=0；reviewStatus={needs_manual_check:5, pending_review:1}；sourceKind={official_sample:2, third_party_practice:2, past_paper:1, unknown:1}；**0 筆 approved_for_import** / (3) `validate_source_registry.mjs` 跑 generated 6/6 PASS / failed=0 / duplicateSourceIds=0 / (4) deterministic 重跑 → `diff -q` byte-identical / (5) duplicate URL + 無 URL fixture → 正確分類 skipped；validator 仍通過 / (6) fake-cambridge.example fixture（discovery 標 sourceType=official + sample_paper resourceType + score=8）→ 自動降為 `publisherType=third_party` + `sourceKind=third_party_practice`，**不**升 official_sample（驗收保守降級邏輯）。**邊界 exit 2**：unsupported mode（`--mode rule-based`）/ 缺 `--input` / 未知 flag / JSON parse 失敗 / input 不是陣列。**`.gitignore` 已於 P3-10-L 補入 `source-registry.generated.json`**（line 57）；`git check-ignore -v` 驗證命中、`git status` 顯示**未被追蹤**。**本輪硬邊界全守**：未呼叫 OpenAI / Brave / 未發 HTTP / 未下載 PDF / image / audio / 未解析 PDF / 未自動產題 / 未修改 `data/p3-example-questions.json` / `data/exam-papers.example.json` / 未改 UI / quiz / review / schema / 未接後端 / DB / 登入 / 未紀錄真實 API key / 未把任何 generated source 標 `approved_for_import` / 未新增 npm 依賴 / 未 commit `.env.local` / 未 commit `*.generated.json`。lint / typecheck / build 全綠。docs 同步：`docs/SOURCE_REGISTRY_PLAN.md` 新增 E-bis 段（v1.1）/ `docs/DISCOVERY_CRAWLER_PLAN.md` 升 v3.2（B 段步驟 7.5 補 build CLI 已落地）/ `docs/PRACTICE_DATA_IMPORT_PLAN.md` 升 v1.2（C 段第 0 步補 build CLI 用法）/ `docs/PRACTICE_DATA_PLAN.md` 升 v1.2（B 段表格 + F 段加 🟡 P3-10-M 部分完成條目）/ README 暫不需大改（既有索引仍指向 SOURCE_REGISTRY_PLAN）。**未做**：collector / normalizer 的 program-layer source-registry gate（屬下一刀；需要在 `scripts/normalize_collected_sources.mjs` / `scripts/web_resource_collect.mjs` 加 `--source-registry` flag、跳過 reviewStatus ≠ approved_for_import 的條目）；approved_for_import 後續整合 / Review UI dashboard / multi-round history。屬 P3-10-M 第一刀；不擴大 scope。**P3-10 / P3 整體仍 🟡 不標完成、P4 / P5 仍 ⬜ 不啟動**。
- 🟡 **P3-10-N**：Collector / Normalizer approved_for_import gate（部分完成，2026-05-15）—— 新增共用 helper `scripts/source_registry_gate.mjs` 暴露 4 個函式：`normalizeSourceUrlForGate` / `loadSourceRegistry` / `buildApprovedUrlSet` / `classifyUrlAgainstRegistry`。三個下游 CLI 新增 optional `--source-registry <path>` flag：
  - `scripts/collect_discovered_resources.mjs` v0.1 → **v0.2**：discovery → collector pipe；提供 flag 時於 eligible 前過 gate，未命中 approved_for_import 的 URL 一律 skipped + reason code（`skipped_source_not_approved_for_import` / `skipped_not_in_source_registry` / `skipped_invalid_url_for_gate`），**不 fetch / 不 HEAD / 不寫 source-document**；summary 加 `sourceRegistry` 區塊（sourceRegistryInput / sourceRegistryEntries / approvedSources / duplicateSourceIdsInRegistry / skippedNotInSourceRegistry / skippedSourceNotApprovedForImport / skippedInvalidUrlForGate）。
  - `scripts/normalize_collected_sources.mjs` v0.1 → **v0.2**：source-documents → normalize；提供 flag 時 gate 套用於 `source_document.url`；未命中 approved 的 source 一律僅輸出 skipped item（**不產 draft / observation**）；同樣 3 種 reason code；summary 加 `sourceRegistry` 區塊（同上欄位）。
  - `scripts/web_resource_collect.mjs` v0.1（最小變更）：單一 URL collector；提供 flag 時於 fetch 前 gate；未命中 approved → **exit 0 不 fetch / 不寫檔**並印原因（保留與 pipe 一致的「gate 拒絕不算錯誤、只是不進下游」語義）；CLI 介面 / 輸出 JSON schema 完全不變（向後相容）。
  **URL normalization 規則（v0.1，保守）**：(1) protocol 保留；(2) host 轉小寫；(3) pathname 結尾若為單一 `/` 保留，否則 strip trailing slash；(4) search 保留；(5) fragment 移除；(6) 不做 utm_* / tracking param 清除。**絕對不做** domain-level 放行——必須是 registry 中明確的 sourceUrl 經 normalize 後 exact match。**5 種 fixture 端到端測試全綠**：(A) registry 有 1 approved + 2 not approved（pending_review / needs_manual_check） + 1 not-in-registry 的 4 URL fixture → collect pipe 與 normalizer 都正確分流：1 通過 gate、3 skipped 並帶正確 reason code；(B) registry 0 approved entries → 全部 skipped、`approvedSources=0`、不 crash；(C) **無 `--source-registry`** → 保留既有行為（無 regression），印 warning 提示正式匯入版應提供；(D) registry JSON 不是 array / parse 失敗 → exit 2 + 印「請用 `scripts/validate_source_registry.mjs` 檢查」提示，且**不寫輸出檔**；(E) URL normalization 驗證——registry 標 `https://example.com/approved/`（尾斜線）+ discovery 3 變體（無尾斜線 / 大寫 host / 含 fragment）→ 全部 3 個都通過 gate（normalize 比對成功）。`web_resource_collect.mjs` 額外驗證 gate 拒絕（exit 0 不 fetch）+ invalid registry（exit 2）+ help text 含 P3-10-N 段。**Gate 通過 ≠ 題目通過 human review**——gate 只是「來源層」通過，題目仍須走 P3-10-E normalizer + P3-10-F human review。**本輪硬邊界全守**：未新增 AI / custom 題 / 未呼叫 OpenAI / Brave / 未發網路請求（測試全用 /tmp fixture + dry-run）/ 未下載 PDF / image / audio / 未解析 PDF / 未自動產題 / 未修改 `data/p3-example-questions.json` / `data/exam-papers.example.json` / `lib/types.ts` / `lib/data.ts` / `components/*` / 未改 UI / schema / 未接後端 / DB / 登入 / 未紀錄真實 API key / 未把任何 generated source 標 `approved_for_import` / 未新增 npm 依賴。lint / typecheck / build 全綠。docs 同步：`docs/SOURCE_REGISTRY_PLAN.md` 升 v1.2（新增 E-bis-5 段「Collector / Normalizer source-first gate」含 URL normalization 規則表 + Gate 行為總覽表 8 種情境 + 不在 P3-10-N 範圍 4 條） / `docs/PRACTICE_DATA_IMPORT_PLAN.md` 升 v1.3（C 段第 0 步補 P3-10-N gate 已落地說明） / `docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` 升 v4.4（A 段補 P3-10-N 程式層 gate 說明 + 3 種 skipped reason）/ `docs/WEB_RESOURCE_COLLECTOR_PLAN.md` 升 v1.4（G 段重寫補 P3-10-N gate 已落地說明）。**未做**：source-registry 程式層 + 後續 P3-10-F approved_for_practice 整合 / 多 reviewer merge tool / domain-level 放行（永不做）/ utm_* 清除（屬未來範圍）。屬 P3-10-N 第一刀；不擴大 scope。**P3-10 / P3 整體仍 🟡 不標完成、P4 / P5 仍 ⬜ 不啟動**。
- 🟡 **P3-10-O**：Source Registry merge / preserve tool（部分完成，2026-05-15）—— `scripts/build_source_registry.mjs` v0.1 → **v0.2**：新增 `--merge-with <existing-source-registry.json>` flag。**Merge key 規則**：主要 `normalizeSourceUrlForGate(sourceUrl)`（與 P3-10-N gate 一致：lowercase host / strip trailing slash / 保留 search / 移除 fragment）、fallback `title(lower-trim)|raw-sourceUrl`；**不**只用 sourceId（input 順序位移會錯位）。**Preserve 規則**：命中既有條目時，下列 14 欄位一律取 existing 值：`sourceId` / `sourceKind` / `publisher` / `publisherType` / `language` / `level` / `exam` / `partsCovered` / `accessType` / `collectionStatus` / `reviewStatus` / `provenanceNotes` / `rightsNotes` / `collectedAt` / `lastCheckedAt`；**特例**：(a) `title` existing 若為 `(no title …)` placeholder 才以 new 替換；(b) `fileType` existing 為 `unknown` 且 new 非 unknown 才以 new 替換；(c) `sourceUrl` 一律保留 existing（避免大小寫 / trailing slash 差異造成 spurious diff）。**Orphan 規則**：既有 entry 在新 discovery input 中找不到對應 mergeKey → 整筆保留附加在 output 尾、**不降級** reviewStatus（即使是 `approved_for_import`）。**sourceId 衝突避讓**：existing entries 保留原 sourceId；新 entries 的 `src-gen-NNN` 編號自動跳過已被既有 entry 使用的 id（例如 existing 占 `src-gen-001` → 新 entries 從 `src-gen-002` 起算）。**`--merge-with` 防護（exit 2 觸發 + 不寫 output）**：檔案不存在 / JSON parse 失敗 / 非 array / 任一 entry 缺 sourceId / 任一 entry 缺 sourceUrl / 含 duplicate sourceId。**Console summary 新增 counters**：`merged` / `newEntries` / `orphaned` / `approvedPreserved` / `existingTotal`，方便 reviewer 一眼看出 preserve 結果。**端到端 fixture 測試全綠**：(A happy path) 3 entries existing（src-test-001 approved_for_import + rightsNotes / src-test-002 needs_manual_check / src-test-orphan approved_for_import + 中文 publisher YLE 台灣 + PDF）+ 3 entries discovery（a / b / c）→ output 4 筆：src-test-001 保留 approved + rightsNotes + sourceKind=official_sample / src-test-002 保留 needs_manual_check / src-gen-001 為新 entry c with pending_review / src-test-orphan 保留 approved + rightsNotes 完全不動；validator 4/4 PASS + 0 duplicates；summary `merged=2 newEntries=1 orphaned=1 approvedPreserved=2`。(B duplicate sourceId in merge-with) → exit 2 + 不寫 output。(C non-array JSON / malformed JSON / 缺 sourceUrl / 不存在檔案) → 全部 exit 2 + 印錯誤訊息 + 不寫 output。(D no `--merge-with`) → 既有行為不變（無 regression）；deterministic 重跑 byte-identical。(E sourceId 衝突避讓) → existing 占 `src-gen-001` 時新 3 entries 配發 `src-gen-002` / `src-gen-003` / `src-gen-004`；validator 通過。(F idempotency) → output 再次當作 `--merge-with` 跑一次 → byte-identical（merge 不會產生 drift）。**本輪硬邊界全守**：未新增 AI / custom 題 / 未呼叫 OpenAI / Brave / 未發網路請求（測試全用 /tmp fixture）/ 未下載 PDF / image / audio / 未解析 PDF / 未自動產題 / 未修改 `data/p3-example-questions.json` / `data/exam-papers.example.json` / `data/imported/source-registry.example.json` / `data/imported/discovered-resources.example.json` / `lib/types.ts` / `lib/data.ts` / `components/*` / 未改 UI / schema / 未接後端 / DB / 登入 / 未紀錄真實 API key / 未把任何 generated source 自動標 `approved_for_import` / 未新增 npm 依賴。lint / typecheck / build 全綠。**docs 同步**：`docs/SOURCE_REGISTRY_PLAN.md` 升 v1.3（新增 E-bis-6 段「Merge / preserve」7 個子段：merge key / preserve 欄位 / orphan / sourceId 衝突避讓 / `--merge-with` 防護 / Console summary counters / 不在 P3-10-O 範圍）/ `docs/PRACTICE_DATA_IMPORT_PLAN.md` 升 v1.4（C 段第 0 步補 P3-10-O merge 已落地說明）/ `docs/PRACTICE_DATA_PLAN.md` 升 v1.4（F 段加 🟡 P3-10-O 部分完成條目）/ README 暫不需大改（既有索引已指向 SOURCE_REGISTRY_PLAN）。**未做**：URL hash deterministic id（屬 v0.3 評估範圍；本輪維持 `src-gen-NNN` + 衝突避讓）/ diff / merge preview 模式 / 多 reviewer 簽核 / 自動化測試 harness（CLI fixture 為手動驗證）。屬 P3-10-O 第一刀；不擴大 scope。**P3-10 / P3 整體仍 🟡 不標完成、P4 / P5 仍 ⬜ 不啟動**。
- 🟡 **P3-10-K 第二刀**：first practice paper 組裝 / paper-level metadata——**部分完成**（2026-05-14）：新增 `scripts/assemble_practice_paper.mjs` v0.1（~450 行；4 段：常數 / CLI parsing / Section 分組 + sourceMix 統計 / main）；7 個 CLI flag：`--questions`（必填）/ `--papers`（必填）/ `--out`（選填，預設 `data/imported/practice-paper.preview.generated.json`）/ `--mode preview|write`（預設 preview）/ `--write yes|no`（預設 no）/ `--paper-id`（必填）/ `--limit`（預設 20）/ `--help`；對齊 `lib/types.ts` `ExamPaper`（**注意實際 schema 使用 `examPaperId` 不是 `id`、且無 `level` 欄位**——任務單範例中的 `level` 不寫入正式 schema） / `ExamSection`（id / title / description / questionIds）/ `SourceMix`（Partial<Record<QuestionSource, number>>）。**組裝策略 v0.1 保守**：(a) 從 `--questions` 讀全部 ExamQuestion；(b) 依 `starterSection` 分組到 listening / reading-writing / speaking 三 sections，缺值依 `question.type` fallback（listening-choice → listening / 其他 → reading-writing）；(c) 沒題目的 section **不出現**避免空 section；(d) `--limit` 限制總題數，超過的標 `skip_due_to_limit` warning（**不挑題、不重排原 array 順序**）；(e) sourceMix 由 question.source 累計 4 種 QuestionSource union 值，不在 union 的標 `unknown_source_value` warning 不計入；(f) 對 9 個 Cambridge Starters Parts（L1-L4 / RW1-RW5）逐一檢查覆蓋率，任一 0 題 → 標 `insufficient_questions_for_part` warning（reviewer 可看出哪 part 還缺）；(g) **不硬造題、不修改 question 內容 / id**。**Duplicate paper id 保護**：preview 標 `duplicate_paper_id` warning + preview JSON 仍寫；write mode 偵測到 dup → 整批 exit 2 + preview JSON 仍寫 + target 不動。**雙開關**：必須同時 `--mode write` + `--write yes` 才能 append 新 paper 到 `--papers`；缺一就 exit 2。**Empty questions 行為**：`--questions` 為 `[]` 時仍寫 preview JSON + warning `no_questions_available` + 9 個 `insufficient_questions_for_part` + 1 個 `paper_has_zero_sections`、exit 0（reviewer 可看狀態）；write mode 不會寫 0-section paper 到 target（exit 0 + 訊息）。`.gitignore` 加 `data/imported/practice-paper.preview.generated.json`；`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` 升 v4.2（F-pre-8-g 新增 7 子段：兩 mode + 雙開關 / 組裝策略 v0.1 / sourceMix 統計 / Part 覆蓋率檢查 / Duplicate paper id 保護 / Output schema / Warning code 表 / v0.1 不做清單；原 F-pre-8-g 更名 F-pre-8-h）；`docs/PRACTICE_DATA_IMPORT_PLAN.md` C 段新增第 7 步「組裝 first practice paper」+ 原 7 步「quiz / review 使用」降為 8。**8 種 CLI 測試全綠**：(1) `--help` exit 0 + 完整 usage / (2) **preview vs real data**（13 題 → 2 sections / sourceMix={ai_generated:9, custom:4}（與既有 example 完全一致） / partBreakdown={L3:1, RW1:3, RW3:5, RW4:3, RW5:1, 其餘 0} / 4 個 part-missing warnings / isDuplicatePaperId=false / 正式 papers 未動） / (3) **write 缺 `--write yes`** → exit 2 + 印 3 條對策 / (4) **duplicate paper id preview**（`--paper-id starters-mock-001` 與既有衝突 → 標 `duplicate_paper_id` warning / isDuplicatePaperId=true / exit 0） / (5) **duplicate paper id write mode** → exit 2 + preview JSON 仍寫 + /tmp/papers-real-copy.json 完全未動 / (6) **empty questions fixture preview**（totalAvailable=0 / sections=0 / 11 warnings 含 `no_questions_available` + 9 個 part insufficient + 1 個 `paper_has_zero_sections` / exit 0） / (7) **empty questions write mode** → 不寫 target（exit 0 + stderr 訊息） / (8) **write happy path vs `/tmp/target-papers.json`**（空 target → 1 paper 寫入 examPaperId=`starters-imported-001` / 2 sections / sourceMix={ai_generated:9, custom:4} / exit 0 + reviewer 須手動 git diff 提示）。**正式 `data/p3-example-questions.json` / `data/exam-papers.example.json` 整輪未被改動**（git diff HEAD 一致）。lint / typecheck / build 全綠。**本輪硬邊界全守**：未讓 `/quiz` 切到 imported 題庫 / 未大改 lib/data.ts 載入流程 / 未改 UI / 未改 quiz / review UI / 未呼叫 OpenAI / 未下載 PDF / image / audio / 未解析 PDF / 未自動產生題目 / 未改 `data/p3-example-questions.json` / 未直接改 `data/exam-papers.example.json`（測試僅用 /tmp）/ 未覆蓋既有 paper / 未新增 npm 依賴 / 未紀錄真實 API key。**未做**：section 內題目自動排序（依 starterPart L1→L4 / RW1→RW5） / Speaking section 內容（屬 P4 範圍）/ `lib/data.ts` 加 paper id 過濾或 imported papers 整合（屬 P3-10-K 第三刀） / Cambridge Starters 官方題量對齊（Listening 20Q / R&W 25Q） / 真實 reviewer commit 後的 e2e 流程實測（reviewer 須先跑 approve_reviewed_questions.mjs 累積足夠題目）
- 🟡 **P3-10-K 修補**：Codex 有條件通過後最小修補（2026-05-14）——`scripts/approve_reviewed_questions.mjs` v0.1 → **v0.1.1**；補上 Codex 驗收指出的兩個問題：(a) **High**：原本只檢查 target 既有 id、未檢查本批 ready items 內部 dup → 修補後拆 `duplicate_id_in_target` / `duplicate_id_in_batch` 兩種 warning code（**同批 dup 兩筆都標 skipped**，避免 reviewer 不知道哪筆對；write mode 任一 dup 整批拒絕 exit 2 不破壞正式題庫 id 唯一性）；summary 新增 `duplicateIdsInTarget` / `duplicateIdsInBatch` 兩個欄位（`duplicateIds` 仍記兩者聯集數量、向後相容）。(b) **Medium**：原本只要 `finalQuestion.source` 非空就直接寫入 question.source、未驗 union → 修補後對齊 `lib/types.ts` `QuestionSource` union 4 種（`official_sample` / `past_paper` / `ai_generated` / `custom`）；空值預設 `custom`；在 union 用該值；**非空但不在 union**（如 reviewer 誤填 `user_provided` / `third_party`）→ 條目 `status=failed` + error code `invalid_question_source`，**不** silent fallback 為 custom（避免掩蓋 reviewer 填錯來源）；reviewer 想表達第三方來源應保留於 `reviewerNotes` / discovery provenance，不寫入正式 `QuestionSource` union。HELP_TEXT 補對應段；流程重組為 4 / 4.5 / 5 / 6 三段（每筆轉換 → batch 內 id 統計 → duplicate 雙維度檢查 → limit）；`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` 升 v4.1（F-pre-8-d 重寫拆兩種 dup + F-pre-8-e 補 source 規則）；`docs/PRACTICE_DATA_IMPORT_PLAN.md` C-6 步補 source union 與 batch dup 說明；`docs/PRACTICE_DATA_PLAN.md` P3-10-K 條目補 v0.1.1 修補摘要。CLI 6 種測試全綠：(1) `--help` 顯示 v0.1.1 + 修補摘要 / (2) preview 對既有 PDF skipped case：仍 readyToAppend=0 / duplicateIds=0(target=0,batch=0) / 正式題庫未動 / (3) write 缺 `--write yes` → exit 2（既有保護） / (4) **Fixture 1 同批 dup preview**：兩筆同 id `q-new-dup` → 兩筆都 status=skipped + warning `duplicate_id_in_batch`、summary `duplicateIdsInBatch=1`、readyToAppend=0、empty-target 不動 / (5) **Fixture 2 同批 dup write mode**：同 fixture → exit 2 + preview JSON 仍寫含 dup warnings + empty-target 不動 / (6) **Fixture 3 invalid source preview + write**：兩筆 source=`third_party` / `user_provided` → status=failed + error `invalid_question_source`、readyToAppend=0、target 不動 / (7) **Fixture 4 合法 source happy path**：5 筆 source=custom / ai_generated / official_sample / past_paper / 空（預設 custom）→ readyToAppend=5、全部寫入 /tmp/target-valid.json（5 題 source 正確分配）。**正式 `data/p3-example-questions.json` / `data/exam-papers.example.json` 整輪未被改動**（git diff HEAD 一致）。lint / typecheck / build 全綠。**本輪硬邊界全守**：未做 first practice paper 整盤組裝 / 未讓 `/quiz` 使用 imported 題庫 / 未呼叫 OpenAI / 未下載 PDF / image / audio / 未解析 PDF / 未自動產生題目 / 未覆蓋既有正式題目 / 未改 UI / quiz / review / schema / 未接後端 / DB / 登入 / 未紀錄真實 API key。**P3-10-K 整體仍 🟡 部分完成**（單題級別轉換鏈 + 兩個保護修補打通；paper 整盤組裝 / Review UI / `--rollback` flag / lib/data.ts approved 過濾 / Codex 驗收 first practice paper 仍未做）
- 🟡 **P3-10-K**：first practice paper 組裝與驗收 / approved → 正式題庫轉換 CLI——**部分完成**（2026-05-14）：新增 `scripts/approve_reviewed_questions.mjs` v0.1（~580 行；6 段：常數 / CLI parsing / JSON helpers / ExamQuestion 轉換 / 分流與管線 / main + entry-script gate）。**3 個必填 flag**（`--reviewed` / `--validation` / `--target`）+ 5 個選填 flag（`--out` 預設 `data/imported/approved-questions.preview.generated.json` / `--mode preview|write` 預設 preview / `--write yes|no` 預設 no / `--limit` 預設 10 / `--help`）；**preview mode 對 target 唯讀、絕對不動正式題庫**；**write mode 雙開關**：必須同時 `--mode write` + `--write yes`，缺一就 exit 2 + 印 3 行明確說明。**5-AND 篩選**（任務單規範）：validate-reviewed `validationStatus=passed`（且 `finalQuestionId` 一致）/ reviewed `reviewStatus=approved_for_practice` / `reviewerFields.approved=true` / `reviewerFields.approvedForPractice=true` / `reviewerFields.finalQuestion` 存在；不符 → 5 種 skipped reason（`skipped_not_in_validation_passed` / `skipped_review_status_not_approved` / `skipped_approved_false` / `skipped_approved_for_practice_false` / `skipped_no_final_question`）。**7 種題型支援**：spelling（image 從 imageSrc；不亂補 spellingHint / letterScramble）/ true-false（answer 強制 yes/no 忽略大小寫）/ multiple-choice（options 正規化 string[]；answer 必在 options）/ picture-choice（image 必填；options string[]）/ word-choice（options 必須 `{value, image}`；answer match value）/ listening-choice（audioSrc 必填；audio legacy 用 audioSrc 同值；options 自動偵測 text/image）/ fill-blank（options 選填；有時須 answer 在 options）；**2 種 unsupported**：`matching`（template 沒 pairs[]）/ `listening-image-choice`（不在 QuestionType union）→ 標 `unsupported_question_type` skip。**Duplicate id 保護**：preview mode 標 warning `duplicate_id` + 仍記入 items（reviewer 可看）；write mode 偵測到任何 duplicate id → **整批拒絕寫入** + **exit 2**（且 preview JSON 仍寫供 reviewer 檢視）；reviewer 須改 id 或從 target 移除既有題目重跑。**ExamQuestion 轉換規則（v0.1 保守）**：source 預設 `custom`；starterPart 依 type fallback（spelling→RW3 / true-false→RW1 / picture-choice→RW1 / word-choice→RW3 / multiple-choice→RW4 / fill-blank→RW4 / listening-choice→L3）；starterSection（listening-* → listening / 其餘 → reading-writing）；image 從 imageSrc 抓只填非空；不自動補 spellingHint / letterScramble / topic / promptVersion / skillFocus / expectedAnswerType / difficulty 等選填欄位。`.gitignore` 加 `data/imported/approved-questions.preview.generated.json`；`docs/QUESTION_IMPORT_NORMALIZATION_PLAN.md` 升 v4（新增 F-pre-8 段 7 個子段：兩 mode / 5-AND 篩選 / 題型支援表 9 種 / duplicate id 保護兩 mode 行為 / ExamQuestion 轉換規則 / preview output schema / v0.1 不做清單）；`docs/PRACTICE_DATA_IMPORT_PLAN.md` C-6 步從「屬 P3-10-K 範圍、本輪不做」升為「P3-10-K v0.1 已落地」；`docs/PRACTICE_DATA_PLAN.md` P3-10-K 條目從 ⬜ 改 🟡。**CLI 端到端 5 種 regression 全綠**：(1) `--help` exit 0 / (2) `preview vs 既有 PDF skipped case`（totalReviewed=1 / validationPassed=0 / readyToAppend=0 / skipped=1 / target 未動） / (3) `--mode write 缺 --write yes` → exit 2 + 印 3 行說明 / (4) **fixture 4 種情境**（spelling passed / spelling duplicate id q-sp-001 / listening-image-choice unsupported / true-false answer="maybe" validation failed）→ preview mode `readyToAppend=1 / skipped=3 / duplicateIds=1`；fx-001 ready 且轉成正確 ExamQuestion（id=q-sp-imp-001 / source=custom / starterPart=RW3 / image=/images/apple.svg）；fx-002-dup 標 `duplicate_id`；fx-003-unsup 標 `unsupported_question_type` 錯誤；fx-004-valfail 標 `skipped_not_in_validation_passed` / (5) **write happy path vs `/tmp/empty-target.json`**（空 target → 2 題追加：q-sp-imp-001 spelling + q-sp-001 spelling，因 target 為空無 duplicate；reviewer 之後可手動 git diff） / (6) **write + duplicate vs `/tmp/real-copy.json`**（既有 13 題含 q-sp-001 → exit 2 + preview JSON 仍寫 + target 未被改）。**本輪硬邊界全守**：未自動產生題目 / 未呼叫 OpenAI / 未下載 PDF / image / audio / 未解析 PDF / 未讓非 approved_for_practice 進正式題庫 / 未覆蓋既有正式題目 / 未改 UI / quiz / review 流程 / 未改 schema / 未接後端 / DB / 登入 / 未紀錄真實 API key / 真正的 `data/p3-example-questions.json` 整輪未被改動（13 題完整保留）。**未做**：matching 題型 template 擴張 + 寫入支援 / `--rollback` flag / Review UI dashboard / first practice paper 的 paper-level metadata 組裝（sourceMix / sections / sections 內 questionOrder 等屬獨立刀數） / Codex 驗收第一份完整 paper 上線

## P4 Speaking Examiner Agent 模擬考官系統（⬜ 未開始）

> 設計一個「**Speaking Examiner Agent（口說考官代理）**」，扮演 Cambridge Starters 口說考官，帶小朋友走完 Speaking Part 1~4 模擬流程。**不是單次丟一句回答給 AI 批改**，而是 agent-based flow——有固定角色設定、流程狀態、考官台詞、題目順序、回合控制與評分規則。
>
> ⚠️ **重要免責**：
> - AI 口說分數**只是練習回饋**，不是 Cambridge 官方成績
> - **不聲稱能預測官方分數**
> - **不把 AI 評分當成正式證明**
> - UI 必須清楚標示「**AI 練習回饋，非官方考試分數**」
>
> ⚠️ **與既有「不做 AI 評分」邊界的關係**：本檔末尾「目前明確不做」清單的「不做 AI 評分」指**客觀題（multiple-choice / fill-blank / matching 等 answer 在資料中可比對的題）的自動評分**——這條維持。Speaking 屬開放式回答，Agent 提供的是「鼓勵性練習回饋」而非「考試評分」，定位明確不同；UI 與文案處處標示「練習回饋 / 不是官方成績」。

### P4-1 Speaking Part 1~4 流程設計

- ⬜ Part 1：請小孩指出 / 放置物件，或回答圖卡相關問題
- ⬜ Part 2：針對大圖問問題（What's this? Where is the …?）
- ⬜ Part 3：針對小圖卡問問題（一系列短問句）
- ⬜ Part 4：問個人問題（What's your name? How old are you? Do you like …?）
- ⬜ 每 part 流程圖：考官提問 → 小孩錄音 → STT 轉文字 → Agent 判斷 → 進入下一題或追問
- ⬜ 與正式 Cambridge Pre A1 Starters Speaking 結構對齊，但**所有題目自製、不引用官方題目原文**

### P4-2 Examiner Agent 狀態機設計

> Agent 不是無狀態的單次 AI prompt，而是**有狀態機**控制整個 Speaking session：

- ⬜ 狀態機欄位：
  - `currentPart`（1 / 2 / 3 / 4）
  - `currentQuestionIndex`
  - `examinerPrompt`（考官台詞，給 TTS 用）
  - `expectedAnswerType`（yes-no / single-word / short-phrase / personal-info）
  - `childResponse`（音檔 blob / 本機 path）
  - `transcript`（STT 轉出的文字）
  - `feedback`（Agent 給的鼓勵性回饋）
  - `score`（pronunciation / vocabulary / response relevance / confidence 等觀察點，**僅作練習回饋**）
  - `nextAction`（next-question / follow-up / end-part / end-session）
- ⬜ Agent 角色設定：友善 Cambridge Starters 口說考官；以鼓勵為主、不打斷
- ⬜ 流程驅動：依 `nextAction` 決定下一個 turn 是「下一題 / 追問 / 結束 part / 結束 session」
- ⬜ 考官台詞固定模板（Welcome / Now we're going to … / Thank you, well done!）

### P4-3 TTS 考官語音

- ⬜ Agent 的 `examinerPrompt` 透過 TTS 播放（Web Speech API / 雲端 TTS / macOS `say` 任選）
- ⬜ 考官語音風格：友善、清晰、語速適中（小一友善）
- ⬜ **不下載 / 不引用官方考官音檔**
- ⬜ 缺 TTS 時 fallback：螢幕顯示考官台詞文字（仍可進行）

### P4-4 小孩錄音與播放

- ⬜ 瀏覽器 MediaRecorder 取得麥克風錄音
- ⬜ 錄音可在本機回放（在交給 STT 之前小朋友可重錄）
- ⬜ 錄音檔以 blob / 本機路徑保存（**不上傳雲端、不送外部伺服器**）
- ⬜ UI 大按鈕（🎤 開始錄音 / ⏹ 停止 / ▶ 試聽 / 👍 提交）
- ⬜ 缺麥克風 / 拒絕授權時友善提示，可改用打字回答（fallback）

### P4-5 Speech-to-text 轉文字

- ⬜ 將錄音轉文字（Web Speech API SpeechRecognition / 雲端 STT / 本機 whisper.cpp 等任選）
- ⬜ 轉文字結果即為 `transcript`，交給 Agent 判斷
- ⬜ 支援英文（小朋友母語非英文，可能有口音；STT 容錯需務實）
- ⬜ STT 失敗時顯示「沒聽清楚，再說一次？」鼓勵性提示

### P4-6 Agent 追問 / 下一題控制

- ⬜ Agent 依 `transcript` + `expectedAnswerType` 判斷：
  - 答案合理 → 給鼓勵 → 下一題
  - 答案聽不清楚或太短 → 簡單追問（Could you say it again? / Tell me more!）
  - 答案不相關 → 友善 redirect（重複考官提問或換種說法）
- ⬜ 追問次數上限（避免小朋友卡住）：每題最多 2 次追問，否則直接進下一題
- ⬜ 不糾正小朋友細節文法錯誤——以「能溝通」為合格門檻

### P4-7 AI 口說評分器

> 評分定位是**練習回饋**，不是官方考試分數。

- ⬜ 評分維度（觀察點，不是正式評分）：
  - **Pronunciation**：發音清晰度（高 / 中 / 加油）
  - **Vocabulary**：是否使用合適單字
  - **Response relevance**：是否回答到問題
  - **Confidence**：說話流暢度與停頓
- ⬜ 整體鼓勵語：「你很棒！繼續加油」「這次很流暢喔」「下次可以試試說得長一點」
- ⬜ UI 標示：「**AI 練習回饋，非官方考試分數**」**必出現在每個評分畫面**
- ⬜ **不做 Cambridge 等級對應宣稱**（不寫「相當於 Pre A1 / A1 / A2」之類）
- ⬜ 評分結果僅作為**練習建議**

### P4-8 Speaking session 紀錄與家長檢視

- ⬜ 每個 Speaking session 保存於 localStorage（沿用 P3-6-B Session 模式）
- ⬜ 每回合紀錄：`part` / `examinerPrompt` / `ttsScript` / `childAudio`（path 或 blob ref）/ `transcript` / `feedback` / `score`（observations）
- ⬜ 家長檢視頁面：可回放整段 session、看 Agent 回饋、看小朋友 transcript
- ⬜ **不上後端、不上雲端、不做跨裝置同步**

### P4-9 弱點分析與複習建議

- ⬜ 從多次 session 紀錄中匯總常見弱點（哪些 part 容易卡住、哪些單字常聽不懂）
- ⬜ 提出**鼓勵性**複習建議：「最近 What's your name? 答得很好！要不要練練 Where is the …?」
- ⬜ 連結回 `/review` 對應分項練習（單字 / 句型 / 聽力）
- ⬜ **不做能力等級評定，不做進度焦慮提示**

## P5 完整仿真考試體驗（⬜ 未開始）

> 把 P3 的 Listening + Reading & Writing 與 P4 的 Speaking 串起來，給小朋友**接近真考流程**的整套體驗。仍是本機自用、不上雲、不上線。

- ⬜ 完整模擬考流程：Listening → Reading & Writing → Speaking 一條龍
- ⬜ TTS 考官流程貫穿（Listening 也由 TTS 主考；Speaking 由 P4 Examiner Agent 接管）
- ⬜ 成績紀錄：客觀題分數（answer 比對）+ Speaking Agent 回饋（明示為練習回饋，非官方成績）
- ⬜ 家長檢視：完整 session 回顧、各 part 表現、Speaking 錄音回放
- ⬜ 弱點分析：跨多次模擬考的趨勢觀察（鼓勵語氣，**不做能力評級**）
- ⬜ 錯題與口說弱點複習：把「答錯題」+「Speaking 不流暢的回合」匯整成下次練習清單
- ⬜ 仍維持「目前明確不做」：不上雲、不上線、不做帳號、不做雲端同步、不做付費
- ⬜ AI 評分一律標示「**練習回饋，非官方考試分數**」

> 歷史對照：原 P4「題型擴充」內容已併入 P3-4 / P3-5 / P3-1；原 P5「模擬考」內容已併入 P3-6（見變更紀錄 2026-05-07）。本輪起 P4 / P5 重新聚焦 Speaking Examiner Agent 與完整仿真考試體驗，舊條目不再保留歷史佔位章節。

---

## 目前明確不做（不在 Roadmap 範圍）

下列項目**不會出現在任何 P 階段的近期任務**。本專案是本機自用工具，不部署、不上線、不商業化（詳見 `docs/PRODUCT_SPEC.md` 的「產品定位」與「目前明確不做」）。

- 登入 / 註冊 / 會員系統 / 多使用者權限
- 付費 / 訂閱 / 金流 / 方案購買
- 公開部署（Vercel / Netlify / Cloudflare 等）
- App Store / Google Play 上架；行動 App 包裝
- 後端 API / 資料庫 / 雲端同步 / 學習進度上雲
- 後台管理介面
- 自動網路爬蟲抓題（素材一律使用者手動匯入）
- **客觀題的 AI 評分**（multiple-choice / fill-blank / matching 等 answer 在資料中可比對的題型，一律以 `answer` 比對為準，不交給 AI；AI 出題仍規劃中見 P3-3 / P3-8）
- 自動下載官方 PDF / 圖片 / 音檔（即使本機也不下載；連結與人工筆記則整理於 P3-7）

> 例外說明：**P4 Speaking Examiner Agent 提供的「口說練習回饋」不是 AI 評分**——定位是**鼓勵性練習建議**而非考試分數，UI 與文案處處標示「練習回饋 / 不是官方成績」，不對外宣稱能預測 Cambridge 官方分數。客觀題的 AI 評分仍維持不做。
- 多語系（除中英對照以外）
- 完整 SEO / 行銷頁

> 本節是給未來輪次 Claude / Codex / ChatGPT 的硬邊界：**不要把上列項目塞進新增的 P 階段或子階段**。如果某個項目真的需要打開（例如要分享給其他家長使用），請先在 `docs/PRODUCT_SPEC.md` 「目前明確不做」開關狀態，再回到本節。

---

## 變更紀錄

- 2026-05-07：初版建立。
- 2026-05-07：P1 收尾——`lint` / `typecheck` / `build` 全綠；補齊 AI workflow 語言規範與 Codex 驗收前置閱讀；新增 `docs/CODEX_VALIDATION_RUNBOOK.md`；整理 `AGENTS.md` / `CLAUDE.md` 為短指向。
- 2026-05-07：Codex 驗收後文件小修——將 runbook 第 6 節改為驗收導向 9 段格式；README 文件索引補上 runbook 與 `reports/`；P1 補記 `lib/data.ts` helper 已完成、P2 對應條目改寫為「`/review` 串接」精準描述，避免把 P2 功能誤標為完成。
- 2026-05-07：P1 收尾收斂為「✅ 已完成」、housekeeping 改為可選分區；P2 進入「🟡 進行中」，完成 P2-1 單字複習頁第一版（分類切換、單字卡、圖片與音檔 fallback）；`docs/PRODUCT_SPEC.md` 補入「國小低年級使用者設計原則」。
- 2026-05-07：P2 主流程重構為 A~Z 字母入口 → 字母總覽 → 單字詳情；新增 `lib/vocabularyNavigation.ts`、`/review/letter/[letter]`、`/review/word/[id]`；補 4 筆 sample 單字（ant / book / boy / cow）以驗證跨字母 next；P2 章節拆為 P2-1 ~ P2-4，本輪完成 P2-2、P2-3；`docs/PRODUCT_SPEC.md` 補入「單字複習主流程」一節，category tab 改定位為未來補充模式。
- 2026-05-07：補 P3「考前練習與題庫」規劃——重寫 P3 章節為 6 個子階段（P3-1 考題資料 schema 擴充、P3-2 本機資料匯入流程、P3-3 AI 仿真題生成規劃、P3-4 Listening 題型第一版、P3-5 Reading & Writing 題型第一版、P3-6 模擬考與錯題複習）；原 P4 / P5 內容已併入新版 P3，章節標題改為「已併入 P3-x」並保留作為歷史紀錄；`docs/PRODUCT_SPEC.md` 新增「複習內容擴充方向」與「測驗與考前練習方向」兩節，並把 AI 出題從「暫不做」清單移出（AI 評分仍維持暫不做）。本輪 P3 全部子項皆維持 ⬜，未開工；P2 仍維持 🟡 進行中。
- 2026-05-07：清理產品範圍——`docs/PRODUCT_SPEC.md` 重寫「產品目標」為「產品定位」，明示本專案為本機自用工具、不部署、不商業化、考完後可能不再長期維護；把「暫不做的功能」改名為「目前明確不做」並依「帳號 / 部署 / 雲端 / AI / 其他」分組擴充清單（含登入、付費、雲端同步、公開部署、App 上架、多使用者權限、後台管理、商業化營運等）。本檔 ROADMAP 末尾新增「目前明確不做（不在 Roadmap 範圍）」一節，給未來輪次硬邊界；P5 末條歷史條目「之後再考慮後端」改為「不上後端 / 雲端」對齊 P3-6。本輪零程式碼變動；P2 仍 🟡，P3 仍 ⬜ 規劃中。
- 2026-05-07：補完整考卷 Session 規劃——`docs/PRODUCT_SPEC.md`「測驗與考前練習方向」新增「完整考卷 Session」一節，含 4 個子小節（Session 概念、作答進度保存、考卷操作、交卷與結果頁），明確第一版不計時、保存以 localStorage 為主（含 8 個欄位）、操作含「繼續作答 / 離開這份考卷 / 重新測驗 / 直接交卷」、結果頁含分數 / 紅色錯題 / 正確答案 / 簡單講解 / 再練習錯題入口；`PROJECT_ROADMAP.md` 將 P3-6 標題從「模擬考與錯題複習」改為「**完整考卷 Session、交卷與錯題複習**」並重寫條目（依完整考卷生成 / 作答進度保存 / 考卷操作 / 交卷與結果頁 / 計時相關 5 組分區）；P5 章節對 P3-6 的指向敘述同步對齊新名字。本輪零程式碼變動，未引入登入 / 後端 / 資料庫 / 雲端同步；P3 全部子項仍 ⬜ 規劃中，P2 仍 🟡 進行中。
- 2026-05-07：完成 P2-4A——`data/vocabulary.json` 從 12 筆擴充至 54 筆（覆蓋 17 字母 / 11 分類，依字典序排列）；`components/VocabularyCard.tsx` 新增 `revealMode` prop（預設 false 保留 P2-1 行為），詳情頁啟用後初始隱藏 translation / exampleEn / exampleZh，由「🔍 看答案」/「🙈 再想一次」雙態切換；切換上一個 / 下一個時靠 `key={current.id}` remount 自動回到收起狀態；`app/review/word/[id]/page.tsx` 對 `<VocabularyCard>` 傳入 `revealMode`。P2-4 章節拆為 P2-4A（已完成）與 P2-4B（真實素材、補齊更多單字、category 補充模式，尚未開始），P2 整體仍 🟡 進行中。
- 2026-05-07：完成 P2-4B——`/review` 從 A~Z 字母網格改為**複習中心首頁**，含 6 個入口卡（單字複習、看圖練習、聽力練習、句型練習、位置 / 顏色 / 數量、考題練習）；新增 `components/ReviewHubCard.tsx` 支援 ready / coming-soon 兩態（後者卡片淡化、不可點、顯示「準備中」徽章）；A~Z 字母網格搬到 `/review/words`，`/review/letter/[letter]` 返回 link 從「回字母選擇」改為「回單字複習」（指向 `/review/words`）；單字複習與考題練習為 ready，其餘四個入口為 coming-soon。文件同步：`README.md` 新增「在平板上使用（同 Wi-Fi 區網）」章節說明 Mac dev server + 平板區網 IP + iPad Safari 加入主畫面 + Vercel 僅作未來可選方案，並修正「下一步」（翻牌互動已於 P2-4A 完成、複習中心首頁已於 P2-4B 完成，故移出待辦）；`docs/PRODUCT_SPEC.md` 在「產品定位」補「第一階段使用方式：Mac 本機 + 平板同 Wi-Fi」、把「主要功能（願景）」的複習區改寫為複習中心 6 入口、「單字複習主流程」進入點改為 `/review/words`。原 P2-4B「真實素材」內容遷移到新 P2-4C 並合併本輪「準備中」入口的未來實作。本輪未動 `/quiz`、未引入登入 / 後端 / 資料庫 / 雲端同步、未真的部署 Vercel。P2 仍 🟡 進行中（P2-4C 尚未開工），P3 仍 ⬜ 規劃中。
- 2026-05-07：P2-4B 小修——`/review` 複習中心**移除「考題練習」主入口卡**（原 6 卡 → 5 卡），改為頁面底部小型輔助提示「想做完整考卷？請到測驗區」連到 `/quiz`，視覺層級低於 5 張主卡；明確 `/review`（分項能力練習）與 `/quiz`（完整考卷、歷屆 / sample 題、AI 仿真題、交卷評分）的分工。`docs/PRODUCT_SPEC.md` 主要功能段改寫為「`/review` 5 入口 + 底部輔助提示」並修正「單字複習主流程」第 2 步空字母頁返回路徑為 `/review/words`（修 Codex 指出的文件同步問題）；`PROJECT_ROADMAP.md` P2-4B 條目從 6 主入口改為 5 主入口 + 1 底部提示，P2-2 條目補一句「後續已於 P2-4B 將 A~Z 字母入口搬到 `/review/words`」註記。本輪未動其他路由、未動 `/quiz`、未進入 P2-4C 或 P3。
- 2026-05-07：完成 P2-4C-1——新增 `/review/picture` 看圖練習頁第一版（題型：看圖選英文 4 選 1）。新增 `components/PicturePractice.tsx`（client 元件）含 `PicturePractice` parent（管 `index`）+ 私有 `PictureQuestion`（用 `key={index}` remount 隔離 `selectedSlot` / `imageStatus`，避開 React 19 `react-hooks/set-state-in-effect`）。Deterministic 選項生成：正確答案 + 後 3 個 vocabulary 循環取、正確答案位置 = `index % 4`，避免 hydration mismatch；圖片預載沿用 P2-1 修補後機制（client `new window.Image()` 預載成功才切真圖）；缺圖 fallback 顯示首字母 + 「圖片準備中」與 `VocabularyCard` 一致；答對 / 答錯即時回饋（鼓勵語氣，答錯顯示正確答案）；「下一題」按鈕在未答題時 disabled，全部題庫循環無 crash。`/review` 看圖練習卡從 coming-soon 改為 ready 連 `/review/picture`。文件同步：P2-4C 拆出 P2-4C-1（已完成）+ P2-4C-2（未開始，含真實素材、聽力、句型、位置 / 顏色 / 數量、看圖第二題型）；`README.md` 目前功能補 `/review/picture` 條目；`docs/PRODUCT_SPEC.md` 主要功能（願景）的看圖練習從「規劃中」改為「第一版已實作（4 選 1 看圖選英文）」。本輪未動 `/quiz`、未引入登入 / 後端 / 雲端、未新增 localStorage / 分數保存 / 真實素材 / 依賴 / 測試框架。P2 仍 🟡 進行中（P2-4C 仍進行中），P3 仍 ⬜ 規劃中。
- 2026-05-07：完成 P2-4C-2A——`/review/picture` 新增題型切換 tab + 第二題型「看字選圖」。`components/PicturePractice.tsx` 重構：parent 加 `mode` state（`"image-to-word"` / `"word-to-image"`），新增 `ModeSwitch` tab 元件、把舊 `PictureQuestion` 改名為 `ImageToWordQuestion` 並新增 `WordToImageQuestion`；抽出 file-private `PracticeImage`（看圖選字大圖與看字選圖選項小圖共用，size lg / sm 由 prop 控制）與 `NextButton`（兩題型共用）。`buildOptions` deterministic helper 沿用 P2-4C-1 同一個。看字選圖題目區用 `bg-sky-50` 區別於看圖選字的 `bg-amber-50` 圖片區，視覺提示題型方向不同；4 個圖片選項用 `grid-cols-2`，缺圖 fallback 顯示首字母（size sm），答對 emerald / 答錯 rose / 其他淡化 + opacity-60 視覺一致。模式切換時自動 reset `index = 0` 與 `selectedSlot = null`（依靠子元件 `key={index}` remount + parent setState 雙重保證）。文件同步：ROADMAP 把舊 P2-4C-2 內「看圖第二題型」條目升級為已完成的 P2-4C-2A 子分區（共 8 條 ✅），舊 7 條未開始項目移到新 P2-4C-2B；`README.md` 目前功能段更新 `/review/picture` 描述為「兩種題型」+ tab 切換 + 不做分數保存的硬邊界；`docs/PRODUCT_SPEC.md` 主要功能（願景）的看圖練習從「第一版已實作（4 選 1 看圖選英文）」更新為「兩個方向都已實作（看圖選英文 + 看英文選圖）」。本輪未動 `/quiz`、未動 `/review` 卡片（description 上輪已寫「看圖選英文、看英文選圖」覆蓋兩題型）、未引入登入 / 後端 / 雲端 / localStorage / 分數保存 / 真實素材 / 依賴 / 測試框架。P2 仍 🟡 進行中（P2-4C-2B 尚未開工），P3 仍 ⬜ 規劃中。
- 2026-05-07：完成 P2-4C-2B-1——`/review/picture` 第一批真實圖片素材接入。新增 10 個自製 SVG（`public/images/{apple,cat,dog,book,red,blue,one,two,mother,father}.svg`），統一 `viewBox="0 0 200 200"` + 淡黃背景；color 類（red / blue）刻意只用色塊不放單字文字以免洩漏看圖選字答案；其他 8 個用簡單形狀組合（蘋果 = 紅圓+綠葉+棕梗、book = 攤開的書 + 文字行模擬、one/two = 1 / 2 顆星星、mother/father = 簡單頭像）。`data/vocabulary.json` 中 10 筆對應 item 的 `image` 從 `.png` 改為 `.svg`，其餘 44 筆 placeholder path 維持以續測 fallback。`<PracticeImage>` / `<VocabularyCard>` 圖片預載 + fallback 邏輯**完全未動**——`<img>` 與 `new window.Image()` 對 SVG 原生支援，行為與 PNG 一致。文件同步：ROADMAP 把舊 P2-4C-2B 拆為 P2-4C-2B-1（已完成 5 條）+ P2-4C-2B-2（未開始 7 條，將「補真實圖片」條目改寫為「往剩下 44 個未補圖單字推進」）；`README.md` 目前功能段補一段「第一批 10 個自製 SVG 已接入；其餘單字仍 fallback」、「下一步」改為 P2-4C-2B-2 並把第一批圖片素材移出待辦；`docs/PRODUCT_SPEC.md`「測驗與考前練習方向」素材策略段補入「自製簡單 SVG 圖像素材作為第一階段策略」；`docs/DATA_SCHEMA.md` 補一句 `image` 可指向 svg / png 任一靜態資源。本輪未動 `/quiz`、未動所有 components / helpers、未引入登入 / 後端 / 雲端 / localStorage / 真實音檔 / 新單字 / 依賴 / 測試框架，未下載任何官方 / 歷屆 / 網路圖片。P2 仍 🟡 進行中（P2-4C-2B-2 尚未開工），P3 仍 ⬜ 規劃中。
- 2026-05-08：完成 P3-1——P3 階段從「⬜ 規劃中」進入「🟡 進行中」，但**僅 P3-1 完成、P3-2 ~ P3-6 全部仍 ⬜**。本輪只做資料 schema / 型別 / 範例資料 / 文件，**不做** UI / localStorage 實際讀寫 / AI 出題工具 / 匯入工具。`lib/types.ts` 新增 P3 完整型別與 P1~P2 既有型別並存：`QuestionSource`（4 種來源）、`QuestionType`(6 種題型)、`BaseQuestion` 共用欄位、6 個題型專屬型別（`ExamMultipleChoiceQuestion` / `PictureChoiceQuestion` / `WordChoiceQuestion` / `ListeningChoiceQuestion` / `FillBlankQuestion` / `MatchingQuestion`）、`ExamQuestion` discriminated union、`ImageOption` / `MatchingPair` 配套、`ExamPaper` / `ExamSection` / `SourceMix`、`ExamSessionState` / `ExamAnswerMap` localStorage 狀態（含 `schemaVersion = 1` 給未來 migration）。`docs/DATA_SCHEMA.md` 把舊「未來題型擴充方向」整段改寫為「P3 考前練習：題庫 / 考卷 / Session schema」，含 6 題型範例 jsonc + ExamPaper + Session 完整欄位表 + localStorage key 命名提案 + 「不在 P3-1 範圍」清單。新增 `data/p3-example-questions.json`（7 題涵蓋 6 題型）與 `data/exam-papers.example.json`（1 份範例考卷），不被 `lib/data.ts` import，純 schema 演示供 Codex 驗收與 P3-2 / P3-3 開工參考。**舊 schema 完全未動**：`MultipleChoiceQuestion` / `QuizQuestion` / `Quiz` / `data/quizzes.json` 一行未改，舊新並存。本輪未動 `/quiz`、未動所有 components / pages / `lib/data.ts`、未實作 localStorage / 交卷 / 錯題複習 / AI 出題 / 匯入工具，未引入新依賴 / 測試框架。P2 仍 🟡 進行中（P2-4C-2B-2 尚未開工），P3 為 🟡 進行中（僅 P3-1 ✅）。
- 2026-05-08：完成 P3-2-A 本機素材匯入流程（規劃 / 文件層）——P3-2 階段升 🟡 進行中，子階段拆 P3-2-A（已完成）+ P3-2-B（自動化工具，未開始）。新增 `source_materials/` 資料夾骨架：`samples/` / `past_papers/` / `ai_generated/` / `custom/` 四個子目錄各對應一個 `QuestionSource`，全部含 `.gitkeep`。新增 `source_materials/.gitignore` 排除原始 PDF / 圖片（jpg / jpeg / png / gif / webp / bmp / tiff）/ 音檔（mp3 / wav / m4a / ogg / aac / flac）/ 影片 / 壓縮檔等二進位，保留純文字（.md / .txt / .csv / .json / .gitkeep）；雖然本專案不公開 repo，但仍預留版權保險。撰寫 `source_materials/README.md` 主文件：用途定位（不是正式公開資料庫，只是本機整理區）、資料夾與 source 對應表、可放 / 禁止做（含「不爬蟲、不下載官方圖片、不放外部 URL」三條硬邊界）、git 政策、建議流程 5 步、命名規範、與 P3-1 schema 關係、不在 P3-2-A 範圍。新增 `source_materials/custom/example-question-draft.md`：欄位對照表 + 7 個自製範例覆蓋 6 題型（multiple-choice / picture-choice / word-choice / listening-choice / fill-blank 兩版 / matching）、明示「非官方真題」、命名建議含題型前綴對照表、解析語氣鼓勵 > 懲罰提醒。`docs/DATA_SCHEMA.md` 補「本機素材匯入流程（P3-2-A）」一節：三層資料分工表（整理區 / 正式題庫 / 靜態素材）、`source` ↔ 整理區子目錄對應表、工作流程概念圖、P3-2-A vs P3-2-B 範圍切分、嚴禁清單。`README.md`「下一步」第 2 條更新明示 P3-2-A 已完成、P3-2-B 與 P3-3 ~ P3-6 仍未開始。本輪**未寫任何自動化程式 / CLI**、未實作 `/quiz` UI、未做 localStorage 實際保存、未做交卷 / 錯題頁、未做 AI 出題工具、未爬網路、未下載任何官方圖片或音檔、未放 Cambridge 官方真題內容、未新增 SVG / 依賴 / 測試框架、未動 PRODUCT_SPEC / TASK_ROUTER / CODEX_VALIDATION_RUNBOOK / AI_DEV_WORKFLOW / AGENTS / CLAUDE 任一文件。順手修 `reports/claude_last_report.md` 中 P3-1 那輪「4 種 source 都有出現」的過時敘述，改為「schema 支援 4 種；範例使用 custom / ai_generated」。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 ✅、P3-2-A ✅、P3-2-B / P3-3 / P3-4 / P3-5 / P3-6 全 ⬜）。
- 2026-05-08：完成 P3-3-A AI 仿真題 Prompt 標準格式——P3-3 階段升 🟡 進行中，子階段拆 P3-3-A（已完成）+ P3-3-B（實際 AI 工具串接，未開始）。新增 `docs/AI_QUESTION_GENERATION.md` 規格文件 10 個 section：定位（不是官方真題、是依風格自製）、難度原則（小一友善、單句短、選項清楚、不刁鑽、不冷僻、不挫折）、來源規則硬邊界（必標 `ai_generated`、不假裝 official_sample / past_paper、不引用官方 / 歷屆題、不放外部 URL）、6 種題型範圍、草稿輸出格式（類 YAML）、轉換流程圖、品質檢查 6 項、與 P3-1 / P3-2-A 對齊、不在 P3-3 範圍、Prompt 版本化政策。新增 `source_materials/ai_generated/prompt-template.md`（v1）：可直接複製給 AI（ChatGPT / Claude）使用的 prompt 範本，含角色設定（Cambridge Starters 兒童英文出題助手）、年齡設定（國小一年級）、6 題型可選參數、類 YAML 草稿格式範例（含 word-choice / matching 巢狀結構）、8 項自我檢查、與 custom 草稿差異對照表、版本歷史。新增 `source_materials/ai_generated/example-ai-questions.md`：6 題自製 AI 草稿覆蓋 6 種題型（`q-ai-mc-001` / `q-ai-pc-001` / `q-ai-wc-001` / `q-ai-lc-001` / `q-ai-fb-001` / `q-ai-mt-001`），id 與既有 P3-1 範例（`data/p3-example-questions.json`）不撞名；附 `imagePrompt` / `ttsScript` / `promptVersion` 草稿欄位、自我檢查紀錄、整理者提醒。`docs/DATA_SCHEMA.md` 補「AI 仿真題草稿與正式題庫的關係（P3-3）」一節：草稿 → 審核 → 正式題庫流程圖、草稿欄位 vs 正式 schema 對應表（`imagePrompt` / `ttsScript` / `notes` 哪些保留 / 不保留）、`source` 強制 `ai_generated`、P3-3-A 範圍 vs P3-3-B 範圍 vs 永久不做（AI 評分）。`README.md`「下一步」第 2 條補一行 P3-3-A 已完成。本輪**未寫任何 AI 工具 / CLI**、未串 OpenAI / Anthropic API、未實作 `/quiz` UI、未做 localStorage / 交卷 / 錯題頁、未爬網路、未下載任何官方圖片 / 音檔、未放 Cambridge 官方真題內容、未新增 SVG / 依賴 / 測試框架、未動 PRODUCT_SPEC / TASK_ROUTER / CODEX_VALIDATION_RUNBOOK / AI_DEV_WORKFLOW / AGENTS / CLAUDE / `lib/types.ts` / 任何 `data/*.json`。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 ✅、P3-2-A ✅、P3-3-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6 全 ⬜）。

- 2026-05-08：P3-3-A 收尾——新增 prompt v1 第一批人工試跑草稿 `source_materials/ai_generated/2026-05-08-starters-v1-batch01.md`，8 題覆蓋 6 題型（mc × 2 / pc × 1 / wc × 1 / lc × 2 / fb × 1 / mt × 1），id 用 `q-ai-v1-*-001/002` 格式與既有 `data/p3-example-questions.json` / `example-ai-questions.md` 皆不撞名。題目全部標 `source: ai_generated` + `promptVersion: starters-v1`；題幹用 vocabulary 既有字（apple / cat / dog / book / red / blue / mother / father / chair / run / jump / sit）；color 類圖片以 `imagePrompt` 強調「無文字、無單字」對齊 PRODUCT_SPEC「素材策略」。檔末附**人工品質檢查紀錄表**（10 項全 ✓：source / promptVersion / answer 在 options / 無官方歷屆題內容 / 無外部 URL / 鼓勵語氣 / 題幹 ≤ 10 字 / 避冷僻字 / id 不撞名 / 題型分配對齊任務單），並逐題分析「需要日後調整」（綠 / 黃色塊 SVG 與 lc 音檔依賴 P2-4C-2B-2，補齊前不能進正式 JSON）。`README.md`「下一步」第 2 條補一行說明 prompt v1 試跑草稿已新增。本輪**未串 AI API、未寫任何程式 / CLI、未做自動轉換工具、未實作 `/quiz` UI**、未動 `lib/types.ts` / 任何 `data/*.json` / 所有 components / pages / `lib/`、未新增圖片 / 音檔 / SVG / 依賴 / 測試框架，未爬網路、未下載任何官方 / 歷屆 / 網路素材、未放 Cambridge 官方真題內容。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 ✅、P3-2-A ✅、P3-3-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6 全 ⬜）。
- 2026-05-08：完成 P3-6-A `/quiz` 最小可玩流程第一版——P3-6 階段升 🟡 進行中，子分區拆 P3-6-A（已完成）+ P3-6-B（持久化 / 操作 / 結果頁完整版，未開始）。`/quiz` 從骨架升級為可實際操作的測驗頁：server page (`app/quiz/page.tsx`) 載入 `data/exam-papers.example.json` 第一份考卷與 `data/p3-example-questions.json` 7 題，依 sections 順序平鋪 questionOrder → 傳給新增的 client 元件 `<QuizPlay>` (`components/QuizPlay.tsx`) 管 state（純 React local，無 localStorage）。6 題型最小渲染：multiple-choice / picture-choice / word-choice 用 amber 系按鈕；listening-choice 顯示 transcript / ttsScript 文字（不播音檔，自製屬 P2-4C-2B-2）；fill-blank 選項版用按鈕 + 自由填空版用 text input（比對忽略大小寫與前後空白）；matching 用閱讀型「我看完了」按鈕作答（計分視為「答完即正確」）。一題一頁：未答題「下一題」disabled、最後一題顯示「看結果」、完成畫面顯示「答對 N/M 題」+ 四級鼓勵文案（全對 / ≥70% / ≥40% / 其他）+ 🔁「重新開始」按鈕 + 回首頁 link。圖片缺檔 fallback：file-private `<QuizImage>`（與 `<PracticeImage>` 同模式但獨立，避免動 P2-4C 既有元件），`new window.Image()` 預載成功才切真圖。`lib/data.ts` 最小擴充：新增 `p3ExampleQuestions` / `p3ExamplePapers` export，舊 `vocabulary` / `quizzes` 一行不動。原 P3-6 5 分區的計時段「第一版不計時」翻 ✅（對齊本輪實作）；其餘 4 分區（完整考卷生成 / 作答進度保存 / 考卷操作 / 交卷與結果頁）保留為 P3-6-B 目標。`README.md` 目前功能補 `/quiz` 條目 + 「下一步」P3-6-A 已完成、P3-6-B 仍規劃中。本輪**零依賴新增**、未做 localStorage / 交卷頁 / 錯題詳解 / AI 評分、未串 API、未動 `lib/types.ts` / `data/*.json` / 任何 `/review` 路由 / 任何 components 既有檔（含 `PicturePractice`、`VocabularyCard`）、未爬網路、未下載任何官方 / 歷屆 / 網路素材、未放 Cambridge 官方真題內容、未新增圖片 / 音檔 / SVG。Codex 暫停期（5/12 恢復）期間由 Claude 自測通過：lint / typecheck / build 三項全綠 + dev smoke test 8 條路由 200 + `/quiz` SSR 結構驗證（標題 / 進度 / 第一題 listening 內容 / 4 選項 / disabled 按鈕 / 完成畫面字串初始 0）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 ✅、P3-2-A ✅、P3-3-A ✅、P3-6-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B 全 ⬜）。
- 2026-05-08：P3-6-A 小修——調整 `/quiz` 題型順序更接近正式 Cambridge Starters 考卷結構。`app/quiz/page.tsx` 新增 file-private `sortQuestionsForStarters()` helper，把 P3-1 範例 7 題依「Section 1 Listening（`listening-choice`）→ Section 2 Reading & Writing（`picture-choice` → `word-choice` → `multiple-choice` → `fill-blank` → `matching`）」重新排序；不修改 `data/p3-example-questions.json` 與 `data/exam-papers.example.json`。`components/QuizPlay.tsx` 新增 `SECTION_LABELS` 對照與 `getSectionTag()` helper；題目卡頂端加區段徽章（sky-100 / amber-100 兩色配色區別 listening 與 R&W 段）+「聽力練習」/「閱讀與書寫練習」中文小字 + 既有「第 X 題 / 共 N 題」進度行。`app/quiz/page.tsx` 頁首文案補「順序：Listening → Reading & Writing」。**Speaking 不做**（本專案明確排除）。其餘流程（選答案、下一題、完成畫面、答對 N/M、重新開始、fill-blank normalize、matching 閱讀型「我看完了」、listening 顯示 transcript 不播音檔）完全保留。本輪零依賴新增，未動 `lib/types.ts` / `lib/data.ts` / 任何 `data/*.json` / 任何 `/review/` 路由 / 其他 components。Codex 暫停期由 Claude 自測通過：lint / typecheck / build 三項全綠 + dev smoke test 8 條路由 200 + visible HTML 第 1 題為 listening（含「Listening」徽章 + 「聽力練習」中文 + transcript）+ RSC payload 內 7 題位置遞增順序符合 listening → picture → multiple → fill-blank 排序。P3-6-A 增加一條 ✅；P3-6-B 仍 ⬜，P3 整體仍 🟡 進行中。
- 2026-05-10：P3-9-C RW1 yes/no 補 No answer 樣本——新增 1 題 `q-tf-002`（cat.svg 重用 + `It is a dog.` + answer `"no"` + `explanation: "圖片是貓，不是狗，所以這句話不對，答 No。"` + `starterPart: "RW1"` 完整 metadata），讓 RW1 yes/no 分布從上輪「1 yes / 0 no」改為「**1 yes (q-tf-001) + 1 no (q-tf-002) 平衡 50 / 50**」，避免孩子只習慣選 Yes。`data/exam-papers.example.json` questionIds 加 q-tf-002（緊接 q-tf-001 之後維持 RW1 連續分區）+ `sourceMix.ai_generated` 從 4 升 5；description 不變（「看圖、選字、填空、連連看、yes / no 判斷」已涵蓋）。`README.md` 「7 題改 8 題」改為「8 題改 9 題」+ 加 q-tf-002 描述對齊任務單範例（answer = no、cat.svg + 「It is a dog.」、補 No answer 樣本讓孩子不只練 Yes）。`PROJECT_ROADMAP.md` P3-9-C 加 1 條 ✅（RW1 yes/no 已補 No answer 樣本）；上輪「未來正式 RW1 / RW2 圖片場景題細化」⬜ 改寫為「未來補更多 RW1 題目與更精準官方格式」（多題 RW1 yes/no 多元主題 + RW2 共用 scene image + 真正 ✓ / ✗ 手寫互動）；P3-9-C 從 18 條 ✅ 升為 19 條 ✅。**未動** UI 邏輯（`<TrueFalseView>` / `<YesNoButton>` / `formatYesNoDisplay()` / `formatUserAnswer` / `formatCorrectAnswer` 全保留）/ `lib/types.ts`（discriminated union 不動）/ `lib/data.ts` / `lib/examSessionStorage.ts` / `app/quiz/page.tsx`（RW_TYPE_ORDER 不變）/ 任何 `app/review/*` / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md`（兩道 RW1 樣本仍對齊「P3-9-C 第三刀已實作」狀態，無需更新）/ `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `docs/TTS_AUDIO_WORKFLOW.md` / `docs/DATA_SCHEMA.md`（true-false schema 已記錄足夠）/ `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / `.env.example` / `.gitignore` / `package.json` / `scripts/*` / 既有 audio 檔案 / 既有 SVG 圖片（cat.svg 重用、未產生新圖）/ 依賴。**本輪未做** OpenAI TTS / 呼叫 OpenAI API / 重產音檔 / 新增 audio / 下載官方素材 / 使用官方題目 / 新增圖片 / 新增依賴 / 改 Listening 行為 / Speaking Agent / 錄音 / STT / 部署 / 後端 / DB / 登入 / 處理 npm audit / commit API key / commit `.env` / `.env.local`。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + RSC payload 含 `q-tf-002` 完整 metadata（id / type=true-false / answer=no / prompt="It is a dog." / image=cat.svg）+ Listening 第 1 題仍用 OpenAI v2 audioSrc + transcript 仍隱藏（三段提示在、`What does the boy want?` 只在 RSC payload 出現 1 次）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。題目總數從 8 題升 9 題（exam paper 含 1 題 listening + 8 題 R&W：q-mc-001 / q-pc-001 / q-tf-001 / q-tf-002 / q-wc-001 / q-fb-001 / q-fb-002 / q-mt-001）。P2 仍 🟡（P2-4C-2B-2 部分 🟡），P3 仍 🟡（P3-9-C 從 18 條 ✅ 升為 19 條 ✅）。
- 2026-05-10：完成 P3-9-C 第三刀 RW1 yes / no 判斷題 UI 第一版——`/quiz` 新增 `true-false` 題型對齊正式 RW1（圖 + 描述句 + Yes / No 二選一）。**只動最小 schema + 1 題範例**：`lib/types.ts` `QuestionType` union 加 `"true-false"`，新增 `TrueFalseQuestion` type（`image` / `prompt` / `answer: "yes" | "no"` 必填，含完整 docstring 說明與 picture-choice 差異）；`ExamQuestion` discriminated union 加進。`data/p3-example-questions.json` 新增 1 題 `q-tf-001`（`source: "ai_generated"` / `image: "/images/cat.svg"` 自製既有 SVG / `prompt: "It is a cat."` / `answer: "yes"` / `explanation: "圖片是貓，所以這句話是對的，答 Yes。"` / `starterPart: "RW1"` 完整 metadata）；`data/exam-papers.example.json` reading-writing section.questionIds 加 `q-tf-001`（位置在 q-pc-001 之後對齊 RW1 順序）+ `sourceMix.ai_generated` 從 3 升 4。`app/quiz/page.tsx` `RW_TYPE_ORDER` 加 `"true-false": 2`（picture-choice 之後、word-choice 之前；其他 RW 題型整體 +1）；TS Record 完整覆蓋避免排序 NaN。`components/QuizPlay.tsx` 改動：(a) imports 加 `TrueFalseQuestion`；(b) `getStarterPartInfo()` 加 (starterPart=RW1, type=true-false) 細分覆寫顯示「Part 1：看圖判斷 yes / no」（比 default `STARTER_PART_DISPLAY[RW1]` 的「看圖判斷 / 看圖選答案」更精準）+ fallback switch 加 case "true-false"；(c) `QuestionView` switch 加 case "true-false" 派發 `<TrueFalseView>`；(d) 新增 `<TrueFalseView>` 元件——大圖 + `<PromptText>` 描述句 + 「這句話對嗎？」副提示 + 兩個大按鈕（Yes emerald / No rose，role="radiogroup" + radio role）；(e) 新增 `<YesNoButton>` helper（min-h-24/28 大按鈕、emerald-300 selected ring-2 emerald-500 / rose-300 selected ring-2 rose-500、unselected white + ring-1 + hover -translate-y-0.5、4xl/5xl emoji + 2xl/3xl label、role="radio" + aria-checked + aria-label 完整 a11y）；(f) `formatUserAnswer` / `formatCorrectAnswer` 對 true-false 透過新 `formatYesNoDisplay()` helper 顯示「Yes ✓」/「No ✗」（給結果頁詳解 + retry result detail 用，UI 詳解卡片自動同步）；(g) `getQuestionPromptDisplay()` 對 true-false 走 default 路徑回 `question.prompt`（描述句作為訂正用題目文字）；`isCorrect()` 邏輯**完全不需動**——直接 `answer === question.answer` 比對 "yes" / "no" 字串。`docs/DATA_SCHEMA.md` 6 題型表加 true-false 一列 + 新增 true-false 詳細 schema 說明段（範例 jsonc + 5 條要點 + 與 picture-choice 並存差異說明）。`docs/STARTERS_PART_TEMPLATES.md` 升 v2.2：RW1 模板段升級為「P3-9-C 第三刀已實作 true-false 題型」狀態（含完整本專案練習版目標 / 互動方式 4 步 / 資料欄位 / 目前 schema 完整支援 ✅ / 未來需要補哪些功能 2 條）；schema 對應表第一層加 `true-false → RW1` 🟢 一筆（最直接對齊）；picture-choice 仍保留作為 RW1 / RW2 preview 第二層。`README.md` `/quiz` 條目把 q-pc-001 → RW1 mapping 描述改為「q-pc-001 → RW1 看圖判斷 / 看圖選答案 preview」+ 新增「**q-tf-001 → RW1 看圖判斷 yes / no（P3-9-C 第三刀新增 `true-false` 題型）**」+ 整體題目數從 7 題改為 8 題；其他敘述不動。`PROJECT_ROADMAP.md` P3-9-C 「RW1 大型 ✓ / ✗ 按鈕」⬜ 改 ✅（RW1 yes / no 判斷題 UI 第一版）+ 新增 2 條 ⬜（未來正式 RW1 / RW2 圖片場景題細化 + 未來更精準對齊官方 part format）；P3-9-C 從 17 條 ✅ 升為 18 條 ✅。**未動** Listening 行為（仍用 OpenAI v2 examiner voice / 仍隱藏 transcript）/ `lib/data.ts` / `lib/examSessionStorage.ts` / 任何 `app/review/*` / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `docs/TTS_AUDIO_WORKFLOW.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / `.env.example` / `.gitignore` / `package.json` / `scripts/*` / 既有 audio 檔案 / 既有 SVG 圖片（cat.svg 重用） / 依賴。**本輪未做** OpenAI TTS / 呼叫 OpenAI API / 重產音檔 / 新增 audio / 新增大量題目 / 下載官方素材 / 使用官方題目 / 新增依賴 / Speaking Agent / 錄音 / STT / 後端 / DB / 登入 / 部署 / 處理 npm audit / commit API key / commit `.env` / `.env.local`。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ TypeScript 完整 narrow（discriminated union exhaustive switch 全綠）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening 仍用 OpenAI v2 audioSrc + transcript 仍隱藏 + RSC payload 含 8 題完整資料含新 q-tf-001（answer "yes" / starterPart "RW1" / image cat.svg）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。P2 仍 🟡（P2-4C-2B-2 部分 🟡），P3 仍 🟡（P3-9-C 從 17 條 ✅ 升為 18 條 ✅）。
- 2026-05-10：完成 OpenAI TTS examiner voice v2 切換為 q-lc-001 正式音檔——使用者實聽 v2 回饋「v2 版自然很多，先採用」即正式切換。`data/p3-example-questions.json` `q-lc-001.audioSrc` 從 `/audio/starters/l3/q-lc-001.m4a` 改為 `/audio/starters/l3/q-lc-001-openai-v2.mp3`；不改其他欄位（transcript / ttsScript / options / answer / metadata 全保留）。**保留三版音檔並存**：v2（33792 bytes / MPEG layer III / 128 kbps / 24 kHz / 2.112 秒，**目前正式**）+ v1 `q-lc-001-openai.mp3`（64896 bytes，比較紀錄）+ macOS `say` `q-lc-001.m4a`（12008 bytes，fallback / 歷史比較）。`docs/TTS_AUDIO_WORKFLOW.md` 升 **v2.2**：在「實聽調整紀錄」表 v2 列「使用者實聽回饋」欄填入「v2 版自然很多，先採用」+ 切換日期；新增「**目前 q-lc-001 採用版本**」對照表（v2 ✅ 正式 / v1 + macOS say 保留為比較與 fallback）+ 三版並存策略說明（v1 為「v1 instructions 太強調 slowly」證據 / macOS say 為「不串雲端 API 也能跑」fallback / 三版並存讓 v3+ 試產有對照組）+ AI-generated 提醒。`README.md` `/quiz` 條目改寫為「目前使用 OpenAI TTS v2 examiner voice」+ v1 / v2 實聽歷程 + v2 是 AI-generated 提醒 + macOS say + v1 保留作 fallback / 比較。`PROJECT_ROADMAP.md` P3-9-C 「使用者實聽比較」🟡 改 ✅（v1 / v2 兩版實聽都記）+ 新增 1 條 ✅（audioSrc 切換到 v2）；P3-9-C 從 15 條 ✅ 升為 17 條 ✅（含本輪兩條）。**未動** 既有 audio 檔案（v1 / v2 / macOS say 大小皆無變動）/ `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / `app/quiz/page.tsx` / `components/QuizPlay.tsx`（UI 邏輯不動，純資料切換）/ 任何 `app/review/*` / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `docs/DATA_SCHEMA.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / `.env.example` / `.gitignore` / `package.json` / `scripts/*` / 依賴。**本輪未做** 呼叫 OpenAI API / 重產音檔 / 刪除舊音檔 / 新增題目 / 改答案 / 改 transcript / 改 ttsScript / 改 UI 邏輯 / 批次產生音檔 / Speaking Agent / 後端 / DB / 登入 / 部署 / 處理 npm audit / commit API key / commit `.env` / `.env.local`。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含新 audio src `/audio/starters/l3/q-lc-001-openai-v2.mp3` + 不再含 `/audio/starters/l3/q-lc-001.m4a` 路徑 + 新 mp3 檔可由瀏覽器 fetch 200 / 33792 bytes / `audio/mpeg` content-type + 既有 `q-lc-001-openai.mp3` (v1) 64896 bytes 不變 + 既有 `q-lc-001.m4a` 12008 bytes 不變 + 考試中 transcript 仍隱藏（visible HTML 含三段提示「請先聽音檔，再選答案」/「文字稿會在交卷後訂正時顯示」/「正式考試中錄音會播放兩次」）+ `What does the boy want?` 在整個 HTML 只出現 1 次（位於 RSC payload 內）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。P2 仍 🟡（P2-4C-2B-2 部分 🟡），P3 仍 🟡（P3-9-C 從 15 條 ✅ 升為 17 條 ✅）。
- 2026-05-10：OpenAI TTS examiner voice **v2 instructions 調整 + 腳本版本化輸出**——依使用者 v1 實聽回饋（「比 macOS say 正式但語速太慢、有點刻意」）調整。`scripts/generate_openai_tts_sample.mjs` 改為：(a) instructions 從 v1「Speak slowly and clearly for a 6-year-old child / Use clear standard British English pronunciation」改為 **v2「Speak clearly at a natural exam pace for a 6-year-old child, not overly slow / Use clear standard English pronunciation」**+ 加 **「Do not over-emphasize each word」**+ 「Tone: warm, neutral, professional, and natural」（保留 examiner 主軸）+ 沿用「Do not sound cartoonish or like a storyteller / Do not add extra words / Read only the given text exactly as written」；(b) 輸出檔名加 `-v2` suffix（預設 `q-lc-001-openai-v2.mp3`），支援 `OPENAI_TTS_OUTPUT_SUFFIX` 環境變數覆寫產生 v3 / v2-fable 等其他版本；(c) docstring 加 v1 / v2 版本歷史 + 完整使用方式（含 OPENAI_TTS_OUTPUT_SUFFIX 用法）；(d) 拒絕覆蓋既有檔案邏輯維持（v2 路徑與 v1 + macOS say m4a 皆不衝突）；(e) 「無 OPENAI_API_KEY 安全退出 exit 0」路徑維持。`docs/TTS_AUDIO_WORKFLOW.md` 升 **v2.1**——「第二階段」段尾新增「**OpenAI examiner voice 實聽調整紀錄**」子段：v1 / v2 instructions 與 output 對照表 + v1 → v2 調整理由（移除 Speak slowly / 加 natural exam pace + not overly slow / 加 Do not over-emphasize each word / British → standard）+ **v2 實聽檢核點 6 項**（語速自然 / 無過度強調為新增重點）+ **v3+ 觸發條件對照表**（仍太快 → 加 comfortable pauses / 仍太慢 → 加 conversational exam pace / 像新聞主播 → 加 patience of someone speaking to a young child / 仍刻意 → 改 voice 為 fable 或 nova / 加字 → 加 Do not greet, do not introduce, do not summarize）+ 跑 v3 的環境變數用法。`README.md` `/quiz` 條目補 v1 實聽回饋 + v2 instructions 調整重點 + 「v2 待實聽」+ 「`/quiz` 仍使用 macOS say 版本切換 audioSrc 屬下一輪」+ 指向實聽紀錄段。`PROJECT_ROADMAP.md` P3-9-C「使用者實聽比較」⬜ 改 🟡（v1 已實聽 / v2 待實聽）+ 新增 1 條 ✅（v2 instructions 調整 + 腳本版本化輸出）。**未動** `data/p3-example-questions.json`（**audioSrc 仍 `.m4a` 不切換**）/ `q-lc-001.m4a`（macOS say 版本完整保留）/ `q-lc-001-openai.mp3`（v1 完整保留）/ `lib/types.ts` / `lib/data.ts` / `app/quiz/page.tsx` / `components/QuizPlay.tsx` / 任何 `app/review/*` / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `docs/DATA_SCHEMA.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / `.env.example` / `.gitignore` / `package.json` / 依賴。**未做** 實際 API 呼叫（Claude 環境無 OPENAI_API_KEY，腳本「無 key 安全退出」路徑自測 exit 0）/ 切換 audioSrc / 批次產生多題 / 覆蓋 v1 或 macOS say 版本 / 修改題目資料 / 新增題目 / 改答案 / Speaking Agent / 錄音 / STT / 後端 / DB / 登入 / 部署 / 處理 npm audit / commit API key / commit `.env` / `.env.local`。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ 腳本「無 OPENAI_API_KEY 安全退出」實跑驗證 exit 0 + working tree 中 v1 + macOS say 版本完整保留（v1 64896 bytes / macOS say 12008 bytes 均無變動）+ `q-lc-001-openai-v2.mp3` 仍**未產生**（待使用者本機跑）。P2 仍 🟡（P2-4C-2B-2 部分 🟡），P3 仍 🟡（P3-9-C 從 14 條 ✅ 升為 15 條 ✅，「使用者實聽比較」從 ⬜ 改 🟡）。
- 2026-05-10：完成 P3-9-C / P2-4C-2B-2 OpenAI TTS examiner voice 試產流程文件 + 腳本（一題版）——讓使用者本機自行跑 + 實聽比較 macOS `say` vs OpenAI TTS 是否更接近正式考試員音色。**本輪未實際呼叫 OpenAI API**（Claude 環境無 OPENAI_API_KEY），腳本「無 key 安全退出」路徑已自測通過 (exit 0)。`docs/PRODUCT_SPEC.md`「目前明確不做 → AI / 自動化」段補一條「不做大量雲端 TTS 批次產生」+ 新增子段「**OpenAI TTS examiner voice 試產（一題版例外，2026-05-10 開放）**」含 6 條邊界（✅ 只把自製文字轉自製音檔 / ✅ 一題試產 / ✅ 人工實聽後下一輪才切 audioSrc / ✅ TTS voice AI-generated 須處處標示 / ❌ API key 不可 commit / ❌ 不下載官方音檔 / ❌ 不做 OpenAI 以外的雲端 TTS）。`docs/TTS_AUDIO_WORKFLOW.md` 升 **v2** 新增「**第二階段：OpenAI TTS examiner voice 試產流程**」完整段——目的（產生更接近 Cambridge-style young learners examiner 音色） + 硬邊界（與第一階段一致 + 雲端 API 額外條款 6 條）+ API key 規範（.env.example 範本 + .env.local 實際 key + 絕不 commit）+ 4 步流程（設定 key → 跑腳本 → API 與 instructions → 人工實聽 5 項）+ 切換 audioSrc 時機（本階段不切換、下一輪實聽通過後才動）+ 失敗處理表（401 / 429 / 500 / 多念字等）+ 第三階段（其他雲端 TTS 評估，仍不做）；版本段加 v2 紀錄。新增 `scripts/generate_openai_tts_sample.mjs`（一題試產腳本，~150 行）：讀 `OPENAI_API_KEY` 環境變數；無 key 印提示安全退出 exit 0；只產 `public/audio/starters/l3/q-lc-001-openai.mp3`；模型 `gpt-4o-mini-tts` + voice `alloy`（可透過 `OPENAI_TTS_VOICE` 環境變數覆寫）+ Cambridge-style examiner instructions（清楚平穩溫和、英式發音、語速略慢、不卡通、不像故事旁白、不自行加字）；用 Node 內建 fetch 呼叫 `https://api.openai.com/v1/audio/speech`（不裝套件）；拒絕覆蓋既有 `q-lc-001.m4a`（macOS `say` 版本）與既存 OpenAI 版本；輸出含檔案大小 + 下一步人工實聽確認指引；TTS voice AI-generated 提醒。`.env.example`（**新增**）`OPENAI_API_KEY=` 空值範本可 commit 給其他開發者參考；`.gitignore` 補 `!.env.example` 例外讓範本可 commit（`.env*` 仍排除實際 key 檔案）。`README.md` `/quiz` 條目補 OpenAI TTS 評估說明 + AI-generated 免責 + API key 不 commit + 不上傳官方原文。`PROJECT_ROADMAP.md` P3-9-C 把原 `⬜ OpenAI TTS examiner voice 試產流程` 升級為 **1 ✅**（試產流程文件 / 腳本完成）+ **4 ⬜**（使用者實聽比較 / 通過後切 audioSrc / 批次產生多題 / P4 Speaking Agent 共用 examiner voice 策略）；P3-9-C 整體仍 🟡（不整體完成）。**未做** 實際 API 呼叫（無 key）/ 切換 audioSrc / 批次產生 / 覆蓋 `q-lc-001.m4a` / 修改 `data/p3-example-questions.json` / 新增題目 / 改答案 / Speaking Agent / 錄音 / STT / 後端 / DB / 登入 / 部署 / 處理 npm audit / commit API key / commit `.env` / `.env.local`。**未動** `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / `app/quiz/page.tsx` / `components/QuizPlay.tsx` / 任何 `app/review/*` / 其他 components / `docs/DATA_SCHEMA.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有圖片 / 音檔（含 `q-lc-001.m4a` 完全保留）/ `package.json` / 依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ 腳本「無 OPENAI_API_KEY 安全退出」路徑實跑驗證 exit 0 + 提示文字含正確指引（複製 .env.example 為 .env.local / `--env-file` 用法）。P2 仍 🟡（P2-4C-2B-2 部分 🟡），P3 仍 🟡（P3-9-C 從 13 條 ✅ 升為 14 條 ✅，新增 4 條 ⬜，總共 14 ✅ + 9 ⬜）。
- 2026-05-10：P3-9-C 第二刀補一條 ⬜——對應任務單新提到的「OpenAI TTS examiner voice 試產流程」，列為未來 ⬜ 條目（目前**不串 API**，需先在 `docs/PRODUCT_SPEC.md`「目前明確不做」開放雲端 TTS + 補 API key 管理規範後才能動工）。其他「考試中隱藏 transcript / 結果頁仍顯示 transcript / 音檔缺失 fallback / 練習模式顯示文字稿開關」4 條目皆於前一輪（同日 P3-9-C 第二刀首次提交）完成，無重複實作。**本輪零程式碼變動、零 doc 內容變動**——只在 P3-9-C 加 1 條 ⬜ 條目對齊任務單規劃。`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）。P2 仍 🟡（P2-4C-2B-2 部分 🟡），P3 仍 🟡（P3-9-C 13 條 ✅ + 6 條 ⬜，含本輪新增的 OpenAI TTS examiner voice 試產流程）。
- 2026-05-10：完成 P3-9-C 第二刀 Listening 考試中隱藏 transcript——`/quiz` listening 題型 UI 行為修正：audioSrc 可播時不再露 transcript（避免孩子直接看到題目原文）；交卷後結果頁詳解仍顯示 transcript 供訂正；音檔缺失時才 fallback 顯示文字練習。**只動 1 個元件 + 4 份 doc，零依賴新增、未動 schema / data / TTS 流程**。`components/QuizPlay.tsx` `ListeningChoiceView` 改寫顯示策略：`audioStatus !== "missing"` + audioSrc 存在時 → 顯示 audio controls + **新增** 🎧 主提示「請先聽音檔，再選答案」（sky-700 sm/text-base 粗體）+ 副提示「文字稿會在交卷後訂正時顯示」（slate-500 11px）+ 既有「💡 正式考試中錄音會播放兩次；本練習版可自行重播音檔練習」（sky-600 11~12px），**移除既有 transcript 大字**；`audioStatus === "missing"` → 顯示「音檔準備中，先用文字練習」amber-700 訊息 + transcript 大字（fallback 模式保留既有行為）。**結果頁 / retry result detail 不受影響**——`QuestionDetailCard` 透過 `getQuestionPromptDisplay()` helper 對 listening 題仍走「優先 transcript → 退 ttsScript」策略，始終顯示文字稿（屬訂正範圍，邏輯刻意與考試中分流）。`docs/DATA_SCHEMA.md` 「transcript / ttsScript / audioSrc 三者關係」表升級為含「考試中是否顯示」+「結果頁是否顯示」雙欄；新增「關鍵原則（P3-9-C 第二刀）」3 條 + 三態顯示策略表（loading / ready / missing 對應 UI 行為）。`docs/TTS_AUDIO_WORKFLOW.md`「用途」段補一條「聽力音檔存在且可播放時，測驗中應隱藏文字稿」+ 新增「transcript / ttsScript 主要用途」4 列表（交卷訂正 / 家長檢查 / 音檔缺失 fallback / TTS 產生來源）+ ⚠️ 提醒。`README.md` `/quiz` 條目把 listening 提示從「audio + 聽兩次提示 + transcript」改為三段 hint（🎧「請先聽音檔，再選答案」/「文字稿會在交卷後訂正時顯示」/「正式考試中錄音會播放兩次」）+ 補「正式練習時隱藏文字稿，交卷後詳解才顯示 transcript」+「音檔缺失才 fallback 文字練習」。`PROJECT_ROADMAP.md` P3-9-C 加 3 條 ✅（考試中隱藏 transcript / 結果頁仍顯示 transcript / 音檔缺失 fallback）+ 1 條 ⬜（練習模式顯示文字稿開關，未來功能）；P3-9-C 整體仍 🟡。**未動** TTS 產生流程 / 不重產音檔 / 不串 OpenAI TTS / 不串雲端 API / 不新增題目 / 不改答案 / 不改 audioSrc / `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / `app/quiz/page.tsx` / 任何 `app/review/*` / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有圖片 / 音檔 / `package.json` / 依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 audio src=m4a + 「請先聽音檔，再選答案」hint + 「文字稿會在交卷後訂正時顯示」副標 + 「正式考試中錄音會播放兩次」+ **不**含 transcript「What does the boy want?」visible HTML（確認考試中已隱藏）+ RSC payload 仍含完整 transcript / ttsScript（給結果頁詳解使用）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。P2 仍 🟡 進行中（P2-4C-2B-2 部分 🟡），P3 仍 🟡 進行中（P3-9-C 從 10 條 ✅ 升為 13 條 ✅，新增 1 條 ⬜）。
- 2026-05-10：完成 L3 第一個自製 TTS 音檔（P2-4C-2B-2 + P3-9-C）——本機自製音檔流程落地，**只用本機 macOS `say` + `afconvert`，不串雲端 TTS API、不下載官方音檔**。建立 `public/audio/starters/l3/` 目錄；用 `say -o /tmp/q-lc-001.aiff "What does the boy want?"` 產生 AIFF（86 KB）+ `afconvert -f m4af -d aac` 轉成 m4a（**12 KB / 1.86 秒 / AAC 22050 Hz / 31696 bps**），檔案放到 `public/audio/starters/l3/q-lc-001.m4a`；`data/p3-example-questions.json` `q-lc-001` audioSrc 從 `.mp3` 改為 `.m4a` 對齊實體檔案；瀏覽器原生支援 m4a / AAC（Chrome / Safari / Firefox / Edge 全支援）。新增 `docs/TTS_AUDIO_WORKFLOW.md`（v1）：用途 / 硬邊界（**✅ 可做：macOS `say` + `afconvert` 本機流程、自錄音、未來雲端 TTS 評估**；**❌ 不可做：串雲端 TTS API、下載官方音檔、複製官方 sample 音檔、用網路 mp3、第三方教學機構錄音、官方 PDF 內嵌音檔**）+ 完整 step 1~6 流程（say AIFF → afconvert m4a → 清理 → 驗證 → 更新 audioSrc → 本機驗證）+ 一行 shell 範例 + 命名規則（`<question-id>.m4a`）+ 路徑（`public/audio/starters/<part>/<id>.m4a`）+ audioSrc 對應規則（4 條：本專案內音檔 ✅ / 外部 CDN ❌ / Data URL ❌ / 官方 sample paper ❌）+ **人工檢查 6 項**（檔案存在性 / 格式正確 / 可播放 / 內容正確 / 長度合理 / 音量合理）+ git 政策（自製 m4a 可 commit / AIFF 不可 / 官方 / 網路 / 個資錄音不可 / 避免錯誤 commit 3 法）+ 第二階段雲端 TTS 評估（OpenAI / Google / Azure / Web Speech API 4 方案比較，**目前不串**，未來若採用需先在 PRODUCT_SPEC「目前明確不做」開放）+ 與其他文件關係表 + 版本 v1。`README.md` `/quiz` 條目補「第一個自製音檔已產生於 `public/audio/starters/l3/q-lc-001.m4a`」+ 文件索引追加 `docs/TTS_AUDIO_WORKFLOW.md`。`PROJECT_ROADMAP.md`：(a) P3-9-C「真實自製 TTS 音檔產生」/「多題 L3 題庫」/「音檔快取管理」3 條 ⬜ 改為「3 ✅（流程文件 / 路徑 / q-lc-001 第一個音檔）+ 4 ⬜（多題音檔 / 音檔品質檢查流程 / 雲端 TTS 評估 / 音檔快取管理）」；(b) P2-4C-2B-2「真實音檔」⬜ 改 🟡（部分進行中）+ 寫明 P3-9-C 第一刀已落地第一個 L3 音檔 + P2 vocabulary 音檔仍 ⬜；P2-4C-2B-2 整體 header 從「尚未開始」改「🟡 部分進行中」；P3-9-C 整體仍 🟡。**未做** OpenAI / Google / Azure / 任何雲端 TTS API / 不下載官方音檔 / 不複製官方 sample 音檔 / 不新增 listening 題目 / 不改題目答案 / 不改非 listening 題目 / 不做 Speaking / 錄音 / STT / AI API / crawler / 後端 / DB / 登入 / 不新增依賴 / 不處理 npm audit / 不部署 / 不升 localStorage schemaVersion。**未動** `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / `app/quiz/page.tsx` / `components/QuizPlay.tsx` / 任何 `app/review/*` / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `docs/DATA_SCHEMA.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有 SVG / vocabulary 音檔 / `package.json` / 依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 audio 元素 + audioSrc 指向新 m4a 檔 + 「正式考試中錄音會播放兩次」聽兩次提示 + transcript 仍顯示 + 新增 audio 檔可由瀏覽器以 200 fetched + `afinfo` 確認 m4a / AAC 編碼正確 + 既有 `/review` 路由完整回歸 + dev log 無 error / hydration / warn。P2 升 🟡 進行中（P2-4C-2B-2 部分 🟡）；P3 仍 🟡 進行中（P3-9-C 從 7 條 ✅ 升為 10 條 ✅）。
- 2026-05-10：完成 P3-9-C 第一刀 L3 聽音選圖 + audio 準備版——`/quiz` listening 題型補 optional `audioSrc` 欄位 + `<audio controls>` UI + 載入失敗 fallback 文字 + 聽兩次小提示。`lib/types.ts` `ListeningChoiceQuestion` 補 `audioSrc?: string` optional 欄位（保留既有 `audio: string` 必填、`transcript?` / `ttsScript?` optional），補完整 docstring 說明三欄位關係（audio = legacy / audioSrc = 本專案自製音檔路徑 / transcript = 字幕 / ttsScript = TTS 腳本）+ audioSrc 硬邊界（嚴禁指向官方音檔或外部 URL，只能是 `public/audio/` 下的本機自製檔）。`data/p3-example-questions.json` 唯一一題 listening (`q-lc-001`) 補 `audioSrc: /audio/starters/l3/q-lc-001.mp3`；不改 `audio` legacy 欄位、不改 transcript / ttsScript / options / answer / metadata；實體 mp3 尚未產生，UI 自動 fallback 文字。`components/QuizPlay.tsx` `ListeningChoiceView` 重構：新增 `AudioStatus` type（`loading` / `ready` / `missing`）+ `useState` 初始依 `audioSrc` 是否存在；audioSrc 存在且未 error 時 render `<audio controls src={audioSrc} onCanPlay={...} onError={...}>` + 「💡 正式考試中錄音會播放兩次；本練習版可自行重播音檔練習」小提示；audioSrc 缺值或 onError 觸發後 setState `missing` → 隱藏 audio 元素 + 顯示「音檔準備中，先用文字練習」amber-700 提示；transcript / ttsScript 文字始終顯示於下方（保留現有顯示能力）。setState 在 onCanPlay / onError 事件 handler 中觸發，不在 effect body，避開 React 19 `react-hooks/set-state-in-effect` 規則。`docs/DATA_SCHEMA.md` listening-choice 段補 `audioSrc?: string` 欄位說明 + transcript / ttsScript / audioSrc 三者關係 4 列表格 + UI 顯示策略 3 段（成功載入 / 缺值 / 載入失敗）+ audioSrc 硬邊界 4 條（不指向官方音檔、不下載官方 sample paper 音檔、只能是 public/audio/ 下本機自製檔、可填佔位 fallback 自動降級）。`docs/STARTERS_PART_TEMPLATES.md` L3 段標題改為「v2 校正 + P3-9-C 第一刀 audio 準備」+ 內容補 audioSrc 支援、UI fallback 機制、聽兩次提示對應「heard twice」官方規則；版本段加 v2.1（2026-05-10）紀錄；硬邊界不變。`PROJECT_ROADMAP.md` P3-9-C 從 ⬜ 升為 🟡 進行中（7 條 ✅：audioSrc 欄位 / audio controls UI / fallback / 聽兩次提示 / q-lc-001 metadata / DATA_SCHEMA 補段 / STARTERS_PART_TEMPLATES 升 v2.1；3 條 ⬜：真實 TTS 音檔產生 / 多題 L3 題庫 / 音檔快取管理；其他 part-specific UI 條目保留 ⬜）；P3-9 整體仍 🟡。`README.md` `/quiz` 條目補 audioSrc 支援 + fallback 說明。**未做** 真正 TTS 音檔產生 / 不串 TTS API / 不下載官方音檔 / 不新增多題 listening 題 / 不改題目答案 / 不改非 listening 題目 / 不做 Speaking / 不做錄音 / 不做 STT / 不做 AI API / 不新增 crawler / 不升 localStorage schemaVersion / 不做 `/quiz/wrong`。**未動** `lib/data.ts` / `lib/examSessionStorage.ts` / 任何 `app/*` 路由 / 其他 components / `docs/PRODUCT_SPEC.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有圖片 / 音檔 / `package.json` / 依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A ✅；P3-6-B-4 部分 ✅（6 / 9）/ P3-7-B 部分 ✅（6 / 9）/ P3-9-B 部分 ✅（6 / 13）/ P3-9-C 部分 ✅（7 / 17）/ P3-6-B-5 部分 ✅（1 / 2）；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-7-C / P3-7-D / P3-8 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-09：完成 P3-7-B 第一輪官方 format 校正——`docs/STARTERS_PART_TEMPLATES.md` 升 **v2**（依官方公開 format 說明校正）；本輪純文件，**零程式碼變動**。**STARTERS_PART_TEMPLATES.md v2 主要變更**：(a) 標題改為「P3-9-A 起，v2 校正版」+ 頂端 callout 標 v2 日期；(b) 新增「**官方 format 摘要**」段（Listening 4 parts / 20 Q / 20 min / heard twice + R&W 5 parts / 25 Q / 20 min / spelling must be correct + Speaking 4 parts / 3-5 min 不在 P3）+「本專案使用方式」邊界；(c) 校正 9 個 Part 模板（L1~L4 + RW1~RW5）描述，每個 Part 補官方規則（heard twice / spelling must be correct / 1~2 examples per part）+ 練習版邊界與 schema 差距；(d) 「目前 P3 schema 對應表」改為**三層分類**——第一層已較接近正式 Part 的題型（listening-choice → L3 preview/部分支援、fill-blank → RW4 partial）+ 第二層 preview / 預備型題型（picture-choice → RW1/RW2 preview、word-choice → RW3 preview 反向、multiple-choice → RW4 preview / 詞彙選擇預備、matching → RW5 preview / 故事理解預備）+ 第三層尚未支援的正式題型（L1 hotspot 互動、L2 多 row 表格、L4 真正塗色、RW1 yes-no 子題型、RW2 sceneGroup、RW3 spelling-input、RW4 multiBlankAnswers + wordBank、RW5 imageSequence + one-word、Speaking SP1~SP4）；明示「這些都還不是官方題庫，只是自製練習題型對正式 parts 的逐步對齊」。(e) 新增「**v2 後續實作優先順序建議**」段（8 項排序：L3 + TTS 音檔最先 / RW3 拼字輸入 / RW1 yes-no / RW4 多空格 / RW5 picture-story / L2 / L4 / L1 hotspot 最後）+ 排序理由 + 跨項目共通要求（不複製官方原文）。(f) 版本段保留 v1（2026-05-08）+ 新增 v2（2026-05-09）紀錄。**OFFICIAL_RESOURCES.md 同步更新**：在「用官方資源校正 P3-9 模板」段補「**P3-7-B 第一輪校正狀態（2026-05-09）**」子段——記錄 11 條校正清單第 1~11 全條第一輪通過、`STARTERS_PART_TEMPLATES.md` 已升 v2、後續第二 / 三輪校正待辦（handbook / sample paper / mock test toolkit 人工筆記、wordlist 覆蓋分析）；版本段加 v1.1（P3-7-B 第一輪校正進度更新）+ v2 規劃調整為「等第二 / 三輪校正後升」。**PROJECT_ROADMAP.md** P3-7-B 從 ⬜ 升為 🟡 進行中（6 條 ✅：v2 升級 / format 摘要 / 三條官方規則 / L1~L4 + RW1~RW5 模板校正 / schema 對應表分級 / v2 後續實作優先順序；3 條 ⬜：後續 handbook / sample paper 人工筆記、後續 v3 校正、後續 wordlist 校正）；P3-7 整體仍 🟡（A 已完成、B 第一輪完成、C 與 D 仍 ⬜）。**README.md** 補一句「`docs/STARTERS_PART_TEMPLATES.md` 已升 v2，依官方公開 format 說明校正」。本輪**未動** `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / 任何 `data/*.json` / 任何 `app/*` 路由 / 任何 components / `docs/PRODUCT_SPEC.md` / `docs/DATA_SCHEMA.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有圖片 / 音檔 / `package.json` / 依賴。**本輪未做** crawler / 不下載官方 PDF / 圖片 / 音檔 / 不複製官方題目 / 不複製 sample paper 題目 / 不新增題目 / 不修改 data 或 types / 不改 /quiz UI / 不做 TTS / 錄音 / STT / AI API / Speaking UI / localStorage / quiz 功能；無新增依賴 / 無處理 npm audit / 無部署 / 無新增後端 / DB / 登入。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A ✅；P3-6-B-4 部分 ✅（6 / 9）/ P3-7-B 部分 ✅（6 / 9）/ P3-9-B 部分 ✅（6 / 13）/ P3-6-B-5 部分 ✅（1 / 2）；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-7-C / P3-7-D / P3-8 / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-09：P3-9-C 小修——`/quiz` Part 顯示文案依 (metadata, question.type) 組合精準化。`components/QuizPlay.tsx` `getStarterPartInfo(question)` 在原有 metadata-first / type-fallback 邏輯之前加一條 (starterPart, type) 組合特殊覆寫：**`RW4 + multiple-choice` → 「Part 4 preview：短句選字 / 詞彙選擇」**，解決 P3-9-B 第一刀回報中風險點 1 的 mc → RW4「短文 / 句子填空」文案落差（q-mc-001「Which one is a fruit?」之前籠統顯示為「Part 4：短文 / 句子填空」，現顯示更精準的「短句選字 / 詞彙選擇 preview」）；其他 (part, type) 組合維持 default `STARTER_PART_DISPLAY` map 文案（`RW4 + fill-blank` → Part 4 短文 / 句子填空、`RW1 + picture-choice` → Part 1 看圖判斷 / 看圖選答案、`RW3 + word-choice` → Part 3 看圖認字 / 拼字練習、`RW5 + matching` → Part 5 圖文配對 / 故事理解預備、`L3 + listening-choice` → Part 3 聽音選圖 等）；fallback 路徑保留——metadata 缺值仍依 `question.type` 推導（保留「Part X preview」字樣於 fallback 路徑）。helper 註解補完整邏輯說明（metadata 決定對齊目標 / question.type 協助練習版細分 / 仍是練習版近似對應，不代表官方題目）。`docs/DATA_SCHEMA.md` 在 Starters part metadata 段新增「UI 文案細分（依 `question.type` 微調）」一節：含 6 列對照表（5 個明示組合 + default fallback）+ 3 條設計原則（STARTER_PART_DISPLAY default + getStarterPartInfo 特殊覆寫 + 增加新組合只需加 if）+ 3 條目的（解決文案落差 / 維持 metadata-first / 仍是練習版近似對應不代表官方題目）。`PROJECT_ROADMAP.md` P3-9-B 由 5 條 ✅ 升為 6 條 ✅（新增 `getStarterPartInfo()` 升級為 (starterPart, type) 細分文案）；DATA_SCHEMA 條目末尾補「+ UI 文案細分對照表」；P3-9 / P3-9-B / P3-9-C 整體仍 🟡。`README.md` `/quiz` 條目把 `q-mc-001` mapping 從「RW4 短文 / 句子填空」改為「RW4 preview 短句選字 / 詞彙選擇」，並補一句「Part 顯示已改為 metadata-first，並可依題型微調練習版文案」。**未動** `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / 任何 `data/*.json` / 其他 components / 任何 `app/*` 路由 / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有圖片 / 音檔 / `package.json` / 依賴；**未升** localStorage `QUIZ_SESSION_SCHEMA_VERSION`；**未做**完整 P3-9-C UI / validator / ttsScript-imagePrompt schema / `/quiz/wrong` / 獨立錯題頁 / 計時器 / Speaking / TTS / 錄音 / STT / AI API / crawler。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-9-B 升為 6 條 ✅ + 7 條 ⬜，P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A 維持 ✅；P3-6-B-4 仍 6✅+3⬜；P3-6-B-5 仍 1✅+1⬜；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-09：完成 P3-9-B 第一刀——把 Starters part metadata 落地進 schema、範例資料與 `/quiz` UI；不一次加入所有未來欄位、不改 UI 大版面、不新增題目。`lib/types.ts` 新增 4 個字串字面量類型：`StarterSection`（listening / reading-writing / **speaking 預留**）、`StarterPart`（L1~L4 / RW1~RW5 / **SP1~SP4 預留**共 13 個值）、`SkillFocus`（listening / vocabulary / spelling / reading / writing / speaking）、`ExpectedAnswerType`（choice / text / number / name / color / one-word / spoken）；`BaseQuestion` 補 4 個 optional 欄位（`starterSection?` / `starterPart?` / `skillFocus?[]` / `expectedAnswerType?`），不破壞 discriminated union、不新增 part-specific question types。`data/p3-example-questions.json` 7 題各補 metadata（依任務單明示 mapping：q-mc-001 → RW4 / q-pc-001 → RW1 / q-wc-001 → RW3 / q-lc-001 → L3 / q-fb-001 → RW4 / q-fb-002 → RW4 / q-mt-001 → RW5），不改 prompt / answer / options / explanation。`components/QuizPlay.tsx` 新增 `STARTER_PART_DISPLAY` map（13 個 part：L1 聽句子配人物 / 物件位置、L2 聽對話寫 name / number、L3 聽音選圖、L4 聽指令 · 顏色 / 物件、RW1 看圖判斷 / 看圖選答案、RW2 看大圖回答 yes / no、RW3 看圖認字 / 拼字練習、RW4 短文 / 句子填空、RW5 圖文配對 / 故事理解預備、SP1~SP4 P4 預留）；`getStarterPartInfo()` / `getSectionTag()` 優先讀 `question.starterPart` / `question.starterSection` metadata，缺值時 fallback 到依 `question.type` 推導（保留「Part X preview」字樣於 fallback 路徑）；fallback 路徑保留所以舊資料完全相容；新增 `StarterPart` import 從 `@/lib/types`。`docs/DATA_SCHEMA.md` 補「Starters part metadata（P3-9-B）」一節（用途 / 邊界 / 4 欄位說明表 / 範例 jsonc / 7 題範例的 metadata 對應表 / 不在 P3-9-B 第一刀範圍 6 條）。`PROJECT_ROADMAP.md` P3-9-B 從 ⬜ 升為 🟡（5 條 ✅：types / BaseQuestion 欄位 / 7 題範例 metadata / Quiz Part 顯示優先讀 metadata / DATA_SCHEMA 補段；7 條 ⬜：ttsScript / imagePrompt 評估、difficulty、part-specific types、validator、sceneGroup、multi-blank、imageSequence 等）；P3-9 整體仍 🟡。`README.md` 「目前功能」`/quiz` 條目補：範例題目已補 Starters section / part metadata、`/quiz` Part 顯示優先讀題目 metadata、目前仍是練習版近似對應、Speaking metadata 預留不在 P3 實作。**未做** ttsScript / imagePrompt 升正式 schema / difficulty 字面量升級 / part-specific question types / metadata validator / 多題共用 scene image / 多空格 fill-blank / 多圖序列 / Speaking UI / TTS / 錄音 / STT / AI API / crawler / `/quiz/wrong` / 獨立錯題頁 / 計時器 / 完整 P3-9-C UI / localStorage schemaVersion 升級。**未動** `lib/data.ts` / 任何 `app/*` 路由 / 其他 components / `lib/examSessionStorage.ts` / `docs/PRODUCT_SPEC.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 既有圖片 / 音檔 / `package.json` / 依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 Listening 徽章 / 聽音選圖 / 直接交卷 / 重新測驗（metadata 帶 starterPart=L3，與 fallback 顯示一致）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A ✅；P3-6-B-4 部分 ✅（6 / 9）/ P3-9-B 部分 ✅（5 / 12）/ P3-6-B-5 部分 ✅（1 / 2）；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-09：完成 P3-6-B-4 第二刀——`/quiz` 結果頁加「再練習這些題目」inline retry mode，純 React in-memory state，**不**新增 `/quiz/wrong` 路由 / **不**升 `QUIZ_SESSION_SCHEMA_VERSION` / **不**新增 `wrongQuestionIds` localStorage 欄位 / **不**覆蓋原始測驗分數。`components/QuizPlay.tsx` 加 4 個新 state（`retryQuestionIds: string[] | null` / `retryAnswers` / `retryIndex` / `retrySubmitted`）+ 5 個 handler（`handleStartRetry` / `handleExitRetry` / `handleSelectRetryAnswer` / `handleRetryNext` / `handleRetrySubmitNow`）+ `resetRetryState` helper 給 `handleRestart` 同時重置 retry state；`inRetry = retryQuestionIds !== null` derived；`retryIdSet` + `retryQuestions = questions.filter(...)` + `retryTotal` derived。**渲染分支順序**：total=0 → inRetry+retrySubmitted（RetryResultView）→ inRetry+!retrySubmitted（retry quiz JSX 內聯）→ session.submitted（ResultView 加 onStartRetry prop）→ !current → 一般 quiz；retry mode 取代既有 session.submitted 結果頁渲染。**Retry quiz JSX 內聯**：amber 系 banner「🔁 再練習模式：只練習錯題與未作答題」+ 副標「本次再練習結果<strong>不會覆蓋</strong>原始測驗分數；隨時可按下方『返回完整結果』回到原始結果頁」+ 段落徽章 / Part 標示重複利用既有 helpers + 進度「再練習 第 X / Y 題」（retry-relative）+ QuestionView + 「下一題」/「看再練習結果」按鈕 + 底部 chip 「📝 直接交卷」/「↩ 返回完整結果」。**新增 `RetryResultView` 元件**：amber 系 banner 含「🔁 本次再練習結果」+ 副標重申不覆蓋原始分數 + 🌱 重新嘗試圖示 + 「再練習完成！」標題 + 答對 N/Y + 雙欄統計（已作答 / 未作答）+ 4 段鼓勵語（依答對率分級）+ 「再練習每題詳解」section + 重複利用 `QuestionDetailCard`（用 `originalIndexById` map 取原始試卷 index、確保「第 N 題」對齊整份試卷）+ 「↩ 回到完整測驗結果」主按鈕（amber-400）+「🔁 重新測驗（清除原始與再練習進度）」次按鈕 + 回首頁 link。**ResultView props 新增** `onStartRetry: (ids: string[]) => void`；reviewCount > 0 時顯示「🔁 再練習這些題目（{reviewCount}）」rose-400 按鈕，位於詳解列表與重新測驗之間；按下傳 `statuses.filter(s => s.status === "incorrect" || s.status === "unanswered").map(s => s.question.id)`。修兩處 markdown `**` syntax 在 JSX 中無法 render 的 bug → 改用 `<strong className="font-bold">` 包裹「不會覆蓋」。**Codex 暫停期由 Claude 自測**通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 Listening / 聽音選圖 / 直接交卷 / 重新測驗 + 首次進入不含「再練習這些題目」/「再練習模式」/「本次再練習結果」/「回到完整測驗結果」/「每題詳解」/「不會覆蓋」（因 submitted=false 時 ResultView / RetryResultView / retry quiz 皆不渲染、字串只在 client JS chunk 內）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。`PROJECT_ROADMAP.md` P3-6-B-4 從「3 條 ✅ + 3 條 ⬜」升為「6 條 ✅ + 3 條 ⬜」（加：「再練習這些題目」入口 / inline retry mode / 不新增路由不升 schemaVersion）；P3-6-B 整體仍 🟡（B-1 / B-2 / B-3 完成；B-4 部分完成；B-5 仍 1 條 ✅ + 1 條 ⬜）。`README.md` `/quiz` 條目補 retry mode 說明（rose-400 按鈕 / amber 系 banner / 不覆蓋原始分數 / 完成後可回完整結果）。本輪未動 `lib/types.ts` / `lib/data.ts` / `lib/examSessionStorage.ts` / 任何 `data/*.json` / `app/quiz/page.tsx` / 任何 `app/review/*` / 其他 components / 所有 docs / source_materials / `package.json` / 依賴。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A ✅；P3-6-B-4 部分 ✅（6 / 9）；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B-5 部分 / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-B / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-09：完成 P3-6-B-4 第一刀——`/quiz` 結果頁加「全部 / 只看錯題 / 只看未作答 / 需要再練習」inline filter，純 React local state、零依賴新增、未動 localStorage。`components/QuizPlay.tsx` 在 file scope 新增 `DetailFilter` type（4 個字串字面量 all / incorrect / unanswered / review）+ `FILTER_LABELS` + `FILTER_ORDER` + `EMPTY_STATE_MESSAGES` 三段友善空狀態提示；ResultView 內加 `useState<DetailFilter>("all")`、一次計算 `statuses` 陣列（每題 question / index / status，後續四個 count 與 filter 共用）、computed `incorrectCount` / `unansweredCount` / `reviewCount` / `filterCounts` record；4 個 chip buttons 排在「每題詳解」標題下方（flex-wrap + center + amber-300 selected / white unselected + 內嵌 count badge）；空狀態時顯示 emerald 系卡片含對應友善訊息（incorrect → 「太棒了，目前沒有答錯的題目！」/ unanswered → 「很好，這次每一題都有作答！」/ review → 「全部都很棒，這次沒有需要再練習的題目！」）；filter 過濾仍保留原始 `index` 顯示「第 N 題」對齊整份試卷編號。matching 完成後算 correct，不出現在 review／incorrect / unanswered 任何 filter。**不**新增 `/quiz/wrong` 路由、**不**新增 `wrongQuestionIds` localStorage 欄位、**不**升 `QUIZ_SESSION_SCHEMA_VERSION`、**不**做正式歷史紀錄頁、**不**做計時器、**不**動 `lib/types.ts` / `data/*.json` / `lib/examSessionStorage.ts` / `app/quiz/page.tsx` / 任何其他 components / 任何 docs。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 Listening 徽章 / 直接交卷 / 重新測驗 + 首次進入不含「每題詳解」/ 4 個 filter chip / 空狀態提示文字（因 submitted=false 時 ResultView 不渲染、字串只在 client JS chunk 內）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。`PROJECT_ROADMAP.md` P3-6-B-4 從 ⬜ 升為 🟡 進行中（3 條 ✅：filter / 數量 / 空狀態；3 條 ⬜：再練習錯題入口 / 獨立錯題複習頁 / wrongQuestionIds 升 v2 + migration）；P3-6-B 整體仍 🟡 進行中（B-1 / B-2 / B-3 完成；B-4 部分完成；B-5 計時器仍 ⬜）。`README.md` `/quiz` 條目補結果頁篩選段。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A ✅；P3-6-B-4 部分 ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B-5 / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-B / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-09：完成 P3-6-B-3 第一刀——`/quiz` 結果頁加「每題詳解列表」。`components/QuizPlay.tsx` 新增 5 個純函式 helper（`getQuestionStatus` 三態 / `formatUserAnswer` / `formatCorrectAnswer` / `getQuestionPromptDisplay` / `getExplanationDisplay`）+ `STATUS_STYLES` 三色配色表（emerald 答對 / rose 答錯 / amber 未作答）+ `QuestionDetailCard` 子元件（每題卡片含：第幾題 + Section + Part + 狀態 chip + 題目文字版 + 你的答案 / 正確答案雙欄 + 說明）；ResultView props 從 `total / correctCount / answeredCount` 改為 `questions / answers`，內部即時計算統計，避免重複計算邏輯；保留既有「答對 N / M + 已作答 X / M + 未作答 M-X + 鼓勵文案 + 重新測驗」上半部視覺；結果頁底部插入新區段「每題詳解」`<section aria-label="每題詳解">` + 卡片清單。每題型答案格式化：multiple-choice / picture-choice / word-choice / listening-choice 顯示選到的字串值（`answer` 即 label 或 ImageOption.value）；fill-blank 選項版顯示選的選項，自由填空版顯示輸入文字；matching `_done` token 顯示「已完成閱讀配對練習」、正確答案顯示「本題目前為閱讀型練習，完成即算正確」；未作答顯示「尚未作答」、不算對。題目顯示文字版（不複製大圖 / 大音檔給家長辨識）：listening 用 `transcript` / `ttsScript`；word-choice 顯示「這個英文單字是「X」」；picture-choice / matching 用 prompt 或備援文字；multiple-choice / fill-blank 用 prompt。explanation 優先顯示 `BaseQuestion.explanation`，無 explanation 時依狀態給三段鼓勵性 fallback。**不**新增獨立錯題頁 / `/quiz/wrong` 路由 / 錯題 localStorage / 歷史紀錄頁；**不**升 `schemaVersion`、**不**動 `lib/types.ts` / `data/*.json` / 任何 `app/review/*` 路由 / `lib/examSessionStorage.ts`；**不**新增依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 Listening 徽章 / 聽音選圖 / 直接交卷 / 重新測驗 + 首次進入不含「每題詳解」（因 submitted=false）+ RSC payload 含全部 7 題資料供 hydration 後 ResultView 組裝（`Which one is a fruit?` / `q-fb-002` / `I have a` 等命中）+ `/review/word/apple` 翻牌完整回歸 + dev log 無 error / hydration / warn。`PROJECT_ROADMAP.md` P3-6-B-3 從 ⬜ 升為 ✅（7 條 ✅）；P3-6-B 整體仍 🟡 進行中（B-1 / B-2 / B-3 完成；B-4 錯題複習獨立頁、B-5 計時器仍 ⬜）。`README.md` `/quiz` 條目補結果頁每題詳解段。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-6-B-3 / P3-7-A / P3-9-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B-4 / P3-6-B-5 / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-B / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-08：完成 P3-6-B-1 + P3-6-B-2——`/quiz` 從「一次性 React state」升級為「可保存進度的 Exam Session 第一版」，這是 P3-6-B 的第一刀。新增 `lib/examSessionStorage.ts` helper（150 行）：localStorage key `cambridge-starters-practice:quiz-session:v1`、`schemaVersion = 1`、`createEmptySession` / `loadSession` / `saveSession` / `clearSession` / `isCompatibleSession` 純函式 + 完整 try/catch（防 JSON parse 失敗 / SecurityError / SSR 環境 / `typeof window === "undefined"`） + `isQuizSessionShape` runtime shape 驗證；session 欄位精簡（schemaVersion / paperId / questionOrder / currentIndex / answers / submitted / startedAt / updatedAt / submittedAt 可選），與 `lib/types.ts` 的 `ExamSessionState` 概念對齊但**不存** `examSessionId` / `score` / `wrongQuestionIds`（屬 P3-6-B-3+）。`components/QuizPlay.tsx` 三個獨立 state（currentIndex / answers / submitted）改為單一 `session` state；hydration 後嘗試從 localStorage 恢復；session 變更後自動寫回 localStorage；setState in effect 透過 `queueMicrotask` 包成非同步 callback 避開 React 19 `react-hooks/set-state-in-effect` 規則（與既有 `QuizImage` 的 `probe.onload` 模式一致）；新增 `hydrated` flag 避免 race（初始 empty session 蓋掉舊 session）；不相容情境（schemaVersion / paperId / questionOrder 任一不符 / JSON parse 失敗）一律丟棄舊 session 並建立新 session、頁面不 crash。**繼續作答**：自動恢復進度 + emerald 系小提示「🔁 已恢復上次作答進度」（首次點選任何答案後消失）。**重新測驗**：題目卡下方灰底 chip 按鈕，無 confirm dialog（依任務單），清 session + 回第一題 + answers 清空 + submitted = false。**直接交卷**：題目卡下方 amber chip 按鈕，submitted + submittedAt 寫入後立即進結果頁。**結果頁**改寫：答對 N / M + 已作答 X / M + 未作答 M-X 三段（emerald / rose 配色）+ 鼓勵文案 + 「未作答的題目算錯」明示 + 重新測驗。`app/quiz/page.tsx` 補 `paperId` prop 傳給 `<QuizPlay>`（從 `paper.examPaperId` 讀 `starters-mock-001`）。`PROJECT_ROADMAP.md` P3-6-B 從單一 ⬜ 拆為五段：**P3-6-B-1**（6 條 ✅）+ **P3-6-B-2**（7 條 ✅）+ **P3-6-B-3** 完整結果頁與每題詳解（4 條 ⬜）+ **P3-6-B-4** 錯題詳解與錯題複習（3 條 ⬜）+ **P3-6-B-5** 計時器（1 條 ✅ 第一版不計時 + 1 條 ⬜ 未來計時器）；P3-6-B 整體狀態 ⬜ → 🟡。**未做**完整每題詳解 / 錯題複習頁 / 正式歷史紀錄 / 計時器 / Speaking / TTS / 錄音 / STT / AI API / crawler。**未動** `lib/types.ts` / `data/*.json` / `docs/PRODUCT_SPEC.md` / `docs/DATA_SCHEMA.md` / `docs/STARTERS_PART_TEMPLATES.md` / `docs/OFFICIAL_RESOURCES.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md` / `source_materials/*` / 任何 `app/review/*` 路由 / `components/PicturePractice` / `components/VocabularyCard` / 既有圖片 / 音檔 / SVG / `package.json` / 任何依賴。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）+ dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 listening visible HTML 含 Listening 徽章 / 聽力練習 / Part 3：聽音選圖 / 直接交卷按鈕 / 重新測驗按鈕 / paperId `starters-mock-001` 在 RSC payload + 首次進入 visible HTML 不含「已恢復上次作答進度」（因為初始空 session）+ 既有 `/review/word/apple` 翻牌與跨字母 ant/baby 完整回歸 + dev log 無 error / hydration / warn；helper 邏輯透過 Node 8 項單測通過（createEmptySession / 5 種不相容情境 / JSON 圓規 / bad JSON 處理）。`README.md` `/quiz` 條目補 localStorage 與基本操作說明。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-6-B-1 / P3-6-B-2 / P3-7-A / P3-9-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B-3 / P3-6-B-4 / P3-6-B-5 / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-B / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-08：完成 P3-7-A 官方資源索引文件第一版——本輪純文件，**零程式碼變動**。新增 `docs/OFFICIAL_RESOURCES.md`（v1）：用繁體中文撰寫，明示「只保存連結與人工筆記、不下載官方 PDF / 圖片 / 音檔、不複製官方題目 / 歷屆題、不做自動爬蟲、不把官方 sample 題目轉入正式題庫、不使用網路圖片當正式素材」7 條硬邊界。**用途與硬邊界**：對齊 PRODUCT_SPEC「目前明確不做」/ source_materials/README「官方資源與歷史題整理原則」/ AI_QUESTION_GENERATION「來源規則硬邊界」三處，列出 7 條 ✅ 可以做 + 9 條 ❌ 不可做。**索引格式**：每筆條目 8 欄位（資源名稱 / 官方用途 / 本專案用途 / 可參考內容 / 不可直接使用內容 / 對應 Roadmap / 對應文件 / 連結）；連結欄位由人工從主入口導航後驗證填入，**v1 不預先填入特定 sub-page URL** 避免 AI 生成過時資訊。**A 段官方考試格式與 parts**（4 條目：Cambridge English 主站 / Pre A1 Starters 考試資訊頁 / Test format 頁 / Handbook for teachers）。**B 段 preparation / sample resources**（4 條目：Preparation 頁 / Sample papers 入口 / Wordlist 入口 → C 段 / Mock test toolkit 入口 → D 段）。**C 段 Wordlist / vocabulary 方向**：C-1 wordlist 條目 + C-2 5 條使用原則（範圍參考、不複製整份 PDF、自家 data/vocabulary.json 為主、分類概念對齊既有 11 個 VocabularyCategory、未來整理需確認來源）+ C-3 與 P3-7-C 對接。**D 段 Sample / mock test toolkit 方向**：D-1 sample papers 5+5 對照表（可參考題型順序 / 互動方式 / 題目數量 / 音檔節奏 / 場景設計；不複製題目 / 圖片 / 音檔 / 答題單 / 場景圖原檔）+ D-2 mock test toolkit 4+4 對照表 + D-3 與 P3-7-D 對接。**用官方資源校正 P3-9 模板**：11 條檢查清單（L1~L4 + RW1~RW5 + getStarterPartInfo preview + STARTERS_PART_TEMPLATES 升 v2）+ 校正動作非目的（不抄原文 / 不要求 100% 還原 / 不轉 sample 為 data JSON）。**AI 仿真題素材來源策略**：6 條 ✅ 應使用（官方題型結構 / 本專案 vocabulary / 自製 imagePrompt / 自製 ttsScript / 自製 explanation / 人工審核）+ 6 條 ❌ 不應使用（官方題目全文 / 官方圖片 / 官方音檔 / 歷屆題原文 / 網路圖片 / 外部 URL）+ 違反時處理 SOP。**與其他文件關係表**：對齊 PRODUCT_SPEC / source_materials/README / STARTERS_PART_TEMPLATES / AI_QUESTION_GENERATION / DATA_SCHEMA / PROJECT_ROADMAP 六處。`PROJECT_ROADMAP.md` 把 P3-7 從單一 ⬜ 拆為四段：**P3-7-A**（11 條 ✅，本輪完成）+ **P3-7-B** format 對 P3-9 模板逐項校正（11 條 ⬜，未開始）+ **P3-7-C** wordlist 對自家 vocabulary 分類校正（4 條 ⬜，未開始）+ **P3-7-D** sample / mock test toolkit 題型觀察筆記（4 條 ⬜，未開始）；P3-7 整體狀態 ⬜ → 🟡 進行中。`README.md`「長期方向」段補一段官方資源索引文件指向 + 文件索引追加 `docs/OFFICIAL_RESOURCES.md`。`source_materials/README.md`「官方資源與歷史題整理原則」段補一句「**詳細官方資源連結索引請看 `docs/OFFICIAL_RESOURCES.md`**」交叉指向。本輪**未動** `lib/types.ts` / `lib/data.ts` / 任何 `data/*.json` / `/quiz` UI / 任何 components / 所有 app routes / `docs/PRODUCT_SPEC.md` / `docs/DATA_SCHEMA.md` / `docs/AI_QUESTION_GENERATION.md` / `docs/STARTERS_PART_TEMPLATES.md` / `AI_DEV_WORKFLOW.md` / `AGENTS.md` / `CLAUDE.md`；**未寫**爬蟲、未下載官方 PDF / 圖片 / 音檔、未複製官方題目 / 歷屆題、未新增官方資源檔案到 source_materials、**未新增**任何題目資料、**未做** AI API / TTS / 錄音 / STT / Speaking UI / localStorage / 結果頁 / 錯題頁、**未新增**依賴、**未處理** npm audit、**未部署**、**未新增**後端 / DB / 登入。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-7-A / P3-9-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B / P3-7-B / P3-7-C / P3-7-D / P3-8 / P3-9-B / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-08：完成 P3-9-A 正式題型模板文件第一版——本輪純文件，**零程式碼變動**。新增 `docs/STARTERS_PART_TEMPLATES.md`（v1）：用繁體中文撰寫，明示「自製練習模板，不是官方題庫、不複製官方題目」+ 硬邊界（不抄歷屆題 / 不下載官方素材 / 不爬蟲 / 不外部 URL / 沒有「官方題」這個 source 值）。完整三大段架構（Listening Part 1~4 / Reading & Writing Part 1~5 / **Speaking 不在 P3，留 P4**）。**Listening Part 1~4** 模板每 Part 12 欄位（官方方向 / 練習版目標 / 互動方式 / 資料欄位 / imagePrompt / ttsScript / answer 型態 / 目前 schema 是否支援 / 未來補哪些功能 / 是否需要圖片 / 是否需要音檔 / 是否適合 AI 仿真）；L1 大圖位置連線（簡化為選人 / 選位置）/ L2 對話寫 name / number（簡化為文字輸入）/ L3 聽音選 A/B/C 圖（最接近現有 listening-choice）/ L4 聽指令塗顏色（簡化為選顏色 / 選物件）。**Reading & Writing Part 1~5** 模板每 Part 10 欄位（同上去掉 ttsScript 與音檔）；RW1 看圖 + 句子 yes/no（對應 true-false / picture-choice 未來版）/ RW2 大場景圖回答 yes/no（需要 scene image + 多題共用，schema 待補）/ RW3 看圖拼字（對應「只顯示圖片不顯示英文，孩子自己拼字」未來單字拼字測驗模式）/ RW4 短文 / 句子填空（對應現有 fill-blank，多空格待補）/ RW5 看故事圖一字答案（需要 3 張圖 / scene + one-word answer，schema 待補）。**目前 P3 schema 對應表**：listening-choice → L3 preview / picture-choice → RW1 / RW2 preview / word-choice → RW3 preview（看圖認字反向） / multiple-choice → RW4 preview / fill-blank → RW4 / matching → RW5 preview，明示「練習版近似對應、不是完整正式題型」。**未來題型資料欄位建議**：starterSection / starterPart（含 SP1~SP4 對齊 P4）/ skillFocus / ttsScript / imagePrompt / expectedAnswerType（choice / text / number / name / color / one-word / spelling / spoken）/ difficulty（starter-easy / starter-medium）/ 其他（imageHotspots / sharedSceneImage / images[] / colorPalette / instructions[] / wordBank / multiBlankAnswers）；**明示本輪只寫建議，不修改 lib/types.ts / 任何 data JSON**。**AI 仿真題 prompt 如何使用模板**：8 步流程（選 section + part → 套用 part template → 限制 vocabulary → 生成 imagePrompt / ttsScript → 生成 answer / explanation → 人工審核 → 進 source_materials → 轉正式 data JSON），對齊 P3-3 / P3-8。**與 P4 Speaking Examiner Agent 關係**：P3 不實作 Speaking、SP1~SP4 模板未來歸屬 `docs/SPEAKING_EXAMINER_AGENT_DESIGN.md`（規劃中）、AI 回饋不是官方成績、處處標示。`PROJECT_ROADMAP.md` P3-9 從單一 ⬜ 子分區拆為三段：**P3-9-A**（10 條 ✅，本輪完成）+ **P3-9-B** part-specific schema / metadata 實作（11 條 ⬜，未開始）+ **P3-9-C** part-specific quiz UI 實作（10 條 ⬜，未開始）；P3-9 整體狀態 ⬜ → 🟡 進行中。`README.md`「長期方向」段補一段說明已新增正式 Starters parts 模板文件、目前仍是練習版近似對應、未來會依模板逐步對齊 L1~L4 + RW1~RW5、Speaking 留 P4。本輪**未動** `lib/types.ts` / `lib/data.ts` / 任何 `data/*.json` / `/quiz` UI / 任何 components / 所有 app routes / 任何素材；**未做** localStorage / 完整結果頁 / 錯題頁 / Speaking UI / TTS / 錄音 / STT / AI API / crawler / 部署 / 後端 / DB / 登入；**未下載**官方 PDF / 圖片 / 音檔；**未複製**官方題目 / 歷屆題；**未新增**依賴；**未處理** npm audit。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（路由 88 不變）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A / P3-9-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B / P3-7 / P3-8 / P3-9-B / P3-9-C 全 ⬜），P4 / P5 仍 ⬜。
- 2026-05-08：P3 / P4 / P5 Roadmap 擴充——本輪只做文件 / Roadmap 規劃，**零程式碼變動**。`PROJECT_ROADMAP.md` 新增 P3-7（官方資源索引與人工整理流程）/ P3-8（AI 仿真題生成流程）/ P3-9（正式題型模板化，含 Listening Part 1~4 + R&W Part 1~5 模板，Speaking 不在 P3 實作）三個子分區皆 ⬜；P4 章節從歷史佔位「題型擴充 ⬜ 已併入 P3-4 / P3-5」**重新聚焦為「Speaking Examiner Agent 模擬考官系統」**，9 個子項全 ⬜（P4-1 流程設計 / P4-2 狀態機設計 / P4-3 TTS 考官語音 / P4-4 錄音與播放 / P4-5 STT / P4-6 追問 / 下一題控制 / P4-7 AI 口說評分器 / P4-8 session 紀錄與家長檢視 / P4-9 弱點分析）；P5 章節從歷史佔位「模擬考 ⬜ 已併入 P3-6」**重新聚焦為「完整仿真考試體驗」**——Listening + Reading & Writing + Speaking 串成完整模擬考、TTS 考官貫穿、成績紀錄 / 家長檢視 / 弱點分析 / 錯題與口說弱點複習；P4 / P5 重要免責語「AI 口說分數只是練習回饋，不是 Cambridge 官方成績」「不聲稱能預測官方分數」「不把 AI 評分當成正式證明」「UI 必須標示『AI 練習回饋，非官方考試分數』」處處標示。「目前明確不做」清單把「AI 評分」改寫為**「客觀題的 AI 評分」**並加例外說明：P4 Speaking Examiner Agent 的口說練習回饋不是 AI 評分、是鼓勵性練習建議；客觀題 AI 評分仍維持不做。`docs/PRODUCT_SPEC.md` 新增「**長期目標：自家仿真 Starters 模擬考系統**」一節，含設計原則、Speaking Examiner Agent agent-based flow 設計（狀態機概念、考官台詞、回合控制、評分定位）、完整仿真考試體驗（P5）；「目前明確不做 → AI / 自動化」段同步重寫為「客觀題不做 AI 評分」+ Speaking 練習回饋例外。`source_materials/README.md` 新增「**官方資源與歷史題整理原則**」一節，列「可以做」（保存連結、人工筆記、自製題、AI 仿真題、自製圖片 / 音檔）與「不可做」（爬蟲、commit 官方 PDF / 圖片 / 音檔、複製歷屆題、用網路圖片當正式素材、聲稱 AI 題是官方題、外部 URL 進題庫資料）兩組明確邊界。`README.md` 新增「**長期方向**」一節，明示專案長期會往完整 Starters 仿真考試系統發展、目前仍在 P3 階段、Speaking / TTS / 錄音 / STT / AI 口說回饋屬 P4 後續、AI 回饋僅作練習建議不是官方成績。本輪**未動** `lib/types.ts` / `lib/data.ts` / 任何 `data/*.json` / 任何 components / 任何 app routes / `docs/DATA_SCHEMA.md` / `docs/AI_QUESTION_GENERATION.md` / `AI_DEV_WORKFLOW.md` / `docs/TASK_ROUTER.md` / `docs/CODEX_VALIDATION_RUNBOOK.md` / `AGENTS.md` / `CLAUDE.md`；**未寫**爬蟲、未下載官方 PDF / 圖片 / 音檔 / 歷屆題、**未新增**任何題目資料、**未做** AI API / TTS / 錄音 / STT / AI 評分 / Speaking UI / localStorage / `/quiz` 功能修改、**未新增**依賴、**未處理** npm audit、**未部署**、**未新增**後端 / DB / 登入。Codex 暫停期由 Claude 自測通過：`npm run lint` / `typecheck` / `build` 三項全綠（path 88 routes 不變）。P2 仍 🟡 進行中，P3 仍 🟡 進行中（P3-1 / P3-2-A / P3-3-A / P3-6-A ✅；P3-2-B / P3-3-B / P3-4 / P3-5 / P3-6-B / P3-7 / P3-8 / P3-9 全 ⬜），P4 / P5 重新進入 Roadmap 雷達但全部 ⬜。
- 2026-05-08：P3-6-A 小修——`/quiz` 對齊正式 Cambridge Pre A1 Starters parts 架構。`components/QuizPlay.tsx` 新增 `StarterPartInfo` type 與 `getStarterPartInfo()` 純函式 helper，把 6 題型映射到對應 Part 的近似標示：listening-choice → Part 3「聽音選圖」；picture-choice → Part 1 / Part 2 preview「看圖判斷 / 看圖選答案」；word-choice → Part 3「看圖認字 / 拼字練習」；multiple-choice → Part 4 preview「短句選字」；fill-blank → Part 4「短文 / 句子填空」；matching → Part 5 preview「圖文配對 / 故事理解預備」。題目卡 header 三列改寫：徽章合併段落英文｜中文「Section 1 · Listening｜聽力練習」/「Section 2 · Reading & Writing｜閱讀與書寫練習」（sky-100 / amber-100 配色）+ 第二行獨立顯示「Part X：題型中文」+ 第三行既有「第 X 題 / 共 N 題」進度。`app/quiz/page.tsx` 頁首補一行小字「目前為練習版，題型逐步對齊正式 Cambridge Starters」，避免使用者誤解為完整官方考卷；明示這是練習版近似對應、Speaking 不做、listening 真實音檔尚未實作。`PROJECT_ROADMAP.md` P3-6-A 子分區補一條 ✅；P2-4C-2B-2 補兩條未來規劃（單字閱讀練習模式、單字拼字測驗模式對應正式 Reading & Writing Part 3，皆屬 review 區獨立練習，**本輪不實作**）。`README.md` 同步「目前功能」`/quiz` 條目補 Part 標示說明。本輪零依賴新增、未動 `lib/types.ts` / `lib/data.ts` / 任何 `data/*.json` / 任何 `/review/` 路由 / 任何其他 components；未實作單字閱讀模式 / 拼字測驗模式 / Speaking / localStorage / 完整結果頁 / 錯題頁 / AI API。Codex 暫停期由 Claude 自測通過：lint / typecheck / build 三項全綠 + dev smoke test 8 條路由 200 + `/quiz` SSR 第 1 題 visible HTML 含「Part 3：聽音選圖」字串 + 徽章合併段落英文中文「Listening｜聽力練習」+ 頁首練習版聲明。P3-6-A ✅ 增加一條，P3 整體仍 🟡 進行中。
